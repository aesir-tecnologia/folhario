---
phase: 06-identification-flow-cost-controls
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/e2e/fixtures/consented-user.ts
  - tests/integration/setup-identification.ts
  - tests/integration/identification/.gitkeep
  - tests/unit/identification/.gitkeep
  - playwright.config.ts
autonomous: true
requirements:
  - IDENT-02
  - IDENT-19
must_haves:
  truths:
    - "consentedUser Playwright fixture exists and inserts the identification_third_party ConsentLog row alongside the standard signup ConsentLogs"
    - "tests/integration/setup-identification.ts exposes a fetch-mock harness for Plant.id v3 + OpenAI vision endpoints and a helper to reset last_alerted_at + provider_circuit_breakers between tests"
    - "playwright.config.ts forces IDENTIFICATION_PROVIDER_MODE=stub for all e2e runs so identify-* specs never hit real providers"
    - "Per-context test directories (tests/integration/identification/, tests/unit/identification/) exist as gitkeep stubs ready for downstream plans to populate"
  artifacts:
    - path: "tests/e2e/fixtures/consented-user.ts"
      provides: "consentedUser Playwright fixture extending authedUser with identification consent"
      exports: ["consentedUserTest", "test"]
      min_lines: 30
    - path: "tests/integration/setup-identification.ts"
      provides: "fetch mock helpers + DB reset helpers for identification tests"
      exports: ["mockPlantIdSuccess", "mockPlantIdFailure", "mockOpenAISuccess", "mockOpenAIFailure", "resetCircuitBreakers", "resetLastAlertedAt"]
    - path: "playwright.config.ts"
      provides: "IDENTIFICATION_PROVIDER_MODE=stub env override for e2e"
      contains: "IDENTIFICATION_PROVIDER_MODE"
  key_links:
    - from: "tests/e2e/fixtures/consented-user.ts"
      to: "tests/e2e/fixtures/authed-user.ts"
      via: "extension via test.extend"
      pattern: "test\\.extend"
    - from: "tests/integration/setup-identification.ts"
      to: "vi.spyOn(global, 'fetch')"
      via: "Vitest fetch mock per RESEARCH line 1057"
      pattern: "vi\\.spyOn.*fetch"
---

<objective>
Stand up Phase 6 test scaffolding — Wave 0 — so subsequent waves write real tests, not test infrastructure.

Per RESEARCH §Validation Architecture and 06-VALIDATION.md, Phase 6 needs: (1) a `consentedUser` Playwright fixture (extends Phase 5's `authedUser` with the `identification_third_party` ConsentLog row), (2) an integration-test setup helper that mocks Plant.id v3 + OpenAI vision HTTP responses (project does NOT use MSW per RESEARCH line 1057 — use `vi.spyOn(global, 'fetch')`), (3) reset helpers for `provider_circuit_breakers.state` and `provider_budgets.last_alerted_at` between tests, and (4) `IDENTIFICATION_PROVIDER_MODE=stub` enforced in `playwright.config.ts` so e2e never hits real providers.

Purpose: Every Phase 6 test plan can `import { test } from "tests/e2e/fixtures/consented-user"` or call helpers from `tests/integration/setup-identification.ts` instead of re-implementing them. Saves ~60% boilerplate per downstream plan.

Output: Reusable test fixtures + helpers committed; per-context test dirs created as `.gitkeep` stubs.
</objective>

<execution_context>
@/Users/machado/Projects/folhario/.claude/get-shit-done/workflows/execute-plan.md
@/Users/machado/Projects/folhario/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-identification-flow-cost-controls/06-CONTEXT.md
@.planning/phases/06-identification-flow-cost-controls/06-RESEARCH.md
@.planning/phases/06-identification-flow-cost-controls/06-PATTERNS.md
@tests/e2e/fixtures/authed-user.ts
@tests/integration/setup.ts
@tests/integration/create-plant.integration.test.ts
@playwright.config.ts

<interfaces>
<!-- Existing fixture pattern to extend (Phase 5). -->

From tests/e2e/fixtures/authed-user.ts (excerpt — full pattern in PATTERNS Domain 10):
```typescript
export const test = base.extend<{ authedUser: AuthedUser }>({
  authedUser: async ({ context }, use) => {
    const email = `playwright-authed-${Date.now()}-...@folhario.test`;
    const password = "TestPassword123!";
    const { id: userId } = await seedUser({ email, password, emailVerifiedAt, ageConfirmedAt, trialSource: "organic" });
    const policyVersions = await seedCurrentPolicyVersions();
    const sql = postgres(process.env.DATABASE_POOL_URL!, { prepare: false, max: 1 });
    try {
      await sql`INSERT INTO consent_logs (user_id, purpose, legal_basis, source, policy_version_id, granted_at)
                VALUES (${userId}, 'signup_acceptance', 'contract', 'signup', ${policyVersions.termsOfService.id}, now()),
                       (${userId}, 'signup_acceptance', 'contract', 'signup', ${policyVersions.privacyPolicy.id},  now())`;
      await sql`INSERT INTO subscriptions (user_id, provider, status, trial_start_date, trial_end_date)
                VALUES (${userId}, 'stripe', 'trialing', NOW(), NOW() + INTERVAL '14 days')`;
    } finally { await sql.end({ timeout: 5 }); }
    // ... sets cookies on the context, calls use(), tears down ...
  },
});
```

The Phase 6 fixture extends this by inserting ONE additional consent_logs row for `identification_third_party`. See PATTERNS Domain 10 line 1041 for the verbatim INSERT.

From tests/integration/create-plant.integration.test.ts (excerpt):
```typescript
const inngestSendMock = vi.fn().mockResolvedValue({ ids: ["test-event-id"] });
vi.mock("@shared/inngest/client", () => ({
  inngest: { send: inngestSendMock },
}));

const mockCapture = vi.fn();
vi.mock("@shared/telemetry/posthog-server", () => ({
  getPostHog: vi.fn(() => ({ capture: mockCapture, shutdown: vi.fn().mockResolvedValue(undefined) })),
}));
```

These same mocks are already established; Phase 6 setup-identification.ts adds a `fetch` mock harness on top.
</interfaces>

<scrubbing_invariant>
The Sentry scrub regex `/\/api\/v1\/identifications(\/|$)/` (src/shared/telemetry/sentry-scrub.ts:15) ALREADY covers `/api/v1/identifications`, `/api/v1/identifications/{id}/confirm`, and `/api/v1/identifications/{id}/correct` because `(\/|$)` matches a slash boundary. NO scrub-list change needed in Phase 6. Plans 06-11 (route handlers) verify this with a regression test; this Wave 0 plan does NOT modify the scrub module.
</scrubbing_invariant>
</context>

<tasks>

<task type="auto">
  <name>Task 1: consentedUser Playwright fixture</name>
  <files>tests/e2e/fixtures/consented-user.ts, tests/integration/identification/.gitkeep, tests/unit/identification/.gitkeep</files>
  <read_first>
    - tests/e2e/fixtures/authed-user.ts (full file — copy structure verbatim)
    - .planning/phases/06-identification-flow-cost-controls/06-PATTERNS.md (Domain 10 — fixture extension pattern, lines 1003-1046)
    - .planning/phases/06-identification-flow-cost-controls/06-CONTEXT.md (`<specifics>` line 227)
  </read_first>
  <action>
    1. Create `tests/e2e/fixtures/consented-user.ts`:
       - DO NOT re-implement seedUser / seedCurrentPolicyVersions — import from the same modules `authed-user.ts` imports (`tests/e2e/fixtures/seed-user.ts`, `tests/e2e/fixtures/seed-policy-versions.ts` — verify exact paths from the existing file).
       - Pattern: re-implement the WHOLE fixture from scratch using the same seed-helpers (do NOT extend `authed-user.ts`'s exported `test` — Playwright's `test.extend` cannot override an existing fixture; importing `authed-user`'s `test` and re-calling `.extend` would create a `consentedUser` fixture that re-runs `authedUser` setup PLUS its own — fine, but means extra DB rows. The simpler path is to copy the body and add ONE extra INSERT.).
       - Recommendation: import `test as base` from `@playwright/test` (NOT from `authed-user.ts`) and define a `consentedUser` fixture that does ALL of authedUser's work plus the extra `INSERT INTO consent_logs ('identification_third_party', ...)` row. Verbatim source for the third INSERT (PATTERNS Domain 10 line 1041):
         ```sql
         INSERT INTO consent_logs (user_id, purpose, legal_basis, source, policy_version_id, granted_at)
         VALUES (${userId}, 'identification_third_party', 'consent', 'identify_screen', ${policyVersions.privacyPolicy.id}, now())
         ```
       - Type alias: `type ConsentedUser = AuthedUser` (re-export the type from `authed-user.ts` so test files don't import two types). The `consentedUser` value shape is the same as `authedUser` — id, email, password, cookieValue.
       - Export `test` as the named export (matches `authed-user.ts` convention). Add `expect` re-export from `@playwright/test` so consumer specs only need to import from this one file.
    2. Create `tests/integration/identification/.gitkeep` and `tests/unit/identification/.gitkeep` (empty files). Per-context directories per RESEARCH §Validation Architecture line 1042; downstream plans (06-08, 06-09, etc.) populate them.
    Do NOT create test files yet — those land in plans 06-05+ alongside the use-cases under test.
  </action>
  <verify>
    <automated>test -f tests/e2e/fixtures/consented-user.ts && test -f tests/integration/identification/.gitkeep && test -f tests/unit/identification/.gitkeep && grep -c "identification_third_party" tests/e2e/fixtures/consented-user.ts | awk '$1 < 1 { exit 1 }' && grep -c "test\.extend\|base\.extend" tests/e2e/fixtures/consented-user.ts | awk '$1 < 1 { exit 1 }' && pnpm exec tsc --noEmit 2>&1 | grep -E "consented-user\.ts" && exit 1 || true</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/e2e/fixtures/consented-user.ts` exists with `>= 30` non-comment lines
    - File contains literal string `identification_third_party` (the consent purpose, used in the extra INSERT)
    - File contains either `test.extend` or `base.extend` (Playwright fixture pattern)
    - File contains `INSERT INTO consent_logs` and `identify_screen` (the source value from D-10)
    - `tsc --noEmit` produces no errors referencing the new file
    - Both `.gitkeep` files exist (`tests/integration/identification/.gitkeep`, `tests/unit/identification/.gitkeep`)
  </acceptance_criteria>
  <done>Playwright fixture compiles; identification consent row inserts; per-context test dirs exist.</done>
</task>

<task type="auto">
  <name>Task 2: Integration test helpers + Playwright stub-mode env</name>
  <files>tests/integration/setup-identification.ts, playwright.config.ts</files>
  <read_first>
    - tests/integration/setup.ts (full file — observe extension pattern; Phase 6 helpers are an ADDITIVE module)
    - tests/integration/create-plant.integration.test.ts (lines 30-78 — `vi.mock` patterns to mirror)
    - playwright.config.ts (full file — find the `use.env` / `webServer.env` block)
    - .planning/phases/06-identification-flow-cost-controls/06-RESEARCH.md (Pattern 2 lines 388-436 for Plant.id response shape; Pattern 3 lines 444-526 for OpenAI shape; lines 1057 for "vi.spyOn(global, fetch)" recommendation)
  </read_first>
  <action>
    1. Create `tests/integration/setup-identification.ts` exporting these helpers (one module — easier to audit than five files):
       - **`mockPlantIdSuccess(suggestions: Array<{name: string, common: string, probability: number}>)`** — installs a `vi.spyOn(global, 'fetch')` mock that, when the URL matches `/v3/identification`, returns a Response with the Plant.id v3 shape `{result: {classification: {suggestions: [{name, probability, details: {common_names: [...]}}]}}}`. Use `Response.json(...)` (Node 22 native).
       - **`mockPlantIdFailure(opts: {status: number, body?: string})`** — same hook, returns the configured non-2xx Response.
       - **`mockOpenAISuccess(results: Array<{speciesName, scientificName, confidence}>)`** — when the URL matches `/v1/chat/completions`, returns `{choices: [{message: {content: JSON.stringify({results})}}]}` (per RESEARCH Pattern 3 the provider parses `choices[0].message.content` as JSON).
       - **`mockOpenAIFailure(opts: {status: number})`** — same hook, non-2xx.
       - **`mockProviderTimeout(provider: 'plant_id' | 'openai_compat', delayMs: number)`** — returns a Promise that resolves after `delayMs`; intended to be paired with `vi.useFakeTimers()` in the caller. The fetch mock returns a fetch-like promise that rejects with `DOMException("aborted", "AbortError")` if the AbortController fires before `delayMs`.
       - **`resetCircuitBreakers(db: postgres.Sql)`** — `await sql\`UPDATE provider_circuit_breakers SET state='closed', consecutive_failures=0, opened_at=NULL, last_failure_at=NULL, in_flight_at=NULL\``.
       - **`resetLastAlertedAt(db: postgres.Sql)`** — `await sql\`UPDATE provider_budgets SET last_alerted_at=NULL\``.
       - **`resetProviderUsageCounters(db: postgres.Sql)`** — `await sql\`DELETE FROM provider_usage_counters\``. (Used by COST-04 / COST-09 tests to start each case fresh.)
       - **`installProviderFetchMock()`** — convenience wrapper that returns `{restore: () => void, plantIdCalls: ReadonlyArray<RequestInit>, openaiCalls: ReadonlyArray<RequestInit>}` so callers can assert "no provider was called" (COST-03 assertion).
       - At the top of the file, document the contract: "Call `installProviderFetchMock()` in `beforeEach` and `restore()` in `afterEach`. The mock matches by URL substring `/v3/identification` (Plant.id) and `/chat/completions` (OpenAI-compat); any other fetch URL falls through to the real `fetch`."
       - Write all helpers as named exports — no default export (matches project convention).
    2. Edit `playwright.config.ts`:
       - Find the `use` block (or `webServer.env` block — verify which exists by reading the file). Add `IDENTIFICATION_PROVIDER_MODE: 'stub'` to whichever env block forwards into the test server.
       - If both `use` and `webServer` exist, add to `webServer.env` (test server runs the route handler — that's where the env var is read).
       - Comment immediately above the line: `// Phase 6: identification e2e specs MUST run against stub providers (RESEARCH §Risk 2 + Phase 1 D-05 SC-4). Real providers (Plant.id, OpenAI-compat) are bypassed via IDENTIFICATION_PROVIDER_MODE=stub.`
    3. Do NOT create unit tests against the helpers themselves — they ARE test infrastructure. They'll be exercised by the integration tests in plans 06-08+.
  </action>
  <verify>
    <automated>test -f tests/integration/setup-identification.ts && grep -c "installProviderFetchMock\|mockPlantIdSuccess\|mockOpenAISuccess\|resetCircuitBreakers" tests/integration/setup-identification.ts | awk '$1 < 4 { exit 1 }' && grep -c "IDENTIFICATION_PROVIDER_MODE" playwright.config.ts | awk '$1 < 1 { exit 1 }' && grep -c "stub" playwright.config.ts | awk '$1 < 1 { exit 1 }' && pnpm exec tsc --noEmit 2>&1 | tee /tmp/06-02-task2-tsc.log | (grep -E "setup-identification\.ts|playwright\.config\.ts" && exit 1 || exit 0)</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/integration/setup-identification.ts` exists; exports `installProviderFetchMock`, `mockPlantIdSuccess`, `mockOpenAISuccess`, `resetCircuitBreakers` (>= 4 of the listed names match the grep)
    - `tsc --noEmit` produces no errors referencing the new file or `playwright.config.ts`
    - `playwright.config.ts` references `IDENTIFICATION_PROVIDER_MODE` and `stub` (env override is wired)
    - The mock fetch-spy delegates non-matching URLs to the real fetch (so existing Supabase calls in integration tests are not intercepted)
  </acceptance_criteria>
  <done>Test helpers exist; e2e env override wired; integration suite ready for Phase 6 use-case tests.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test fixture → DB seed | Seed inserts cross from test code into the DB; trusted because tests use the local Supabase instance (CI uses postgres:17-alpine container) |
| Test fetch mock → in-test code | Mock spy replaces `global.fetch` for the duration of a test; restoration is `afterEach`-managed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02-01 | I (Information Disclosure) | consented-user.ts in CI logs | mitigate | Fixture sets a per-test unique `Date.now()`-suffixed email; no real email or PII in logs. Matches authed-user.ts pattern. |
| T-06-02-02 | T (Tampering) | global.fetch monkey-patch | mitigate | `installProviderFetchMock()` returns a `restore()` function; downstream tests MUST call it in `afterEach`. URL-substring match prevents accidental interception of unrelated fetches. |
| T-06-02-03 | E (Elevation) | IDENTIFICATION_PROVIDER_MODE in CI | accept | Forcing `stub` in playwright.config.ts is a defense — preventing real provider keys from being needed for e2e. Production deploys override this via Vercel env (per Phase 1 D-05). |
| T-06-02-04 | I | consent-logs.identification_third_party row | accept | Test data only; ConsentLog rows have RLS but are inserted via service-role from the fixture (matches existing `authedUser` pattern). |

</threat_model>

<verification>
- `tsc --noEmit` clean
- `tests/e2e/fixtures/consented-user.ts` exports `test` and `expect`
- `tests/integration/setup-identification.ts` exports the four named helpers (grep-confirmed)
- `playwright.config.ts` has IDENTIFICATION_PROVIDER_MODE=stub in the env block forwarded to the test server
- Per-context test directories exist (tracked via `.gitkeep`)
</verification>

<success_criteria>
- Wave 0 test scaffolding committed; downstream plans can `import { test } from "tests/e2e/fixtures/consented-user"` and `import { installProviderFetchMock, ... } from "tests/integration/setup-identification"`
- No real test files added; this plan stages infrastructure only
- E2E identify specs cannot accidentally hit real Plant.id or OpenAI APIs (stub-mode enforced at config layer)
- The Sentry scrub regex `/\/api\/v1\/identifications(\/|$)/` (already in `src/shared/telemetry/sentry-scrub.ts:15`) is verified to cover the new sub-routes (`…/confirm`, `…/correct`) — NO module change needed in this plan; documented in the SUMMARY as a regression-test target for plan 06-11
</success_criteria>

<output>
After completion, create `.planning/phases/06-identification-flow-cost-controls/06-02-SUMMARY.md`. SUMMARY MUST note:
1. Fixture import path for downstream plans (`tests/e2e/fixtures/consented-user`)
2. Helper module import path (`tests/integration/setup-identification`)
3. Confirmation that `IDENTIFICATION_PROVIDER_MODE=stub` is enforced in `playwright.config.ts`
4. Reminder to plan 06-11: assert via Playwright that POST `/api/v1/identifications` request body is dropped from Sentry breadcrumbs (Phase 1 D-22 invariant)
</output>
