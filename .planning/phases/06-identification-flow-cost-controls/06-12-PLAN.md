---
phase: 06-identification-flow-cost-controls
plan: 12
type: execute
wave: 2
depends_on: ["06-04"]
files_modified:
  - src/shared/ui/confidence-ladder.tsx
  - src/shared/ui/ai-provenance-chip.tsx
  - src/shared/ui/cap-hit-chip.tsx
  - src/shared/ui/capture-guide.tsx
  - tests/unit/identification/confidence-ladder.unit.test.tsx
  - tests/unit/identification/ai-provenance-chip.unit.test.tsx
  - tests/unit/identification/cap-hit-chip.unit.test.tsx
  - tests/unit/identification/capture-guide.unit.test.tsx
autonomous: true
requirements:
  - UI-15
must_haves:
  truths:
    - "ConfidenceLadder renders three tiers (high ≥0.70, medium 0.40-0.6999, low ≥min_confidence-0.3999) with FOUR redundant signals: bar fill color + segment count + numeric percentage + sr-only tier label"
    - "ConfidenceLadder respects prefers-reduced-motion: spring fill becomes instant render at target width"
    - "AiProvenanceChip exposes size prop (sm | md); sm=12px label/4px vertical pad, md=14px label/8px vertical pad; Lucide Sparkles 18px Trust Teal"
    - "CapHitChip two-line label using Intl.RelativeTimeFormat('pt-BR'); Lucide Clock 18px Overdue Rust; chip height 48px"
    - "CaptureGuide 2x2 grid with three Lucide icons (Leaf, Flower2, TreePine) at 24px Understory Sage stroke + one text card 'Mais fotos melhoram a precisão'; role='img' on container with aggregate aria-label"
    - "All four primitives axe-clean in light + dark themes (axe-core unit-test gate per UI-SPEC § Verification Gates)"
  artifacts:
    - path: "src/shared/ui/confidence-ladder.tsx"
      provides: "Confidence ladder primitive with four-cue redundancy"
      exports: ["ConfidenceLadder"]
    - path: "src/shared/ui/ai-provenance-chip.tsx"
      provides: "Gerado por IA pill (sm + md sizes)"
      exports: ["AiProvenanceChip"]
    - path: "src/shared/ui/cap-hit-chip.tsx"
      provides: "Limite atingido two-line pill"
      exports: ["CapHitChip"]
    - path: "src/shared/ui/capture-guide.tsx"
      provides: "Static photo capture guide composer"
      exports: ["CaptureGuide"]
  key_links:
    - from: "src/shared/ui/confidence-ladder.tsx"
      to: "@/messages/pt-BR.json"
      via: "useTranslations('identify.results') for 'Confiança alta/média/baixa' SR labels"
      pattern: "useTranslations"
---

<objective>
Land four of Phase 6's seven new UI primitives (Group A — semantically simpler, axe-on-unit-test required). These are all server-renderable or `"use client"` only when motion is involved (ConfidenceLadder is the only one with `"use client"` because its bar fill animates). Group B (PhotoStrip + IdentificationResultCard + IdentificationHistoryItem) ships in 06-13 alongside the identify page that consumes them.

Per UI-SPEC § Verification Gates: ConfidenceLadder MUST pass axe-core in 3 tiers × 2 themes × 2 motion preferences = 12 axe runs. The test infra for axe-on-unit-test is the project's first consumer (RESEARCH §Validation line 1049 + Domain 9 line 930). This plan stands up the axe-on-unit-test scaffolding alongside the primitive.

Output: 4 primitive files + 4 unit-DOM test files (using Vitest's unit-dom project + jsdom + axe-core).
</objective>

<execution_context>
@/Users/machado/Projects/folhario/.claude/get-shit-done/workflows/execute-plan.md
@/Users/machado/Projects/folhario/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-identification-flow-cost-controls/06-UI-SPEC.md
@.planning/phases/06-identification-flow-cost-controls/06-PATTERNS.md
@src/shared/typography/scientific-name.tsx
@src/shared/ui/empty-state.tsx
@src/shared/ui/skeleton.tsx
@messages/pt-BR.json
@vitest.config.ts

<interfaces>
<!-- ConfidenceLadder per UI-SPEC § 1 (lines 138-172). -->

```typescript
type ConfidenceLadderProps = {
  confidence: number; // 0..1, normalized at render
  segments?: number; // default 5
};
```

Tier mapping (UI-SPEC table line 152-158):
- high: confidence >= 0.70 — Canopy Green / Canopy Sprout fill — "Confiança alta, {n}%" SR
- medium: 0.40 <= confidence < 0.70 — Honey Amber / Honey Lantern fill — "Confiança média, {n}%" SR
- low: confidence < 0.40 — Calm Slate / Lantern Slate fill — "Confiança baixa, {n}%" SR

Visual:
- Bar 6px tall, full width, Hairline Beige bg, 3px radius
- Inner 4 segment-tick dividers at 20/40/60/80%
- Fill: confidence * 100% width, tier color, 3px left radius
- Percentage label: 13/18 weight 600 Forest Ink; format Math.round(confidence * 1000) / 10 + '%'; whole numbers render WITHOUT decimal ("70 %" not "70.0 %")
- SR-only span: "Confiança alta, 85 por cento" etc

Reduced motion: spring fill from 0% → confidence% on first render (~240ms primary spring); reduced-motion → instant.

<!-- AiProvenanceChip per UI-SPEC § 7 (lines 357-381). -->

```typescript
type AiProvenanceChipProps = {
  size?: "sm" | "md"; // sm = 12px label, md = 14px label
};
```

- 999px pill, Paper Cream bg, 1px Trust Teal stroke
- Lucide Sparkles 18px Trust Teal stroke at left, 8px gap
- Label "Gerado por IA" Plus Jakarta Sans weight 600
- sm: 12/16 label, vertical pad 4px (Phase 3 sub-8 token), horizontal pad 12px → height 26px
- md: 14/18 label, vertical pad 8px, horizontal pad 12px → height 34px
- role="status", aria-label="Conteúdo gerado por inteligência artificial"

<!-- CapHitChip per UI-SPEC § 8 (lines 386-409). -->

```typescript
type CapHitChipProps = {
  resetAt: Date; // formatted via Intl.RelativeTimeFormat('pt-BR')
};
```

- 999px pill, Paper Cream bg, 1px Overdue Rust stroke
- Lucide Clock 18px Overdue Rust at left, 8px gap
- Two-line label: line 1 "Limite atingido" 12/16 weight 600 Overdue Rust; line 2 "Próximo em {resetTime}" 12/16 weight 400 Calm Slate
- Vertical pad 8px, horizontal pad 12px → height 48px
- role="status" aria-live="polite"

<!-- CaptureGuide per UI-SPEC § 2 (lines 176-198). -->

2x2 grid 64px tiles + 8px gap + 16px container padding.
- (0,0) Leaf icon + i18n label `identify.captureGuide.leaf`
- (0,1) Flower2 icon + `identify.captureGuide.flower`
- (1,0) TreePine icon + `identify.captureGuide.whole`
- (1,1) text-only card + `identify.captureGuide.morePhotos`
- Tiles 0,0 and 0,1 and 1,0: Warm Ivory bg, 8px radius, 1.5px Hairline Beige dashed stroke, icon centered, label below (Plus Jakarta Sans 12/16 weight 500 Calm Slate, max 2 lines + ellipsis)
- Tile 1,1: same bg + radius, NO stroke, text only
- Container role="img" + aria-label="Guia de fotos: folha próxima, flor se houver, planta inteira, mais fotos melhoram a precisão"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ConfidenceLadder + axe-on-unit scaffolding</name>
  <files>src/shared/ui/confidence-ladder.tsx, tests/unit/identification/confidence-ladder.unit.test.tsx, vitest.config.ts (verify if axe-core dep needs to be added — check package.json first)</files>
  <read_first>
    - src/shared/typography/scientific-name.tsx (full file — pure server primitive analog)
    - src/shared/ui/skeleton.tsx (full file — motion + reduced-motion analog)
    - .planning/phases/06-identification-flow-cost-controls/06-UI-SPEC.md (§ ConfidenceLadder lines 138-172 verbatim)
    - vitest.config.ts (full file — verify the unit-dom project setup; check if jsdom + @testing-library/jest-dom are wired)
    - package.json (verify axe-core or vitest-axe is present; if not, this plan ADDS the dep — RESEARCH said no new deps for Phase 6, but axe-on-unit may require one)
  </read_first>
  <behavior>
    - Test 1 (high tier): confidence=0.85 → bar fill width=85%; renders "85 %" (no decimal); SR span contains "Confiança alta, 85 por cento"; bar fill color uses Canopy Green token class (`bg-canopy` or `text-canopy` from globals.css — verify Phase 3 D-04 token names)
    - Test 2 (medium tier): confidence=0.55 → "55 %"; SR "Confiança média, 55 por cento"; Honey Amber class
    - Test 3 (low tier): confidence=0.32 → "32 %"; SR "Confiança baixa, 32 por cento"; Calm Slate class
    - Test 4 (decimal handling): confidence=0.85432 → "85.4 %" (1 decimal max — banned-pattern enforcement)
    - Test 5 (whole percentage): confidence=0.7 → "70 %" NOT "70.0 %"
    - Test 6 (segments): renders 5 segment ticks (4 divider elements at 20/40/60/80%)
    - Test 7 (axe — high tier light theme): axe(container).violations.length === 0
    - Test 8 (axe — medium tier dark theme): wrap render in `<div data-theme='dark'>`, run axe; violations === 0
    - Test 9 (axe — low tier reduced-motion): set `matchMedia` mock for prefers-reduced-motion: reduce; render; axe; violations === 0
    - (covers UI-SPEC § Verification Gates "3 tiers × 2 themes × 2 motion preferences = 12 axe runs" — abbreviated to 3 representative combos here; Playwright 06-15 covers the full visual matrix)
  </behavior>
  <action>
    1. Verify package.json for axe-core. If absent, install:
       ```bash
       pnpm add -D vitest-axe
       ```
       (or `axe-core` + custom matcher — `vitest-axe` is the cleaner integration). Update `pnpm-lock.yaml`.
    2. Implement `src/shared/ui/confidence-ladder.tsx`:
       - `"use client"` directive (animates the bar fill via Phase 3 motion springs).
       - Uses `useTranslations("identify.results")` for the SR-label tier strings.
       - Tier resolution: `const tier = confidence >= 0.70 ? "high" : confidence >= 0.40 ? "medium" : "low";`
       - Percentage: `const pct = Math.round(confidence * 1000) / 10; const display = pct % 1 === 0 ? \`${pct.toFixed(0)} %\` : \`${pct.toFixed(1)} %\``
       - Bar fill animation: use `motion/react` (already in package.json) with `springs.primary` from Phase 3 motion contract; `prefers-reduced-motion` honored by setting initial = target.
       - Tailwind classes use Phase 3 @theme tokens (e.g. `bg-canopy`, `bg-honey`, `bg-slate`, `bg-hairline`); verify exact token names in `src/app/globals.css`.
       - SR span uses `<span className="sr-only">`.
    3. Write `tests/unit/identification/confidence-ladder.unit.test.tsx` per behaviors. Pattern:
       ```typescript
       import { render } from "@testing-library/react";
       import { axe } from "vitest-axe";
       import { describe, it, expect } from "vitest";
       import { ConfidenceLadder } from "@shared/ui/confidence-ladder";

       it("axe-clean at high tier", async () => {
         const { container } = render(<ConfidenceLadder confidence={0.85} />);
         const results = await axe(container);
         expect(results.violations).toEqual([]);
       });
       ```
       Mock matchMedia in `tests/unit/setup-env.ts` if not already (Phase 3 should have wired this).
    4. RED → GREEN cycle.
  </action>
  <verify>
    <automated>pnpm exec vitest run --project=unit-dom tests/unit/identification/confidence-ladder.unit.test.tsx 2>&1 | tee /tmp/06-12-task1.log | grep -E "Tests +[0-9]+ passed" && grep -c "Confiança alta\|Confiança média\|Confiança baixa" src/shared/ui/confidence-ladder.tsx | awk '$1 < 0 { exit 0 }' && grep -c "use client" src/shared/ui/confidence-ladder.tsx | awk '$1 < 1 { exit 1 }'</automated>
  </verify>
  <acceptance_criteria>
    - 9 tests pass including 3 axe-clean assertions
    - File contains the literal `"use client"` directive
    - Bar fill width is `${pct}%` style — confirmed by snapshot or DOM query
    - Reduced-motion mode renders without spring animation (no `motion.div` initial→animate transition)
  </acceptance_criteria>
  <done>ConfidenceLadder ships + axe-on-unit-test infrastructure stood up.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AiProvenanceChip + CapHitChip + CaptureGuide</name>
  <files>src/shared/ui/ai-provenance-chip.tsx, src/shared/ui/cap-hit-chip.tsx, src/shared/ui/capture-guide.tsx, tests/unit/identification/{ai-provenance-chip,cap-hit-chip,capture-guide}.unit.test.tsx</files>
  <read_first>
    - src/shared/typography/scientific-name.tsx (analog for pure server-renderable primitive)
    - .planning/phases/06-identification-flow-cost-controls/06-UI-SPEC.md (§ AiProvenanceChip lines 357-381, § CapHitChip lines 386-409, § CaptureGuide lines 176-198)
  </read_first>
  <behavior>
    AiProvenanceChip tests (3 tests):
    - Test 1 (sm): renders Sparkles 18px + label "Gerado por IA" 12px; height 26px; aria-label="Conteúdo gerado por inteligência artificial"; role="status"
    - Test 2 (md): label 14px; height 34px
    - Test 3 (axe): violations === 0 light + dark

    CapHitChip tests (3 tests):
    - Test 1: resetAt = Date 2 hours from now → label includes "em 2 horas" (Intl.RelativeTimeFormat pt-BR auto-pluralizes)
    - Test 2: resetAt = Date tomorrow morning → label includes "amanhã" or "em 1 dia"
    - Test 3 (axe): violations === 0; role="status" aria-live="polite"

    CaptureGuide tests (3 tests):
    - Test 1: 2x2 grid renders 4 tiles; tiles 0-2 have icons (Leaf, Flower2, TreePine); tile 3 is text-only
    - Test 2: Container has role="img" + aria-label that includes "folha próxima", "flor se houver", "planta inteira", "mais fotos"
    - Test 3 (axe): violations === 0
  </behavior>
  <action>
    1. Implement three primitive files. AiProvenanceChip + CapHitChip are pure server components (no `"use client"` needed). CaptureGuide uses `useTranslations("identify.captureGuide")` so it needs to be either server or pure-client; since `useTranslations` works in server components via next-intl 4.x, render it server-side.
    2. CapHitChip uses `Intl.RelativeTimeFormat('pt-BR')` for the second line. Compute relative time from `resetAt` vs `new Date()` at render. If the chip ever needs to update live, the parent re-renders; for Phase 6, render-once-per-page-load is fine.
    3. Three unit-DOM test files. Each has 3 tests including the axe assertion.
    4. RED → GREEN.
  </action>
  <verify>
    <automated>pnpm exec vitest run --project=unit-dom tests/unit/identification/ai-provenance-chip.unit.test.tsx tests/unit/identification/cap-hit-chip.unit.test.tsx tests/unit/identification/capture-guide.unit.test.tsx 2>&1 | tee /tmp/06-12-task2.log | grep -E "Tests +[0-9]+ passed"</automated>
  </verify>
  <acceptance_criteria>
    - 9 tests pass (3 + 3 + 3)
    - Three primitive files exist; named exports
    - All four primitives (including ConfidenceLadder from Task 1) live in `src/shared/ui/` (consistent with Phase 3 / Phase 5 placement)
    - No Lucide icon used at a size outside the locked set {18, 20, 24, 28}: `grep -E "size={(18|20|24|28)}" src/shared/ui/{confidence-ladder,ai-provenance-chip,cap-hit-chip,capture-guide}.tsx | wc -l` >= 5; `grep -E "size={[0-9]+}" src/shared/ui/{...}.tsx | grep -vE "size={(18|20|24|28)}"` returns empty
  </acceptance_criteria>
  <done>Group-A primitives ship; Lucide locked-set conformance verified.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Untrusted text (e.g., manualCorrection, plant names) → JSX | React auto-escapes — text injection prevented |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-12-01 | T (Tampering) | XSS via plant name in chip | mitigate | React auto-escapes children; no `dangerouslySetInnerHTML` in any primitive |
| T-06-12-02 | I (Information Disclosure) | aria-label leaks PII | accept | All aria-labels are static i18n strings; no user data interpolated |
| T-06-12-03 | E (Elevation) | matchMedia mock leaking into prod | accept | matchMedia mocking is test-setup-only (`tests/unit/setup-env.ts`); production reads the real API |

</threat_model>

<verification>
- All 18 unit-DOM tests pass (9 + 9)
- `tsc --noEmit` clean for all four primitive files
- axe-core wired into the unit-dom project (vitest-axe added if missing)
- Banned-pattern lint regression test (Phase 3 existing) verifies these primitives don't use `<Spinner>` or circular-spinner classes
</verification>

<success_criteria>
- UI-15 (confidence ladder 4-cue redundancy) covered by ConfidenceLadder unit tests + axe gate
- AiProvenanceChip is the FIRST consumer (Phase 7 will reuse on care-guide augmented chip — UI-SPEC line 358 confirms)
- CapHitChip is the FIRST consumer (Phase 6 § State 9 cap-hit screen)
- CaptureGuide is consumed by Identify page state 1, 2, 3 (idle/consent-modal/picking) per UI-SPEC State Machine table
- Phase 6 introduces ZERO Lucide icon sizes outside {18, 20, 24, 28} (Phase 3 D-05 lock honored — verified by Task 2 acceptance criteria grep)
</success_criteria>

<output>
After completion, create `.planning/phases/06-identification-flow-cost-controls/06-12-SUMMARY.md`. SUMMARY MUST note:
1. Four primitives shipped (ConfidenceLadder, AiProvenanceChip, CapHitChip, CaptureGuide)
2. axe-core wiring (vitest-axe was/wasn't added as a dep)
3. Lucide locked-set conformance verified
4. Reminder for 06-13: PhotoStrip + IdentificationResultCard + IdentificationHistoryItem (Group B) ship in 06-13 alongside the identify page
</output>
