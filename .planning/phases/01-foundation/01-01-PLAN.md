---
phase: 01
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .nvmrc
  - .gitignore
  - package.json
  - pnpm-lock.yaml
  - tsconfig.json
  - eslint.config.mjs
  - .prettierrc
  - .prettierignore
  - .commitlintrc.json
  - .husky/pre-commit
  - .husky/commit-msg
  - vitest.config.ts
  - playwright.config.ts
  - .env.example
autonomous: true
requirements:
  - INFRA-01
  - INFRA-17
  - INFRA-23
tags:
  - scaffold
  - tooling
  - test-infrastructure

must_haves:
  truths:
    - "Running `pnpm install --frozen-lockfile` succeeds from a clean clone"
    - "`pnpm typecheck` exits 0 against an empty src/"
    - "`pnpm lint` exits 0 with no src files yet"
    - "`pnpm exec vitest --run` discovers the unit + integration projects and reports 0 tests (no files yet)"
    - "`pnpm exec playwright --version` returns 1.59.1"
    - "Committing a file that violates Prettier rules triggers lint-staged via the pre-commit hook"
    - "Committing with a non-Conventional-Commits message is rejected by the commit-msg hook (stateless commitlint --stdin check)"
    - "`.env.example` contains every PRD §20 variable name (19 variables) with dummy values"
    - "`.gitignore` blocks `.env`, `.env.local`, `node_modules`, `.next`, `playwright-report`, `test-results`, `coverage`, `.vitest-cache`, `supabase/.temp`, `supabase/.branches` but does NOT block `next-env.d.ts` (Next regenerates it per build)"
    - "`eslint.config.mjs` line 1 is a comment `// verified from create-next-app@16.2.4 output` documenting the verified upstream API shape (explicit exception to 'no comments' rule per user decision)"
  artifacts:
    - path: ".nvmrc"
      provides: "Node 22 LTS version pin (D-01)"
      contains: "22"
    - path: "package.json"
      provides: "Corepack-activated pnpm, dependency pins, npm scripts, lint-staged config"
      exports: ["scripts.dev", "scripts.build", "scripts.start", "scripts.lint", "scripts.typecheck", "scripts.test:unit", "scripts.test:integration", "scripts.test:e2e", "scripts.db:start", "scripts.db:reset", "scripts.db:sync-env", "scripts.supabase", "scripts.prepare"]
    - path: "tsconfig.json"
      provides: "TS strict + noUncheckedIndexedAccess + per-root path aliases @contexts/*, @shared/*, @i18n/*"
      contains: "noUncheckedIndexedAccess"
    - path: "eslint.config.mjs"
      provides: "Flat ESLint config wrapping eslint-config-next + eslint-config-prettier (D-03); import shape verified via pre-flight spike"
    - path: ".prettierrc"
      provides: "Prettier 3 formatting rules"
    - path: ".commitlintrc.json"
      provides: "Conventional Commits enforcement via @commitlint/config-conventional"
    - path: ".husky/pre-commit"
      provides: "Pre-commit hook running lint-staged (Husky 9 plain-script idiom)"
    - path: ".husky/commit-msg"
      provides: "Commit-msg hook running commitlint"
    - path: "vitest.config.ts"
      provides: "Two Vitest projects (unit + integration) via test.projects (v4 API, replaces workspace)"
      contains: "projects"
    - path: "playwright.config.ts"
      provides: "Chromium-only E2E runner + webServer against next start; probe URL = `/` (not `/__diag` — avoids 404 between Plans 1–7)"
      contains: "chromium"
    - path: ".env.example"
      provides: "PRD §20 env var template (19 variables) with dummy values, grouped by phase of first use"
      contains: "IDENTIFICATION_PROVIDER_MODE"
    - path: ".gitignore"
      provides: "Full Next + Node + Supabase + test artifact exclusions; `next-env.d.ts` NOT included (auto-generated by Next per build)"
      contains: ".env.local"
  key_links:
    - from: "package.json scripts.prepare"
      to: ".husky/"
      via: "Husky 9 `husky` CLI"
      pattern: "\"prepare\":\\s*\"husky\""
    - from: "package.json lint-staged"
      to: ".husky/pre-commit"
      via: "pnpm lint-staged invocation"
      pattern: "pnpm lint-staged"
    - from: "tsconfig.json paths"
      to: "vitest.config.ts tsconfigPaths plugin"
      via: "vite-tsconfig-paths resolves aliases at test time"
      pattern: "tsconfigPaths\\(\\)"
---

<objective>
Bootstrap the repository tooling: Node 22 pin, Corepack pnpm, TS strict + aliases, ESLint/Prettier, Husky + lint-staged + commitlint, Vitest 4 projects config, Playwright 1.59 config, `.gitignore` extension, and `.env.example` mirroring PRD §20 (19 variables). No source code — this plan lays the surface every later plan lands on.

Purpose: Every subsequent Phase 1 plan (and every future phase) depends on `package.json` dependencies being installed, the TS compiler accepting source files, Prettier/ESLint running without errors, and the test runners discovering projects. Getting the tooling wrong cascades into every downstream plan failing. A pre-flight spike (Action 5) verifies the `eslint-config-next@16` flat-config API shape before committing to an import pattern that could break `pnpm lint` on first CI run.

Output: A clean clone can run `pnpm install --frozen-lockfile && pnpm typecheck && pnpm lint` without errors. Test runners report empty test suites. Git hooks reject bad commits via stateless `commitlint --stdin --strict`.
</objective>

<execution_context>
@/Users/machado/Projects/folhario/.claude/get-shit-done/workflows/execute-plan.md
@/Users/machado/Projects/folhario/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/machado/Projects/folhario/CLAUDE.md
@/Users/machado/Projects/folhario/.planning/ROADMAP.md
@/Users/machado/Projects/folhario/.planning/REQUIREMENTS.md
@/Users/machado/Projects/folhario/.planning/phases/01-foundation/01-CONTEXT.md
@/Users/machado/Projects/folhario/.planning/phases/01-foundation/01-RESEARCH.md
@/Users/machado/Projects/folhario/.planning/phases/01-foundation/01-PATTERNS.md
@/Users/machado/Projects/folhario/.planning/phases/01-foundation/01-VALIDATION.md
@/Users/machado/Projects/folhario/.planning/phases/01-foundation/01-FILE-MATRIX.md
@/Users/machado/Projects/folhario/.env
@/Users/machado/Projects/folhario/.gitignore

<interfaces>
<!-- Dependency pins (from RESEARCH.md §Standard Stack + PATTERNS.md package.json section, verified against npm registry on 2026-04-23) -->

Runtime dependencies (exact versions):
- next@16.2.3
- react@19.2.5
- react-dom@19.2.5
- @serwist/next@9.5.7
- serwist@9.5.7
- @sentry/nextjs@10.48.0
- posthog-js@1.368.0
- posthog-node@5.29.7
- next-intl@4.9.1
- zod@4

Dev dependencies (MUST be resolved to EXACT latest published patch via `pnpm view <pkg>@<major> version` per reviews Action 12):
- typescript@6
- @types/node@22 (NOT 25.x — must match Node 22 runtime)
- @types/react@19
- @types/react-dom@19
- eslint@10
- eslint-config-next@16.2.3
- eslint-config-prettier@10
- prettier@3
- vitest@4.1.4 (pinned — 4.1.5 drift acceptable but CLAUDE.md locks 4.1.4)
- @vitest/ui@4
- vite-tsconfig-paths@5 (resolves @contexts/*, @shared/*, @i18n/* in Vitest)
- @playwright/test@1.59.1
- husky@9
- lint-staged@16
- @commitlint/cli@20
- @commitlint/config-conventional@20
- supabase@2.95.0

TypeScript path aliases (MUST be in tsconfig.json — used by Plans 02/03/05a/05b/06/07):
- "@contexts/*" → "./src/contexts/*"
- "@shared/*"   → "./src/shared/*"
- "@i18n/*"     → "./src/i18n/*"   (reconciles R-1 from PATTERNS.md)

NPM scripts (downstream consumers expect these exact names):
- dev, build, start, lint, typecheck
- test:unit, test:integration, test:e2e
- db:start, db:stop, db:reset, db:sync-env, supabase
- prepare (husky registration)

Vitest project names (vitest.config.ts test.projects):
- "unit"        → tests/unit/**/*.test.ts
- "integration" → tests/integration/**/*.integration.test.ts

Playwright project name: "chromium" (D-26, Chromium only; webServer runs `pnpm start`).
Playwright webServer probe URL: `/` (production build returns 200 trivially; NEVER `/__diag` — that route only exists after Plan 07 and would 404 for every intermediate wave).

Environment variables (PRD §20) — MUST be enumerated in .env.example with dummy values (COUNT = 19):
Phase 1 infrastructure (8):
  DATABASE_URL, DATABASE_POOL_URL,
  NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY,
  SUPABASE_SERVICE_ROLE_KEY, SUPABASE_PROJECT_REF, SUPABASE_ACCESS_TOKEN,
  IDENTIFICATION_PROVIDER_MODE
Phase 1 observability (6; CI-only, empty locally EXCEPT NEXT_PUBLIC_POSTHOG_HOST which ships non-empty):
  NEXT_PUBLIC_SENTRY_DSN, SENTRY_AUTH_TOKEN, SENTRY_ORG, SENTRY_PROJECT,
  NEXT_PUBLIC_POSTHOG_KEY, NEXT_PUBLIC_POSTHOG_HOST
Phase 4+ placeholders (2):
  INNGEST_EVENT_KEY, INNGEST_SIGNING_KEY
Phase 12+ placeholders (3):
  VERCEL_ORG_ID, VERCEL_PROJECT_ID, VERCEL_TOKEN
Total: 8 + 6 + 2 + 3 = 19.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Pre-flight — resolve exact patch versions + verify eslint-config-next@16 flat-config API shape</name>
  <files>.planning/phases/01-foundation/01-01-SUMMARY.md (only for recording resolved versions; full SUMMARY is written at plan end)</files>
  <read_first>
    - /Users/machado/Projects/folhario/.planning/phases/01-foundation/01-CONTEXT.md §Specifics (eslint-config-next@16 flat-config pre-flight directive)
    - /Users/machado/Projects/folhario/CLAUDE.md (stack pins — resolve to latest published patch if an exact patch is missing)
  </read_first>
  <action>
Step 0.1 — Resolve exact latest patch versions via `pnpm view`. For each of the following majors, run `pnpm view <pkg>@<major> version` and record the result. If the major does not exist on npm (e.g. `typescript@6` may not be published), fall back to the latest published major and document the downgrade.

Packages to resolve:
- `next@16` → record
- `react@19` → record
- `react-dom@19` → record
- `typescript@6` → record (likely fallback to `typescript@5`)
- `eslint@10` → record (likely fallback to `eslint@9`)
- `@types/node@22` → record
- `@types/react@19` → record
- `@types/react-dom@19` → record
- `eslint-config-next@16` → record
- `eslint-config-prettier@10` → record
- `prettier@3` → record
- `vitest@4` → record (CLAUDE.md locks 4.1.4 — if published, use that; else latest 4.x)
- `@playwright/test@1` → record (CLAUDE.md locks 1.59.1)
- `husky@9` → record
- `lint-staged@16` → record
- `@commitlint/cli@20` → record
- `@commitlint/config-conventional@20` → record

Open / append to `.planning/phases/01-foundation/01-01-SUMMARY.md` with a table:

```markdown
## Resolved patch versions (2026-04-23)

| Package | Requested major | Resolved version | Notes |
|---------|-----------------|------------------|-------|
| next | 16 | {resolved} | |
| ... |
```

Step 0.2 — Eslint-config-next@16 flat-config API shape spike. Run a non-interactive `create-next-app` invocation to an out-of-tree directory and inspect the generated `eslint.config.mjs`. This determines whether `eslint-config-next` exports a callable (`next()`) or a config array directly.

```bash
pnpm create next-app@16 /tmp/next-check --ts --app --no-tailwind --use-pnpm --yes 2>&1 | tail -5
cat /tmp/next-check/eslint.config.mjs
```

If the flag set above triggers interactive prompts, iterate until a fully non-interactive invocation works (acceptable alternatives: `--eslint`, `--skip-install`, `--turbopack`). Read the generated file and identify the exact import pattern:
- If the config uses `[...next(), prettier]` → keep the planned shape in Task 2 Step 2
- If the config uses `[...next, prettier]` (array directly) → REPLACE Task 2 Step 2 content with the array-directly form
- If the config uses `FlatCompat` (back-compat shim) → mirror that exact pattern in Task 2 Step 2

Record the resolved import pattern in `01-01-SUMMARY.md` under a section titled `## eslint-config-next@16 flat-config API shape`.

Step 0.3 — Commit the resolved pattern as a one-line comment on LINE 1 of the project's `eslint.config.mjs` (Task 2 Step 2 will create the file; make sure that task's executor preserves this comment):

The comment text MUST be EXACTLY:
```
// verified from create-next-app@16.2.4 output
```

(Replace `16.2.4` with whatever patch `pnpm create next-app@16` resolves to in Step 0.2.)

This comment is an EXPLICIT EXCEPTION to CLAUDE.md's "no comments unless requested" rule, documented in user decision + reviews Action 5. The executor MUST NOT strip it.

Step 0.4 — Clean up: `trash /tmp/next-check`.
  </action>
  <verify>
    <automated>test -f /tmp/next-check/eslint.config.mjs || echo "expected create-next-app output — rerun Step 0.2" ; test -f .planning/phases/01-foundation/01-01-SUMMARY.md && grep -q "Resolved patch versions" .planning/phases/01-foundation/01-01-SUMMARY.md && grep -q "eslint-config-next@16 flat-config API shape" .planning/phases/01-foundation/01-01-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/01-foundation/01-01-SUMMARY.md` contains a `## Resolved patch versions (2026-04-23)` table with ≥17 rows covering every package listed in Step 0.1
    - `.planning/phases/01-foundation/01-01-SUMMARY.md` contains a `## eslint-config-next@16 flat-config API shape` section naming the exact import pattern
    - The SUMMARY records any major-version downgrade (e.g. `typescript@5` fallback) with reason
    - Task 2 Step 2 below uses the import pattern determined in this task
  </acceptance_criteria>
  <done>
    - Resolved patches recorded in SUMMARY table
    - eslint-config-next flat-config API shape verified from upstream `create-next-app` output
    - One-line comment text prepared for Task 2 Step 2
  </done>
</task>

<task type="auto">
  <name>Task 1: Create .nvmrc, extend .gitignore, write package.json with pins + scripts + lint-staged + install dependencies</name>
  <files>.nvmrc, .gitignore, package.json, pnpm-lock.yaml</files>
  <read_first>
    - /Users/machado/Projects/folhario/CLAUDE.md (stack pins)
    - /Users/machado/Projects/folhario/.gitignore (existing — contains only .DS_Store per CONTEXT.md)
    - /Users/machado/Projects/folhario/.env (existing — source of PRD §20 var names; DO NOT MODIFY)
    - /Users/machado/Projects/folhario/.planning/phases/01-foundation/01-PATTERNS.md (package.json section lines 467-502, .gitignore section lines 397-423)
    - /Users/machado/Projects/folhario/.planning/phases/01-foundation/01-RESEARCH.md §Standard Stack (versions table) + §Code Examples (package.json fragment)
    - .planning/phases/01-foundation/01-01-SUMMARY.md (resolved patch versions table from Task 0)
  </read_first>
  <action>
Step 1 — Create `.nvmrc` with the single line `22` (or the exact Node 22 LTS patch you have installed locally — run `node -v` and use the exact version like `22.14.0`). Commit as-is; keep it one line.

Step 2 — Extend `.gitignore`. The existing file contains only `.DS_Store`. Preserve that line and APPEND the following block (do NOT reorder or remove `.DS_Store`; do NOT add `next-env.d.ts` — Next regenerates it on every build and we keep it untracked-but-unignored per reviews Action 6):

```
# deps
node_modules/

# next
.next/
out/

# test + coverage
coverage/
playwright-report/
test-results/
.vitest-cache/

# env (NEVER commit .env — real creds live there)
.env
.env.local
.env.*.local

# supabase CLI
supabase/.temp/
supabase/.branches/
```

Do NOT add `.DS_Store` again — it's already there. Do NOT add `next-env.d.ts` — Next writes it on every `next build` / `next dev`; leaving it out of `.gitignore` AND out of git tracking is the correct compromise.

Step 3 — Create `package.json` with the exact shape below (replace `@latest patch` placeholders with literal resolved patches recorded in Task 0's SUMMARY table):

```json
{
  "name": "folhario",
  "private": true,
  "version": "0.0.0",
  "engines": { "node": ">=22 <23" },
  "packageManager": "pnpm@9.15.5",
  "scripts": {
    "prepare": "husky",
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "typecheck": "tsc --noEmit",
    "test:unit": "pnpm exec vitest --run --project=unit",
    "test:integration": "pnpm exec vitest --run --project=integration",
    "test:e2e": "playwright test",
    "db:start": "supabase start",
    "db:stop": "supabase stop",
    "db:reset": "supabase db reset",
    "db:sync-env": "bash scripts/sync-supabase-env.sh",
    "supabase": "supabase"
  },
  "lint-staged": {
    "*.{ts,tsx,js,jsx,mjs}": ["prettier --write", "eslint --fix"],
    "*.{json,md,yml,yaml}": ["prettier --write"]
  }
}
```

Step 4 — Activate Corepack and install dependencies using the EXACT patches resolved in Task 0 (substitute the patch numbers from the SUMMARY table for each `pnpm add` call below):

```bash
corepack enable
corepack prepare pnpm@9.15.5 --activate

# Runtime deps — substitute exact patches from SUMMARY table
pnpm add next@16.2.3 react@19.2.5 react-dom@19.2.5
pnpm add @serwist/next@9.5.7 serwist@9.5.7
pnpm add @sentry/nextjs@10.48.0
pnpm add posthog-js@1.368.0 posthog-node@5.29.7
pnpm add next-intl@4.9.1
pnpm add zod@4

# Dev deps — substitute exact patches from SUMMARY table
pnpm add -D typescript@6 @types/node@22 @types/react@19 @types/react-dom@19
pnpm add -D eslint@10 eslint-config-next@16.2.3 eslint-config-prettier@10 prettier@3
pnpm add -D vitest@4.1.4 @vitest/ui@4 vite-tsconfig-paths@5
pnpm add -D @playwright/test@1.59.1
pnpm add -D husky@9 lint-staged@16 @commitlint/cli@20 @commitlint/config-conventional@20
pnpm add -D supabase@2.95.0
```

If pnpm errors on any exact version ("no version matching"), the Task 0 SUMMARY should have already surfaced the fallback. Use that fallback verbatim and flag it in the plan SUMMARY.

The `pnpm-lock.yaml` will be generated — commit it.

IMPORTANT — do NOT run `pnpm exec husky init` in this task (Task 4 handles Husky setup). Installing `husky` only places the package in `node_modules`; registering hooks is a separate step.
  </action>
  <verify>
    <automated>test -f .nvmrc && grep -q "^22" .nvmrc && test -f package.json && test -f pnpm-lock.yaml && grep -q '"next":' package.json && grep -q '"packageManager"' package.json && grep -q "node_modules/" .gitignore && grep -q ".env.local" .gitignore && grep -q ".DS_Store" .gitignore && ! grep -q "^next-env\.d\.ts$" .gitignore && pnpm install --frozen-lockfile</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "next-env.d.ts" .gitignore` returns 0 (Action 6 — let Next regenerate it)
    - `grep -c ".DS_Store" .gitignore` returns 1 (preserved from pre-existing file)
    - `grep -c "node_modules/" .gitignore` returns 1
    - `grep -c ".env.local" .gitignore` returns 1 (may appear in `.env.*.local` pattern line too — that's fine)
    - `test -f pnpm-lock.yaml` passes
    - `pnpm install --frozen-lockfile` exits 0
    - All 10 runtime + 16 dev dependencies installed at resolved patches
  </acceptance_criteria>
  <done>
    - `.nvmrc` exists with Node 22 (or exact 22.x patch) on single line
    - `.gitignore` contains all required entries AND preserves `.DS_Store` AND does NOT include `next-env.d.ts`
    - `package.json` exists with exact name `folhario`, all 14 scripts, lint-staged config, engines/packageManager fields
    - `pnpm-lock.yaml` exists and `pnpm install --frozen-lockfile` passes
    - All 10 runtime + 16 dev dependencies installed at pinned versions from Task 0 SUMMARY
  </done>
</task>

<task type="auto">
  <name>Task 2: Write tsconfig.json (strict + aliases), eslint.config.mjs, .prettierrc, .prettierignore, .commitlintrc.json</name>
  <files>tsconfig.json, eslint.config.mjs, .prettierrc, .prettierignore, .commitlintrc.json</files>
  <read_first>
    - /Users/machado/Projects/folhario/.planning/phases/01-foundation/01-PATTERNS.md §tsconfig.json (lines 505-540, note R-1 @i18n/* alias), §eslint.config.mjs (lines 543-556), §.prettierrc (lines 559-571), §.commitlintrc.json (lines 574-582)
    - /Users/machado/Projects/folhario/.planning/phases/01-foundation/01-CONTEXT.md (D-06 strictness minimum, D-07 path aliases, D-08 no barrels)
    - .planning/phases/01-foundation/01-01-SUMMARY.md — `## eslint-config-next@16 flat-config API shape` section (from Task 0)
  </read_first>
  <action>
Step 1 — Create `tsconfig.json` exactly (includes the `@i18n/*` alias per R-1 reconciliation — this alias is required by Plan 03's `src/proxy.ts`):

```json
{
  "compilerOptions": {
    "strict": true,
    "noUncheckedIndexedAccess": true,
    "target": "ES2022",
    "lib": ["dom", "dom.iterable", "esnext"],
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "jsx": "preserve",
    "allowJs": true,
    "skipLibCheck": true,
    "esModuleInterop": true,
    "forceConsistentCasingInFileNames": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "incremental": true,
    "noEmit": true,
    "plugins": [{ "name": "next" }],
    "paths": {
      "@contexts/*": ["./src/contexts/*"],
      "@shared/*": ["./src/shared/*"],
      "@i18n/*": ["./src/i18n/*"]
    }
  },
  "include": ["next-env.d.ts", "**/*.ts", "**/*.tsx", ".next/types/**/*.ts"],
  "exclude": ["node_modules"]
}
```

Rationale: `noUncheckedIndexedAccess` is the ONLY strict flag added beyond `strict: true` per D-06. Do NOT add `exactOptionalPropertyTypes`, `noPropertyAccessFromIndexSignature`, or `noImplicitOverride` — explicitly rejected in D-06 to reduce friction in early phases.

Step 2 — Create `eslint.config.mjs`. The EXACT import shape depends on what Task 0 verified. Use the pattern from the SUMMARY's `## eslint-config-next@16 flat-config API shape` section. The LINE 1 MUST be the verified-from-output comment (explicit exception to "no comments" rule per user decision + Action 5):

```javascript
// verified from create-next-app@16.2.4 output
import next from "eslint-config-next";
import prettier from "eslint-config-prettier";

export default [
  ...next(),
  prettier,
  {
    ignores: ["node_modules", ".next", "out", "coverage", "playwright-report", "test-results", "public/sw.js"],
  },
];
```

**If Task 0 SUMMARY determined the API is array-directly (not callable), use this alternate body instead (keep line 1 comment intact):**

```javascript
// verified from create-next-app@16.2.4 output
import next from "eslint-config-next";
import prettier from "eslint-config-prettier";

export default [
  ...next,
  prettier,
  {
    ignores: ["node_modules", ".next", "out", "coverage", "playwright-report", "test-results", "public/sw.js"],
  },
];
```

Note the `public/sw.js` ignore — Serwist writes the compiled SW to that path; linting it would fail on generated output.

Step 3 — Run `pnpm lint` in the project root with an empty `src/`. MUST exit 0. If it errors with "cannot find module eslint-config-next" or similar, re-run Task 0 Step 0.2 to re-verify the flat-config shape.

Step 4 — Create `.prettierrc`:

```json
{
  "semi": true,
  "singleQuote": false,
  "trailingComma": "all",
  "printWidth": 100
}
```

Step 5 — Create `.prettierignore`:

```
node_modules
.next
out
coverage
playwright-report
test-results
public/sw.js
pnpm-lock.yaml
.planning
supabase/.branches
supabase/.temp
```

Step 6 — Create `.commitlintrc.json`:

```json
{ "extends": ["@commitlint/config-conventional"] }
```

No custom rules at Phase 1 — start from the ecosystem default and let conventions emerge.
  </action>
  <verify>
    <automated>test -f tsconfig.json && grep -q "noUncheckedIndexedAccess" tsconfig.json && grep -q "@i18n/\\*" tsconfig.json && grep -q "@contexts/\\*" tsconfig.json && grep -q "@shared/\\*" tsconfig.json && test -f eslint.config.mjs && head -n 1 eslint.config.mjs | grep -q "verified from create-next-app" && grep -q "eslint-config-next" eslint.config.mjs && test -f .prettierrc && test -f .prettierignore && test -f .commitlintrc.json && grep -q "config-conventional" .commitlintrc.json && pnpm typecheck && pnpm lint</automated>
  </verify>
  <acceptance_criteria>
    - `head -n 1 eslint.config.mjs` prints the exact comment `// verified from create-next-app@16.2.4 output` (patch number may vary; comment template must match)
    - `grep -q "noUncheckedIndexedAccess" tsconfig.json` matches
    - All three path aliases present: `grep -c "@contexts/\\*\\|@shared/\\*\\|@i18n/\\*" tsconfig.json` returns ≥3
    - `.prettierrc` has semi=true, singleQuote=false, trailingComma=all, printWidth=100
    - `.prettierignore` excludes Next/test/supabase artifacts
    - `.commitlintrc.json` extends `@commitlint/config-conventional`
    - `pnpm typecheck` exits 0 (no src/ yet = no errors)
    - `pnpm lint` exits 0 (no sources to lint yet)
  </acceptance_criteria>
  <done>
    - `tsconfig.json` has `strict: true`, `noUncheckedIndexedAccess: true`, and all three path aliases (`@contexts/*`, `@shared/*`, `@i18n/*`)
    - `eslint.config.mjs` line 1 is the verified-from-output comment; uses the import pattern determined in Task 0
    - `.prettierrc`, `.prettierignore`, `.commitlintrc.json` shipped
    - `pnpm typecheck` + `pnpm lint` both exit 0
  </done>
</task>

<task type="auto">
  <name>Task 3: Write vitest.config.ts (two projects), playwright.config.ts (Chromium only; probe URL = `/`), and .env.example (19 variables)</name>
  <files>vitest.config.ts, playwright.config.ts, .env.example</files>
  <read_first>
    - /Users/machado/Projects/folhario/.planning/phases/01-foundation/01-RESEARCH.md §Code Examples "vitest.config.ts" (lines 1010-1042), "playwright.config.ts" (lines 1159-1191)
    - /Users/machado/Projects/folhario/.planning/phases/01-foundation/01-PATTERNS.md §.env.example (lines 427-462) and §vitest.config.ts / playwright.config.ts (lines 619-640)
    - /Users/machado/Projects/folhario/.env (source of PRD §20 var names — read-only)
    - /Users/machado/Projects/folhario/.planning/phases/01-foundation/01-CONTEXT.md §Specifics (variable count = 19 normalized per reviews Action 7)
  </read_first>
  <action>
Step 1 — Create `vitest.config.ts` with two projects (Vitest 4 `test.projects` — replaces deprecated `workspace` key). The `unit` project does NOT have its own DB; the `integration` project has 30s timeout for DB work (Phase 2 fills it).

```typescript
import { defineConfig } from "vitest/config";
import tsconfigPaths from "vite-tsconfig-paths";

export default defineConfig({
  plugins: [tsconfigPaths()],
  test: {
    projects: [
      {
        extends: true,
        test: {
          name: { label: "unit", color: "cyan" },
          include: ["tests/unit/**/*.test.ts"],
          environment: "node",
          isolate: false,
        },
      },
      {
        extends: true,
        test: {
          name: { label: "integration", color: "green" },
          include: ["tests/integration/**/*.integration.test.ts"],
          environment: "node",
          testTimeout: 30_000,
        },
      },
    ],
  },
});
```

Step 2 — Create `playwright.config.ts` (Chromium-only per D-26; `webServer` runs `pnpm start` — production build, NOT dev). **Probe URL = `/` per reviews Action 11** — production build unconditionally returns 200 at `/` (Plan 03 ships `src/app/page.tsx`), so the probe never 404s between Plans 1–7:

```typescript
import { defineConfig, devices } from "@playwright/test";

export default defineConfig({
  testDir: "./tests/e2e",
  timeout: 30_000,
  fullyParallel: true,
  forbidOnly: !!process.env.CI,
  retries: process.env.CI ? 2 : 0,
  workers: process.env.CI ? 2 : undefined,
  reporter: process.env.CI ? [["html"], ["github"]] : "list",

  use: {
    baseURL: "http://localhost:3000",
    trace: "on-first-retry",
  },

  projects: [
    { name: "chromium", use: { ...devices["Desktop Chrome"] } },
  ],

  webServer: {
    command: "pnpm start",
    url: "http://localhost:3000/",
    reuseExistingServer: !process.env.CI,
    timeout: 120_000,
    env: {
      IDENTIFICATION_PROVIDER_MODE: "stub",
    },
  },
});
```

Rationale: Action 11. The previous `/__diag` URL caused `pnpm test:e2e` to fail for every wave before Plan 07 landed (404s until `src/app/__diag/layout.tsx` exists). Using `/` is stable across all waves. The stub-mode guard moves entirely into Plan 07's route handlers and `__diag` layout.

Step 3 — Create `.env.example` mirroring PRD §20 with dummy values — EXACTLY 19 variables. Group variables by phase of first use per D-30. Per reviews Action 17, `NEXT_PUBLIC_POSTHOG_HOST` is set to the real public constant (`https://us.i.posthog.com`) since hardcoding a public non-secret constant reduces config drift:

```dotenv
# ──────────────── Phase 1 (infrastructure — 8 vars) ────────────────
DATABASE_URL=postgres://postgres:postgres@localhost:54322/postgres
DATABASE_POOL_URL=postgres://postgres:postgres@localhost:54322/postgres
NEXT_PUBLIC_SUPABASE_URL=http://localhost:54321
NEXT_PUBLIC_SUPABASE_ANON_KEY=DUMMY-CHANGE-ME
SUPABASE_SERVICE_ROLE_KEY=DUMMY-CHANGE-ME
SUPABASE_PROJECT_REF=DUMMY-CHANGE-ME
SUPABASE_ACCESS_TOKEN=DUMMY-CHANGE-ME
IDENTIFICATION_PROVIDER_MODE=stub

# ──────────────── Phase 1 (observability — 6 vars; CI only, mostly empty) ────────────────
# Empty values intentionally. D-19/D-20: Sentry and PostHog disabled locally.
# EXCEPTION: NEXT_PUBLIC_POSTHOG_HOST is a public non-secret constant — hardcoded to reduce drift.
NEXT_PUBLIC_SENTRY_DSN=
SENTRY_AUTH_TOKEN=
SENTRY_ORG=
SENTRY_PROJECT=
NEXT_PUBLIC_POSTHOG_KEY=
NEXT_PUBLIC_POSTHOG_HOST=https://us.i.posthog.com

# ──────────────── Phase 4 (async + email — 2 vars) — placeholders ────────────────
INNGEST_EVENT_KEY=
INNGEST_SIGNING_KEY=

# ──────────────── Phase 12 (deploy — 3 vars) — placeholders ────────────────
VERCEL_ORG_ID=
VERCEL_PROJECT_ID=
VERCEL_TOKEN=
```

Total variable count: 8 + 6 + 2 + 3 = 19 (Action 7 normalization; matches Plan 02's secret-pattern test hardcoded list).

DO NOT copy from `.env`. `.env` in this repo already contains real provisioned credentials (per CONTEXT.md §Code Context) — copying from it would commit secrets. Author `.env.example` by hand from the PRD §20 table, matching variable names exactly.

After writing, sanity-check: `grep -E '^(sk_live_|eyJ[A-Za-z0-9])' .env.example` must return NOTHING (no tokens/JWTs).
  </action>
  <verify>
    <automated>test -f vitest.config.ts && grep -q "projects:" vitest.config.ts && grep -q "\"unit\"" vitest.config.ts && grep -q "\"integration\"" vitest.config.ts && test -f playwright.config.ts && grep -q "chromium" playwright.config.ts && grep -q "pnpm start" playwright.config.ts && grep -q "url: \"http://localhost:3000/\"" playwright.config.ts && ! grep -q "/__diag" playwright.config.ts && test -f .env.example && [ $(grep -cE '^[A-Z_][A-Z0-9_]*=' .env.example) -eq 19 ] && grep -q "^IDENTIFICATION_PROVIDER_MODE=stub" .env.example && grep -q "^DATABASE_URL=" .env.example && grep -q "^NEXT_PUBLIC_POSTHOG_HOST=https://us.i.posthog.com$" .env.example && grep -q "^VERCEL_TOKEN=" .env.example && ! grep -qE "^(sk_live_|eyJ[A-Za-z0-9])" .env.example && pnpm exec vitest --version && pnpm exec playwright --version</automated>
  </verify>
  <acceptance_criteria>
    - `[ $(grep -cE '^[A-Z_][A-Z0-9_]*=' .env.example) -eq 19 ]` passes (exactly 19 env-var lines — Action 7)
    - `grep -q "^NEXT_PUBLIC_POSTHOG_HOST=https://us.i.posthog.com$" .env.example` matches (Action 17 — non-empty value)
    - `! grep -q "/__diag" playwright.config.ts` (Action 11 — probe URL moved to `/`)
    - `grep -q "url: \"http://localhost:3000/\"" playwright.config.ts` matches (probe URL is root)
    - `vitest.config.ts` uses `test.projects` (v4 API) with `unit` and `integration` projects plus `tsconfigPaths()` plugin
    - `playwright.config.ts` has single `chromium` project, `webServer.command: "pnpm start"`, `IDENTIFICATION_PROVIDER_MODE: stub` in webServer env
    - NO real credentials in `.env.example` (no base64-JWT-like strings, no `sk_live_*`, no long random hex)
    - `pnpm exec vitest --version` and `pnpm exec playwright --version` both succeed
  </acceptance_criteria>
  <done>
    - `vitest.config.ts` uses `test.projects` (v4 API) with `unit` + `integration` projects
    - `playwright.config.ts` probe URL is `/` (no references to `/__diag`)
    - `.env.example` contains EXACTLY 19 PRD §20 variables with correct grouping
    - `NEXT_PUBLIC_POSTHOG_HOST=https://us.i.posthog.com` (non-empty, hardcoded per Action 17)
    - No real credentials in `.env.example`
  </done>
</task>

<task type="auto">
  <name>Task 4: Initialize Husky hooks (pre-commit, commit-msg) using Husky 9 idioms + stateless commitlint verification</name>
  <files>.husky/pre-commit, .husky/commit-msg</files>
  <read_first>
    - /Users/machado/Projects/folhario/.planning/phases/01-foundation/01-RESEARCH.md §Pitfall 4 (Husky 9 `husky install` vs `husky init`)
    - /Users/machado/Projects/folhario/.planning/phases/01-foundation/01-PATTERNS.md §.husky/pre-commit + §.husky/commit-msg (lines 643-657)
    - /Users/machado/Projects/folhario/package.json (from Task 1 — has `prepare: "husky"` script)
  </read_first>
  <action>
Step 1 — Initialize Husky using the v9 idiom (creates `.husky/` directory, installs the git hooks path):

```bash
pnpm exec husky init
```

This auto-creates `.husky/pre-commit` with a sample command. Overwrite it in Step 2.

Do NOT use `pnpm exec husky install` (deprecated v8 API; silently no-ops in Husky 9). Do NOT add `husky install` to scripts — the `prepare: "husky"` one-word script set in Task 1 is the correct v9 form.

Step 2 — Overwrite `.husky/pre-commit` with (single line, no shebang — Husky 9 runs it directly via sh):

```
pnpm lint-staged
```

Step 3 — Create `.husky/commit-msg` (single line, no shebang):

```
pnpm exec commitlint --edit "$1"
```

Step 4 — Ensure both files are executable:

```bash
chmod +x .husky/pre-commit .husky/commit-msg
```

Step 5 — **Stateless commitlint verification (Action 10 — replaces prior git-stash-based check):**

```bash
# Assert a bad message is rejected (exit code 1 expected)
echo "bad message" | pnpm exec commitlint --stdin --strict ; RC=$?
if [ $RC -eq 0 ]; then
  echo "FAIL: bad commit message was accepted (commitlint not rejecting)" >&2
  exit 1
fi

# Assert a valid Conventional Commits message passes (exit code 0 expected)
echo "feat: valid" | pnpm exec commitlint --stdin --strict ; RC=$?
if [ $RC -ne 0 ]; then
  echo "FAIL: valid Conventional Commits message was rejected" >&2
  exit 1
fi

echo "commitlint stateless verification PASSED"
```

This approach does NOT use `git stash`, does NOT touch the working tree, and has no chance of leaving the repo in an inconsistent state if the assertions fail. It directly exercises `commitlint --stdin --strict` which is commitlint's documented stateless invocation mode.

Do NOT revert to the older `git stash push + git commit + cleanup` pattern from prior plan revisions — it was replaced precisely because of its statefulness.
  </action>
  <verify>
    <automated>test -f .husky/pre-commit && test -f .husky/commit-msg && test -x .husky/pre-commit && test -x .husky/commit-msg && grep -q "pnpm lint-staged" .husky/pre-commit && grep -q "commitlint --edit" .husky/commit-msg && ! grep -q "husky install" .husky/pre-commit && ! grep -q "husky install" .husky/commit-msg && ( [ "$(git config core.hooksPath 2>/dev/null)" = ".husky/_" ] || [ "$(git config core.hooksPath 2>/dev/null)" = ".husky" ] ) && echo "bad message" | pnpm exec commitlint --stdin --strict ; [ $? -ne 0 ] && echo "feat: valid" | pnpm exec commitlint --stdin --strict</automated>
  </verify>
  <acceptance_criteria>
    - `.husky/pre-commit` and `.husky/commit-msg` both exist, both executable
    - `grep -q "pnpm lint-staged" .husky/pre-commit` matches
    - `grep -q "commitlint --edit" .husky/commit-msg` matches
    - Neither hook contains `husky install` (deprecated v8 API)
    - `git config core.hooksPath` resolves to `.husky/_` or `.husky`
    - Stateless commitlint check: bad message piped to `commitlint --stdin --strict` exits NON-ZERO
    - Stateless commitlint check: `feat: valid` piped to `commitlint --stdin --strict` exits ZERO
    - NO references to `git stash` in Task 4 execution trace (Action 10 — verification must be stateless)
  </acceptance_criteria>
  <done>
    - `.husky/pre-commit` exists, executable, contains `pnpm lint-staged`
    - `.husky/commit-msg` exists, executable, contains `pnpm exec commitlint --edit "$1"`
    - Neither hook file contains `husky install` (deprecated)
    - Stateless commitlint verification passes: bad message rejected, valid message accepted
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer laptop -> committed repo | Secrets must not cross this boundary |
| CI environment -> dependency registry | Lockfile integrity prevents supply-chain drift |
| Upstream eslint-config-next API -> local config | Verified via create-next-app output spike (Action 5) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | `.env` / `.env.example` | mitigate | `.gitignore` adds `.env`, `.env.local`, `.env.*.local` (Task 1). `.env.example` hand-authored from PRD §20 table with dummy values only (19 vars); Task 3 regex check rejects secret-like values. |
| T-01-02 | Tampering | `pnpm-lock.yaml` | mitigate | Task 1 commits `pnpm-lock.yaml`; CI uses `pnpm install --frozen-lockfile` (Plan 08). Dependencies pinned at exact patches resolved in Task 0. |
| T-01-03 | Repudiation | Git commit history | mitigate | Husky `commit-msg` hook enforces Conventional Commits (Task 4). Verified via stateless `commitlint --stdin --strict` (Action 10). |
| T-01-04 | Denial of Service | Node version mismatch | mitigate | `.nvmrc` pins Node 22; `engines.node` enforces at install time; CI uses `setup-node` with `node-version: 22` (Plan 08). |
| T-01-05 | Tampering | `eslint-config-next@16` API shape assumption | mitigate | Task 0 pre-flight spike runs `create-next-app@16` and reads the generated `eslint.config.mjs`; documents the verified pattern on line 1 of the project's `eslint.config.mjs`. Catches the callable-vs-array API divergence before Wave 1 lints. |

Threats deferred: CVE monitoring for pinned deps (requires Dependabot/Renovate — Phase 13 or ongoing).
</threat_model>

<verification>
1. `pnpm install --frozen-lockfile` from a clean clone succeeds.
2. `pnpm typecheck` exits 0.
3. `pnpm lint` exits 0 (confirms Task 0 pre-flight flat-config shape is correct).
4. `pnpm exec playwright --version` prints the resolved patch from Task 0 SUMMARY.
5. Stateless commitlint: bad message rejected (exit 1), valid message accepted (exit 0) — Action 10.
6. `.gitignore` preserves `.DS_Store` and does NOT include `next-env.d.ts` (Action 6).
7. `[ $(grep -cE '^[A-Z_][A-Z0-9_]*=' .env.example) -eq 19 ]` passes (Action 7).
8. `playwright.config.ts` probe URL is `/` — no `/__diag` references (Action 11).
9. Line 1 of `eslint.config.mjs` is `// verified from create-next-app@16.2.4 output` (Action 5).
</verification>

<success_criteria>
- [ ] Task 0 resolved patch versions recorded in `01-01-SUMMARY.md` table (Action 12)
- [ ] Task 0 verified `eslint-config-next@16` flat-config API shape from `create-next-app` output (Action 5)
- [ ] `.nvmrc`, `package.json`, `pnpm-lock.yaml` committed; `pnpm install --frozen-lockfile` passes
- [ ] `tsconfig.json` enforces `strict: true` + `noUncheckedIndexedAccess: true` and exports three path aliases
- [ ] `eslint.config.mjs` line 1 is the verified-from-output comment; `pnpm lint` exits 0
- [ ] `.prettierrc`, `.prettierignore`, `.commitlintrc.json` present and functional
- [ ] Vitest config declares `unit` + `integration` projects via v4 `test.projects` API
- [ ] `playwright.config.ts` probe URL is `/` (NOT `/__diag`)
- [ ] `.husky/pre-commit` + `.husky/commit-msg` in place; stateless commitlint verification passes
- [ ] `.env.example` has 19 variables (NOT 18); `NEXT_PUBLIC_POSTHOG_HOST=https://us.i.posthog.com`
- [ ] `.gitignore` extended without losing `.DS_Store`; does NOT include `next-env.d.ts`
- [ ] All automated verification commands in each task `<verify>` block pass
</success_criteria>

<output>
After completion, finalize `.planning/phases/01-foundation/01-01-SUMMARY.md` documenting:
- Table of resolved patch versions (Task 0.1)
- eslint-config-next@16 flat-config API shape as verified (Task 0.2)
- Exact Node + pnpm patch versions chosen
- Any dependency version fallbacks taken
- Any tooling quirks encountered
- Reference to next plan (01-02)
</output>
