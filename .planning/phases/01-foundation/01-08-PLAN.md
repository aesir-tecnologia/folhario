---
phase: 01
plan: 08
type: execute
wave: 6
depends_on: [01, 02, 03, 04, 05a, 05b, 06, 07]
files_modified:
  - .github/workflows/ci.yml
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: false
requirements:
  - INFRA-12
tags:
  - ci
  - github-actions
  - planning-doc-edits

user_setup:
  - service: github-actions
    why: "CI must have Sentry + PostHog secrets configured for OBS-01/OBS-02 verification; without them the diagnostics-specs' CI-gated assertions fail"
    env_vars:
      - name: SENTRY_DSN_CI
        source: "Sentry project -> Settings -> Projects -> folhario-ci -> Client Keys (DSN)"
      - name: POSTHOG_KEY_CI
        source: "PostHog US project -> Project settings -> API key"
    dashboard_config:
      - task: "Create a dedicated CI Sentry project (Sentry org -> Create Project -> Next.js platform)"
        location: "https://sentry.io"
      - task: "Create a dedicated CI PostHog project (PostHog US cloud -> Create Project)"
        location: "https://us.posthog.com"
      - task: "Add GitHub Actions repository secrets named SENTRY_DSN_CI + POSTHOG_KEY_CI"
        location: "GitHub repo -> Settings -> Secrets and variables -> Actions"

must_haves:
  truths:
    - "`.github/workflows/ci.yml` triggers on pull_request and push to main"
    - "`concurrency: { group: ci-${{ github.ref }}, cancel-in-progress: true }` cancels superseded runs (D-28)"
    - "Job runs a `postgres:17-alpine` service container (D-25 — parity with local Supabase)"
    - "pnpm store is cached via actions/cache keyed on pnpm-lock.yaml"
    - "Playwright browsers cached via actions/cache keyed on pnpm-lock.yaml (D-28)"
    - "Steps order: install -> lint -> typecheck -> test:unit -> test:integration -> build -> playwright install chromium -> test:e2e"
    - "Integration step sets DATABASE_URL + DATABASE_POOL_URL pointing at the Postgres service container AND runs the new server-probe test"
    - "E2E step sets NEXT_PUBLIC_SENTRY_DSN + NEXT_PUBLIC_POSTHOG_KEY from repo secrets"
    - "Build step does NOT set SENTRY_AUTH_TOKEN (Pitfall 7 — upload is Phase 12)"
    - "Unit + integration tests run with single-run semantics (never watch mode)"
    - "`.planning/REQUIREMENTS.md` INFRA-12 text reads postgres:17-alpine (was postgres:16-alpine)"
    - "`.planning/REQUIREMENTS.md` OBS-05 mapping moved from Phase 1 -> Phase 13 (user decision 2 — 2026-04-23 defer)"
    - "`.planning/ROADMAP.md` Phase 1 SC-4 wording amended per user decision 1 (client/server split + Vitest server-probe + manual dashboard)"
    - "`.planning/ROADMAP.md` Phase 1 plan list updated to 9 plans reflecting 05a/05b split (Action 16, Action 20)"
    - "Playwright report artifact uploaded on any outcome (retention-days: 14)"
    - "`actions/checkout` + `actions/setup-node` versions verified at scaffold time with fallback to @v4 if @v5 unreleased (Action 19)"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "CI pipeline: lint + typecheck + vitest unit + vitest integration (vs postgres:17-alpine, including server-probe) + next build + Playwright smoke"
      contains: "postgres:17-alpine"
    - path: ".planning/REQUIREMENTS.md"
      provides: "INFRA-12 text corrected + OBS-05 re-mapped to Phase 13 (user decision 2)"
      contains: "postgres:17-alpine"
    - path: ".planning/ROADMAP.md"
      provides: "SC-4 wording amended (user decision 1) + Phase 1 plan list updated to 9 plans (Action 16 + Action 20); Phase 13 scope expanded with OBS-05"
      contains: "Vitest integration test"
  key_links:
    - from: ".github/workflows/ci.yml integration step"
      to: "postgres:17-alpine service container"
      via: "DATABASE_POOL_URL env pointing at localhost:5432"
      pattern: "postgres://postgres:postgres@localhost:5432/folhario_test"
    - from: ".github/workflows/ci.yml integration step"
      to: "tests/integration/diagnostics-server-probe.integration.test.ts"
      via: "`pnpm test:integration` executes ALL integration tests in the project"
      pattern: "pnpm test:integration"
    - from: ".github/workflows/ci.yml E2E step"
      to: "tests/e2e/*.spec.ts"
      via: "pnpm test:e2e against pnpm start built production bundle"
      pattern: "pnpm test:e2e"
    - from: ".github/workflows/ci.yml E2E step"
      to: "github secrets SENTRY_DSN_CI + POSTHOG_KEY_CI"
      via: "env: NEXT_PUBLIC_SENTRY_DSN: secrets.SENTRY_DSN_CI"
      pattern: "secrets\\.SENTRY_DSN_CI"
---

<objective>
Write `.github/workflows/ci.yml` exercising the full Phase 1 pipeline, then amend `.planning/REQUIREMENTS.md` (INFRA-12 text + OBS-05 defer per user decision 2) and `.planning/ROADMAP.md` (SC-4 wording per user decision 1 + Phase 1 plan list updated to 9 plans per Action 16+20 + Phase 13 scope expansion). Human checkpoint: set GitHub secrets, watch the first PR run, record manual dashboard verification with screenshot links.

Purpose: INFRA-12 + INFRA-23 require this workflow. Without it there is no automated gate preventing regressions, no verification that OBS-01/02 diagnostics transport works at CI scale, no safety net for Phase 2+. This is the final wave gate for Phase 1 AND the plan that makes the Phase-1 planning documents (REQUIREMENTS + ROADMAP + CONTEXT) consistent with the revisions applied by `/gsd-plan-phase 1 --reviews` on 2026-04-23.

Output: A PR opened against this repo triggers `ci.yml`, all steps pass, Sentry CI project receives at least 1 event, PostHog CI project receives at least 1 event. The Vitest server-probe confirms server-side invocation automatically. ROADMAP SC-4 wording reflects the split-verification posture. REQUIREMENTS.md correctly scopes OBS-05 to Phase 13.
</objective>

<execution_context>
@/Users/machado/Projects/folhario/.claude/get-shit-done/workflows/execute-plan.md
@/Users/machado/Projects/folhario/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/machado/Projects/folhario/.planning/phases/01-foundation/01-CONTEXT.md
@/Users/machado/Projects/folhario/.planning/phases/01-foundation/01-RESEARCH.md
@/Users/machado/Projects/folhario/.planning/phases/01-foundation/01-PATTERNS.md
@/Users/machado/Projects/folhario/.planning/phases/01-foundation/01-FILE-MATRIX.md
@/Users/machado/Projects/folhario/.planning/REQUIREMENTS.md
@/Users/machado/Projects/folhario/.planning/ROADMAP.md
@/Users/machado/Projects/folhario/package.json
@/Users/machado/Projects/folhario/playwright.config.ts

<interfaces>
Consumed from Plan 01: package.json scripts (test:unit, test:integration, test:e2e, build, lint, typecheck), pnpm-lock.yaml.
Consumed from Plan 04: tests/integration/postgres-connection.integration.test.ts.
Consumed from Plan 07: 5 Playwright specs + 1 new Vitest integration test (diagnostics-server-probe).

Required GitHub Actions secrets (human must configure):
- SENTRY_DSN_CI — dedicated CI Sentry project DSN
- POSTHOG_KEY_CI — dedicated CI PostHog project API key

Service container config (GitHub Actions):
- image: postgres:17-alpine
- env: POSTGRES_USER=postgres, POSTGRES_PASSWORD=postgres, POSTGRES_DB=folhario_test
- ports: 5432:5432
- health-cmd: pg_isready, health-interval: 10s, health-timeout: 5s, health-retries: 5

REQUIREMENTS.md INFRA-12 text (existing):
- Current: "integration against real `postgres:16-alpine` service container..."
- Target: "integration against real `postgres:17-alpine` service container..."

REQUIREMENTS.md OBS-05 mapping (existing):
- Current: mapped to Phase 1
- Target: mapped to Phase 13 (per user decision 2 — 2026-04-23)

ROADMAP.md Phase 1 SC-4 wording (current, line ~39):
- "A deliberately thrown error in the local app appears in Sentry with `Authorization`, `Cookie`, `email`, `password`, `token`, and `photo_url` scrubbed, `Sentry.setUser({ id })` only, and request bodies dropped on `/api/v1/identifications/*` routes; a PostHog ping event fires from both the client and `posthog-node` server and lands in the PostHog US project — both verified automatically by the Playwright smoke."

ROADMAP.md Phase 1 SC-4 wording (target — user decision 1):
- "A deliberately thrown error in the local app appears in Sentry with `Authorization`, `Cookie`, `email`, `password`, `token`, and `photo_url` scrubbed, `Sentry.setUser({ id })` only, and request bodies dropped on `/api/v1/identifications/*` routes. A PostHog ping event fires from both the client and `posthog-node` server. Client-side envelopes are verified automatically by the Playwright smoke (Sentry transport + PostHog `$pageview` intercepted at the network layer). Server-side envelopes are verified in two ways: (a) an automated Playwright assertion that the server route handler successfully called its Sentry/PostHog client (via a server-side log probe or transport spy), and (b) a manual dashboard check recorded in `01-08-SUMMARY.md` with screenshot links."

ROADMAP.md Phase 1 plan list (current, ~lines 44-51) — 8 entries; target 9 entries reflecting 05a + 05b split:
```
Plans:
- [ ] 01-01-PLAN.md -- Repo tooling bootstrap
- [ ] 01-02-PLAN.md -- Scaffold + TDD error registry + TDD Zod env (split into server-env + client-env)
- [ ] 01-03-PLAN.md -- Next 16 app + i18n + proxy (locale only) + Serwist + next.config.ts with headers()
- [ ] 01-04-PLAN.md -- Local Supabase Docker stack + env sync script + postgres-js prod dep
- [ ] 01-05a-PLAN.md -- TDD Sentry scrub module (LGPD-13)
- [ ] 01-05b-PLAN.md -- Sentry init file wiring (3 files) + instrumentation.ts cleanup
- [ ] 01-06-PLAN.md -- Hand-rolled PostHog providers (client-env import only)
- [ ] 01-07-PLAN.md -- Diagnostics routes + 5 Playwright specs + Vitest server-probe
- [ ] 01-08-PLAN.md -- ci.yml + REQUIREMENTS.md + ROADMAP.md amendments
```
Plan count: `**Plans:** 9 plans`.

Action pins (Action 19 — verify at scaffold time, fall back to @v4 if @v5 unreleased):
- actions/checkout — try @v5, fallback @v4
- actions/setup-node — try @v5, fallback @v4 (latest stable as of 2026-04 is v4; verify before committing)
- actions/cache@v4
- actions/upload-artifact@v4
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Create 01-FILE-MATRIX.md sanity check (deliverable already shipped during revision — verify still current)</name>
  <files>.planning/phases/01-foundation/01-FILE-MATRIX.md</files>
  <read_first>
    - /Users/machado/Projects/folhario/.planning/phases/01-foundation/01-FILE-MATRIX.md (authoritative; pre-existing)
  </read_first>
  <action>
Sanity-check that the FILE-MATRIX entries still match the shipped reality. This task does NOT regenerate the matrix; it only asserts every path the matrix lists has been created OR (for files it says 'edited by Plan N') was actually edited by that plan. Run:

```bash
# Every file path listed in the matrix table must either exist or be explicitly marked as a documentation artifact.
grep -oE '`[^`]+`' .planning/phases/01-foundation/01-FILE-MATRIX.md | tr -d '`' | sort -u | while read -r p; do
  # Skip non-file entries (plan names, labels, `edited by` text fragments)
  case "$p" in
    *.ts|*.tsx|*.js|*.mjs|*.json|*.toml|*.sh|*.yml|*.yaml|*.md|.env.example|.gitignore|.husky/*|public/*|scripts/*|src/*|tests/*|.github/*|.planning/*)
      if [ ! -e "$p" ]; then
        echo "WARN: $p referenced in FILE-MATRIX but does not exist yet (acceptable only if a LATER-wave plan owns it)"
      fi
      ;;
  esac
done
```

The script above WARNs rather than exits with error because Plan 08 runs at Wave 6 — by this point every file in the matrix must exist; any WARN is a real issue. If WARNs appear, stop and investigate before continuing to Task 1.
  </action>
  <verify>
    <automated>test -f .planning/phases/01-foundation/01-FILE-MATRIX.md && grep -q "Wave DAG" .planning/phases/01-foundation/01-FILE-MATRIX.md && grep -q "01-05a" .planning/phases/01-foundation/01-FILE-MATRIX.md && grep -q "01-05b" .planning/phases/01-foundation/01-FILE-MATRIX.md && grep -q "Total.*9 plans" .planning/phases/01-foundation/01-FILE-MATRIX.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/01-foundation/01-FILE-MATRIX.md` exists
    - Matrix references 9 plans including 05a + 05b
    - Every file path listed in the matrix exists on disk (script above emits zero WARN lines)
  </acceptance_criteria>
  <done>
    - FILE-MATRIX.md sanity-check passed
  </done>
</task>

<task type="auto">
  <name>Task 1: Create .github/workflows/ci.yml with verified action versions (Action 19 fallback)</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - /Users/machado/Projects/folhario/.planning/phases/01-foundation/01-RESEARCH.md lines 1044-1155 (full ci.yml recipe), lines 974-981 (Pitfall 7 no source-map upload)
    - /Users/machado/Projects/folhario/.planning/phases/01-foundation/01-PATTERNS.md lines 660-674 (ci.yml section)
    - /Users/machado/Projects/folhario/.planning/phases/01-foundation/01-CONTEXT.md D-25, D-26, D-28
  </read_first>
  <action>
Step 0 — Action 19: verify `actions/checkout@v5` and `actions/setup-node@v5` are published before pinning. Use the GitHub CLI (or curl against tags API):

```bash
# Check actions/checkout v5 availability
gh api repos/actions/checkout/tags 2>/dev/null | grep -q '"v5.' && CHECKOUT_VER=v5 || CHECKOUT_VER=v4
# Check actions/setup-node v5 availability
gh api repos/actions/setup-node/tags 2>/dev/null | grep -q '"v5.' && SETUPNODE_VER=v5 || SETUPNODE_VER=v4
echo "CHECKOUT_VER=$CHECKOUT_VER SETUPNODE_VER=$SETUPNODE_VER"
```

If `gh` CLI is unavailable, use `curl -s https://api.github.com/repos/actions/checkout/tags | grep '"name"' | head -5` and read the output.

Record the resolved versions in `.planning/phases/01-foundation/01-08-SUMMARY.md` under a section `## Resolved GitHub Actions versions (Action 19)`:

```markdown
## Resolved GitHub Actions versions (Action 19)

| Action | Requested | Resolved | Fallback? |
|--------|-----------|----------|-----------|
| actions/checkout | @v5 | @{resolved} | {yes/no} |
| actions/setup-node | @v5 | @{resolved} | {yes/no} |
| actions/cache | @v4 | @v4 | no |
| actions/upload-artifact | @v4 | @v4 | no |
```

Step 1 — Create `.github/workflows/` directory if missing, then write `.github/workflows/ci.yml`. Substitute `${CHECKOUT_VER}` and `${SETUPNODE_VER}` with the resolved literals from Step 0 (e.g. `@v5` or `@v4`):

```yaml
name: CI

on:
  pull_request:
  push:
    branches: [main]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

env:
  NODE_VERSION: "22"

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    services:
      postgres:
        image: postgres:17-alpine
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: folhario_test
        ports: ["5432:5432"]
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - uses: actions/checkout@v5

      - name: Enable Corepack (pnpm)
        run: corepack enable

      - name: Setup Node
        uses: actions/setup-node@v5
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Get pnpm store path
        id: pnpm-cache
        shell: bash
        run: echo "STORE_PATH=$(pnpm store path --silent)" >> "$GITHUB_OUTPUT"

      - name: Cache pnpm store
        uses: actions/cache@v4
        with:
          path: ${{ steps.pnpm-cache.outputs.STORE_PATH }}
          key: pnpm-store-${{ runner.os }}-${{ hashFiles('pnpm-lock.yaml') }}
          restore-keys: |
            pnpm-store-${{ runner.os }}-

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Lint
        run: pnpm lint

      - name: Typecheck
        run: pnpm typecheck

      - name: Unit tests
        env:
          DATABASE_URL: postgres://postgres:postgres@localhost:5432/folhario_test
          DATABASE_POOL_URL: postgres://postgres:postgres@localhost:5432/folhario_test
          SUPABASE_SERVICE_ROLE_KEY: test
          NEXT_PUBLIC_SUPABASE_URL: http://localhost:54321
          NEXT_PUBLIC_SUPABASE_ANON_KEY: anon
          IDENTIFICATION_PROVIDER_MODE: stub
        run: pnpm test:unit

      - name: Integration tests (includes diagnostics-server-probe per D-27-a)
        env:
          DATABASE_URL: postgres://postgres:postgres@localhost:5432/folhario_test
          DATABASE_POOL_URL: postgres://postgres:postgres@localhost:5432/folhario_test
          SUPABASE_SERVICE_ROLE_KEY: test
          NEXT_PUBLIC_SUPABASE_URL: http://localhost:54321
          NEXT_PUBLIC_SUPABASE_ANON_KEY: anon
          IDENTIFICATION_PROVIDER_MODE: stub
          NEXT_PUBLIC_POSTHOG_KEY: ph_test_key
        run: pnpm test:integration

      - name: Build
        env:
          DATABASE_URL: postgres://postgres:postgres@localhost:5432/folhario_test
          DATABASE_POOL_URL: postgres://postgres:postgres@localhost:5432/folhario_test
          SUPABASE_SERVICE_ROLE_KEY: test
          NEXT_PUBLIC_SUPABASE_URL: http://localhost:54321
          NEXT_PUBLIC_SUPABASE_ANON_KEY: anon
          IDENTIFICATION_PROVIDER_MODE: stub
        run: pnpm build

      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-${{ runner.os }}-${{ hashFiles('pnpm-lock.yaml') }}
          restore-keys: |
            playwright-${{ runner.os }}-

      - name: Install Playwright Chromium
        run: pnpm exec playwright install chromium --with-deps

      - name: Playwright smoke
        env:
          NEXT_PUBLIC_SENTRY_DSN: ${{ secrets.SENTRY_DSN_CI }}
          NEXT_PUBLIC_POSTHOG_KEY: ${{ secrets.POSTHOG_KEY_CI }}
          NEXT_PUBLIC_POSTHOG_HOST: https://us.i.posthog.com
          IDENTIFICATION_PROVIDER_MODE: stub
          DATABASE_URL: postgres://postgres:postgres@localhost:5432/folhario_test
          DATABASE_POOL_URL: postgres://postgres:postgres@localhost:5432/folhario_test
          SUPABASE_SERVICE_ROLE_KEY: test
          NEXT_PUBLIC_SUPABASE_URL: http://localhost:54321
          NEXT_PUBLIC_SUPABASE_ANON_KEY: anon
          SENTRY_RELEASE: ${{ github.sha }}
        run: pnpm test:e2e

      - name: Upload Playwright report
        uses: actions/upload-artifact@v4
        if: ${{ !cancelled() }}
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 14
```

CRITICAL: do NOT add a Sentry source-map upload step (Pitfall 7). Do NOT set `SENTRY_AUTH_TOKEN` in the Build step. Phase 12 owns production source-map upload in `deploy-production.yml`.

Step 2 — YAML validation:

```bash
python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"
```
  </action>
  <verify>
    <automated>test -f .github/workflows/ci.yml && grep -q "postgres:17-alpine" .github/workflows/ci.yml && grep -q "cancel-in-progress: true" .github/workflows/ci.yml && grep -q "actions/cache@v4" .github/workflows/ci.yml && grep -q "playwright install chromium" .github/workflows/ci.yml && grep -q "SENTRY_DSN_CI" .github/workflows/ci.yml && grep -q "POSTHOG_KEY_CI" .github/workflows/ci.yml && ! grep -q "^[^#]*SENTRY_AUTH_TOKEN:" .github/workflows/ci.yml && python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` created, YAML parses cleanly
    - `grep -q "postgres:17-alpine" .github/workflows/ci.yml` matches
    - `grep -q "cancel-in-progress: true" .github/workflows/ci.yml` matches
    - `grep -q "SENTRY_DSN_CI" .github/workflows/ci.yml` matches
    - `grep -q "POSTHOG_KEY_CI" .github/workflows/ci.yml` matches
    - `! grep -q "^[^#]*SENTRY_AUTH_TOKEN:" .github/workflows/ci.yml` (Pitfall 7 — no auth token)
    - Integration step sets `NEXT_PUBLIC_POSTHOG_KEY: ph_test_key` so the server-probe mock has a non-empty key to unlock `getPostHog()`
    - `01-08-SUMMARY.md` records resolved action versions (Action 19)
  </acceptance_criteria>
  <done>
    - CI workflow created with 8-step pipeline + postgres:17-alpine + caches
    - Build step has NO SENTRY_AUTH_TOKEN (Pitfall 7)
    - E2E step has CI secrets wired
    - Playwright report uploaded as artifact on any outcome
    - Action versions verified (Action 19)
  </done>
</task>

<task type="auto">
  <name>Task 2: Update .planning/REQUIREMENTS.md — INFRA-12 text amend + OBS-05 defer to Phase 13 (user decision 2)</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - /Users/machado/Projects/folhario/.planning/REQUIREMENTS.md (find INFRA-12 line + OBS-05 mapping)
    - /Users/machado/Projects/folhario/.planning/phases/01-foundation/01-CONTEXT.md D-25 + Deferred Ideas (OBS-05 user decision 2 — 2026-04-23)
  </read_first>
  <action>
Step 1 — Confirm current INFRA-12 wording:

```bash
grep -n "postgres:16-alpine" .planning/REQUIREMENTS.md
```

Expect one match.

Step 2 — Use the Edit tool to replace the INFRA-12 bullet. Old string (find exact text in REQUIREMENTS.md):

```
- [ ] **INFRA-12**: `ci.yml` GitHub Action: install, lint, typecheck, unit (Vitest), integration against real `postgres:16-alpine` service container with Drizzle migrations seeded, build (§20)
```

New string:

```
- [ ] **INFRA-12**: `ci.yml` GitHub Action: install, lint, typecheck, unit (Vitest), integration against real `postgres:17-alpine` service container with Drizzle migrations seeded, build (§20). *(Amended 2026-04-23 per Phase 1 D-25 — parity with local Supabase Postgres 17.)*
```

Step 3 — Move OBS-05 mapping from Phase 1 -> Phase 13 per user decision 2. The Requirements-Traceability table in REQUIREMENTS.md maps each requirement ID to a phase. Locate the OBS-05 row (search `grep -n "OBS-05" .planning/REQUIREMENTS.md`) and use the Edit tool to change its phase column from `Phase 1` to `Phase 13`. Append a parenthetical rationale `*(Deferred from Phase 1 -> Phase 13 on 2026-04-23 per user decision; alert rules need real traffic to tune thresholds.)*` to the OBS-05 description.

Step 4 — Verify acceptance:

```bash
! grep -q "postgres:16-alpine" .planning/REQUIREMENTS.md && grep -q "postgres:17-alpine" .planning/REQUIREMENTS.md
grep -q "OBS-05.*Phase 13" .planning/REQUIREMENTS.md && echo OK || (echo "FAIL: OBS-05 not mapped to Phase 13" && exit 1)
```
  </action>
  <verify>
    <automated>! grep -q "postgres:16-alpine" /Users/machado/Projects/folhario/.planning/REQUIREMENTS.md && grep -q "postgres:17-alpine" /Users/machado/Projects/folhario/.planning/REQUIREMENTS.md && grep -q "Amended 2026-04-23" /Users/machado/Projects/folhario/.planning/REQUIREMENTS.md && grep -q "OBS-05.*Phase 13" /Users/machado/Projects/folhario/.planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `! grep -q "postgres:16-alpine" .planning/REQUIREMENTS.md` — no stale mention of 16
    - `grep -q "postgres:17-alpine" .planning/REQUIREMENTS.md` — amended text present
    - `grep -q "Amended 2026-04-23" .planning/REQUIREMENTS.md` — amendment marker
    - `grep -q "OBS-05.*Phase 13" .planning/REQUIREMENTS.md` — OBS-05 re-mapped per user decision 2
    - `! grep -q "OBS-05.*Phase 1$" .planning/REQUIREMENTS.md` — old Phase 1 mapping gone
  </acceptance_criteria>
  <done>
    - INFRA-12 text reads postgres:17-alpine
    - OBS-05 mapped to Phase 13 (user decision 2)
    - Both amendments have visible 2026-04-23 markers
  </done>
</task>

<task type="auto">
  <name>Task 3: Update .planning/ROADMAP.md — SC-4 wording (user decision 1) + Phase 1 plan list (9 plans, Action 16+20) + Phase 13 OBS-05 addition</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - /Users/machado/Projects/folhario/.planning/ROADMAP.md (Phase 1 goal/success criteria block lines ~31-52)
    - /Users/machado/Projects/folhario/.planning/phases/01-foundation/01-CONTEXT.md Deferred Ideas (SC-4 + OBS-05)
  </read_first>
  <action>
Step 1 — Replace Phase 1 SC-4 wording. Use Edit tool. Old string (currently on ~line 39 of ROADMAP.md):

```
  4. A deliberately thrown error in the local app appears in Sentry with `Authorization`, `Cookie`, `email`, `password`, `token`, and `photo_url` scrubbed, `Sentry.setUser({ id })` only, and request bodies dropped on `/api/v1/identifications/*` routes; a PostHog ping event fires from both the client and `posthog-node` server and lands in the PostHog US project — both verified automatically by the Playwright smoke.
```

New string (user decision 1):

```
  4. A deliberately thrown error in the local app appears in Sentry with `Authorization`, `Cookie`, `email`, `password`, `token`, and `photo_url` scrubbed, `Sentry.setUser({ id })` only, and request bodies dropped on `/api/v1/identifications/*` routes. A PostHog ping event fires from both the client and `posthog-node` server. Client-side envelopes are verified automatically by the Playwright smoke (Sentry transport + PostHog `$pageview` intercepted at the network layer). Server-side envelopes are verified in two ways: (a) an automated Playwright assertion that the server route handler successfully called its Sentry/PostHog client (via a server-side log probe or transport spy), and (b) a manual dashboard check recorded in `01-08-SUMMARY.md` with screenshot links.
```

Step 2 — Update Phase 1 Plans metadata. Old:

```
**Plans**: 8 plans
```

New:

```
**Plans**: 9 plans
```

Step 3 — Replace the Phase 1 plan list (current 8 entries -> new 9 entries reflecting 05 split). Old block:

```
Plans:
- [ ] 01-01-PLAN.md -- Repo tooling bootstrap
- [ ] 01-02-PLAN.md -- Scaffold + TDD error registry + TDD Zod env
- [ ] 01-03-PLAN.md -- Next 16 app + i18n + proxy + Serwist + next.config.ts
- [ ] 01-04-PLAN.md -- Local Supabase Docker stack + env sync script
- [ ] 01-05-PLAN.md -- TDD Sentry scrub (LGPD-13) + three init files
- [ ] 01-06-PLAN.md -- Hand-rolled PostHog providers
- [ ] 01-07-PLAN.md -- Diagnostics routes + 4 Playwright E2E specs
- [ ] 01-08-PLAN.md -- ci.yml + REQUIREMENTS.md INFRA-12 amendment
```

New:

```
Plans:
- [ ] 01-01-PLAN.md -- Repo tooling bootstrap (pre-flight eslint spike + patch-version resolution)
- [ ] 01-02-PLAN.md -- Scaffold + TDD error registry + TDD Zod env SPLIT into server-env + client-env
- [ ] 01-03-PLAN.md -- Next 16 app + i18n + proxy (locale only) + Serwist + next.config.ts with headers() for every response
- [ ] 01-04-PLAN.md -- Local Supabase Docker stack + env sync script + postgres-js as prod dep + supabase.co guard
- [ ] 01-05a-PLAN.md -- TDD Sentry scrub module (LGPD-13 load-bearing)
- [ ] 01-05b-PLAN.md -- Sentry init file wiring (server+edge+instrumentation-client) + instrumentation.ts cleanup
- [ ] 01-06-PLAN.md -- Hand-rolled PostHog providers (client-env import only per Action 4)
- [ ] 01-07-PLAN.md -- Diagnostics routes + 5 Playwright specs + Vitest server-probe (SC-4 server-auto)
- [ ] 01-08-PLAN.md -- ci.yml + REQUIREMENTS.md + ROADMAP.md amendments (including this file)
```

Step 4 — Update Phase 13 scope to include OBS-05 moved from Phase 1 (user decision 2). Find the Phase 13 entry in ROADMAP.md (`grep -n "Phase 13:" .planning/ROADMAP.md`) and append `OBS-05` to its Requirements line. Example: if current reads `**Requirements**: OBS-03, OBS-04`, change to `**Requirements**: OBS-03, OBS-04, OBS-05 *(deferred from Phase 1 on 2026-04-23 per user decision; alert rules need real traffic)*`.

Step 5 — Verification:

```bash
grep -q "01-05a-PLAN.md" .planning/ROADMAP.md && \
grep -q "01-05b-PLAN.md" .planning/ROADMAP.md && \
! grep -q "01-05-PLAN.md" .planning/ROADMAP.md && \
grep -q "9 plans" .planning/ROADMAP.md && \
grep -q "Vitest" .planning/ROADMAP.md && \
grep -q "screenshot links" .planning/ROADMAP.md
```
  </action>
  <verify>
    <automated>grep -q "01-05a-PLAN.md" .planning/ROADMAP.md && grep -q "01-05b-PLAN.md" .planning/ROADMAP.md && ! grep -q "01-05-PLAN.md[^ab]" .planning/ROADMAP.md && grep -q "9 plans" .planning/ROADMAP.md && grep -q "screenshot links" .planning/ROADMAP.md && grep -q "OBS-05" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "01-05a-PLAN.md" .planning/ROADMAP.md` matches
    - `grep -q "01-05b-PLAN.md" .planning/ROADMAP.md` matches
    - `! grep -q "01-05-PLAN.md " .planning/ROADMAP.md` (old non-split name removed from plan list — 05 with trailing space)
    - `grep -q "Plans.*9 plans" .planning/ROADMAP.md` matches (Phase 1 count updated)
    - `grep -q "screenshot links" .planning/ROADMAP.md` matches (SC-4 wording amended per user decision 1)
    - `grep -q "OBS-05" .planning/ROADMAP.md` appears under Phase 13 section (user decision 2)
  </acceptance_criteria>
  <done>
    - ROADMAP Phase 1 SC-4 wording amended per user decision 1
    - Phase 1 plan list shows 9 plans including 05a + 05b
    - OBS-05 moved to Phase 13 scope
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human checkpoint — configure GitHub secrets, run first CI PR, record manual dashboard verification with screenshot links in SUMMARY</name>
  <files>.planning/phases/01-foundation/01-08-SUMMARY.md (manual dashboard verification section — human populates)</files>
  <read_first>
    - /Users/machado/Projects/folhario/.github/workflows/ci.yml (from Task 1)
    - /Users/machado/Projects/folhario/.planning/ROADMAP.md (revised SC-4 — human verifies manual dashboard portion)
  </read_first>
  <action>Pause execution and wait for the developer to configure the dashboards + secrets, open a test PR, record the results, AND populate the 01-08-SUMMARY.md `## Manual dashboard verification` section with screenshot links per user decision 1.</action>
  <verify>Developer replies with `approved` after the PR's `ci.yml` run is fully green AND the Sentry/PostHog CI projects show the expected events AND 01-08-SUMMARY.md contains the filled-in Manual dashboard verification section.</verify>
  <acceptance_criteria>
    - Developer populates `.planning/phases/01-foundation/01-08-SUMMARY.md` `## Manual dashboard verification (user decision 1 / SC-4 (b))` section with two screenshot links:
      - Sentry CI project issue URL (redacted as needed)
      - PostHog CI project event URL (redacted as needed)
    - Developer confirms PR run URL + CI job duration
    - `grep -q "Manual dashboard verification" .planning/phases/01-foundation/01-08-SUMMARY.md` matches
    - `grep -qE "https://sentry\\.io|https://.*sentry\\..*/issues" .planning/phases/01-foundation/01-08-SUMMARY.md` matches
    - `grep -qE "posthog\\.com" .planning/phases/01-foundation/01-08-SUMMARY.md` matches
  </acceptance_criteria>
  <done>Approval received + SUMMARY populated OR issue details captured (CI log URL + Sentry issue URL + PostHog event URL).</done>
  <what-built>
    Plan 08 Task 1 created `.github/workflows/ci.yml`. Task 2 corrected the INFRA-12 requirement text + moved OBS-05 to Phase 13. Task 3 updated ROADMAP.md SC-4 wording + plan list + Phase 13 scope. What remains is human-only:

    1. Two Sentry and PostHog projects must be provisioned
    2. Two GitHub repository secrets must be set
    3. A test PR must open to verify the pipeline actually runs green end-to-end
    4. Manual confirmation that ≥1 Sentry event and ≥1 PostHog event landed in the CI projects
    5. Populate `01-08-SUMMARY.md` `## Manual dashboard verification` section with screenshot links per user decision 1 / SC-4 (b)
  </what-built>
  <how-to-verify>
    1. In https://sentry.io, create a new project under your org named `folhario-ci` with platform `Next.js`. Copy the DSN.
    2. In https://us.posthog.com, create a new project named `folhario-ci`. Copy the Project API key.
    3. In GitHub: `Settings` -> `Secrets and variables` -> `Actions` -> `New repository secret`:
       - Name: `SENTRY_DSN_CI`, Value: (the DSN from step 1)
       - Name: `POSTHOG_KEY_CI`, Value: (the API key from step 2)
    4. Push a branch (e.g. `ci-smoke-test`) with a trivial README edit and open a PR against `main`. The workflow should trigger automatically.
    5. Watch the `build-and-test` job run. All steps should be green:
       - Install / Lint / Typecheck
       - Unit tests / Integration tests (against postgres:17-alpine, including the new `diagnostics-server-probe` test per D-27-a)
       - Build
       - Playwright Chromium install + smoke (5 specs including sentry-local-mode guard)
       - Upload Playwright report
    6. Once the job completes, open the Sentry `folhario-ci` project -> Issues. Expect to see at least 1 issue from "Playwright diagnostics client error" AND "Playwright diagnostics server error". Verify the issue details do NOT contain `scrub-me`, `should-be-scrubbed`, or any of the other forbidden scrub sentinels.
    7. Open the PostHog `folhario-ci` project -> Activity / Events. Expect to see both `$diagnostics_client_ping` AND `$diagnostics_server_ping` events.
    8. **Populate `01-08-SUMMARY.md`** with a new section (template below). Screenshot or copy direct links to the Sentry issue and PostHog event:

       ```markdown
       ## Manual dashboard verification (user decision 1 / SC-4 (b))

       - **Sentry CI issue URL:** https://sentry.io/organizations/{org}/issues/{id}/ (screenshot: {link})
       - **PostHog CI event URL:** https://us.posthog.com/project/{id}/events?eventName=%24diagnostics_server_ping (screenshot: {link})
       - **Verified by:** {developer name}
       - **Verified at:** {ISO timestamp}
       - **Forbidden sentinels absent in Sentry issue details:** YES / NO (if NO — attach Sentry issue JSON export)
       ```
  </how-to-verify>
  <resume-signal>
    Reply `approved` once the PR run is green AND Sentry + PostHog CI projects show the expected events AND the 01-08-SUMMARY.md `## Manual dashboard verification` section is populated with screenshot links.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner -> Postgres service container | Service-container credentials are ephemeral per job |
| CI runner -> Sentry/PostHog ingest | Secrets live in GitHub repo secrets, never in source |
| Build artifact -> Playwright report | Report uploaded as actions/upload-artifact (private per repo ACL) |
| Planning docs -> scope truthfulness | REQUIREMENTS.md + ROADMAP.md edits must reflect the shipped 9-plan reality (not drift to old 8-plan wording) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Information Disclosure | CI secrets leaked in build logs | mitigate | GitHub Actions automatic secret redaction; no `echo` of `NEXT_PUBLIC_SENTRY_DSN` or `NEXT_PUBLIC_POSTHOG_KEY` in any step. |
| T-08-02 | Tampering | Malicious dependency in pnpm-lock.yaml | mitigate | `pnpm install --frozen-lockfile` — any lockfile mutation fails CI. |
| T-08-03 | Denial of Service | Runaway integration tests holding Postgres connections | mitigate | `timeout-minutes: 20` on the job; `testTimeout: 30_000` in vitest integration project config (Plan 01). |
| T-08-04 | Repudiation | Source-map upload leaking auth token | mitigate | Pitfall 7: `SENTRY_AUTH_TOKEN` NEVER set in Phase 1 CI. Upload is Phase 12 `deploy-production.yml` only. |
| T-08-05 | Elevation of Privilege | `pull_request_target` event granting full secret access to fork PRs | mitigate | Workflow uses `on: pull_request` (NOT `pull_request_target`). Fork PRs run without secrets; CI-gated diagnostics assertions tolerate empty DSN/key. |
| T-08-06 | Repudiation | Planning doc drift (ROADMAP/REQUIREMENTS claiming OBS-05 is in Phase 1 while it ships in Phase 13) | mitigate | Tasks 2+3 grep-verify both files agree: OBS-05 mapped to Phase 13 in BOTH REQUIREMENTS.md and ROADMAP.md. Amendment markers (`Amended 2026-04-23`) document why. |

Threats deferred: Dependabot auto-merge policy; branch protection rules on `main`; runner image hardening.
</threat_model>

<verification>
1. `.github/workflows/ci.yml` exists and parses as valid YAML.
2. `grep -c "postgres:17-alpine" .github/workflows/ci.yml` returns 1.
3. `grep -c "cancel-in-progress: true" .github/workflows/ci.yml` returns 1.
4. `grep -c "SENTRY_DSN_CI" .github/workflows/ci.yml` returns 1.
5. `grep -c "POSTHOG_KEY_CI" .github/workflows/ci.yml` returns 1.
6. `! grep -q "^[^#]*SENTRY_AUTH_TOKEN:" .github/workflows/ci.yml` (no auth token — Pitfall 7).
7. No `postgres:16-alpine` occurrences remain in `.planning/REQUIREMENTS.md`.
8. `grep -q "OBS-05.*Phase 13" .planning/REQUIREMENTS.md` (user decision 2).
9. `grep -q "screenshot links" .planning/ROADMAP.md` (SC-4 amended — user decision 1).
10. `grep -q "01-05a-PLAN.md\\|01-05b-PLAN.md" .planning/ROADMAP.md` AND `grep -q "9 plans" .planning/ROADMAP.md` (plan list + count updated — Action 16+20).
11. Human checkpoint green — PR run succeeds, SUMMARY populated with dashboard links.
</verification>

<success_criteria>
- [ ] `.github/workflows/ci.yml` created; 8-step pipeline; postgres:17-alpine; caches; pnpm + Playwright
- [ ] Build step has NO SENTRY_AUTH_TOKEN (Pitfall 7)
- [ ] E2E step has CI secrets wired; Integration step sets NEXT_PUBLIC_POSTHOG_KEY for server-probe mock
- [ ] Playwright report uploaded as artifact
- [ ] `.planning/REQUIREMENTS.md` INFRA-12 reads postgres:17-alpine; OBS-05 mapped to Phase 13
- [ ] `.planning/ROADMAP.md` Phase 1 SC-4 wording amended; plan list shows 9 plans (05a + 05b); Phase 13 scope includes OBS-05
- [ ] Human checkpoint passes — first PR CI run green, SUMMARY populated with Sentry/PostHog screenshot links
- [ ] Action 19: resolved actions/checkout + actions/setup-node versions recorded in SUMMARY
</success_criteria>

<output>
After completion, finalize `.planning/phases/01-foundation/01-08-SUMMARY.md` with:
- First CI run URL + duration
- Resolved actions/checkout + actions/setup-node versions (Action 19)
- `## Manual dashboard verification (user decision 1 / SC-4 (b))` section with Sentry + PostHog screenshot links
- Confirmation all 3 planning docs (REQUIREMENTS.md, ROADMAP.md, CONTEXT.md) agree on the revised Phase 1 scope
- Any flakes encountered + root cause + mitigation
- "Lessons learned" note: execute plans (01, 03, 04, 06, 08) can be looser on exact commit shapes in future phases (Action 21 — informational)
- Launch of Phase 2 readiness: all 5 ROADMAP success criteria for Phase 1 verified
</output>
