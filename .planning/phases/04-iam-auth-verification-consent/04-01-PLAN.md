---
phase: 04-iam-auth-verification-consent
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/04-iam-auth-verification-consent/04-PREREQ-AUDIT.md
autonomous: false
requirements: []
must_haves:
  truths:
    - "Phase 2 deliverables required by Phase 4 are verified present"
    - "Phase 3 design tokens + UI primitives are verified present (or absence is documented as ship blocker)"
    - "Plan 02+ executor sees a written audit document describing exactly which Phase 2/3 artifacts exist"
    - "If anything is missing, execution blocks until the user runs the missing phase first"
  artifacts:
    - path: ".planning/phases/04-iam-auth-verification-consent/04-PREREQ-AUDIT.md"
      provides: "Written audit of Phase 2 + Phase 3 prerequisite state"
      contains: "Status: GO or BLOCKED"
  key_links:
    - from: ".planning/STATE.md"
      to: "Phase 2/3 completion status"
      via: "STATE.md `progress.completed_phases` + per-phase plan completion table"
      pattern: "Phase 2.*Plans Complete"
---

<objective>
Verify that the Phase 2 (Data Layer & Bounded Contexts) and Phase 3 (Design System & App Shell) deliverables Phase 4 inherits are actually present in the worktree before any Phase 4 implementation begins.

Purpose: Phase 4 RESEARCH.md "Phase 2 Inheritance Audit" lists ~13 Phase 2 deliverables Phase 4 depends on. STATE.md reports Phase 2 = 0/10 plans complete and Phase 3 has not been planned. UI-SPEC.md:38 explicitly flags Phase 3 as a hard prerequisite. This plan is a one-task gate that audits the worktree, writes a PREREQ-AUDIT.md document with a binary GO/BLOCKED verdict, and — if BLOCKED — exits non-zero so the executor halts before any Phase 4-specific work runs.

Output: `.planning/phases/04-iam-auth-verification-consent/04-PREREQ-AUDIT.md` containing concrete file/table existence checks and a final `Status: GO` or `Status: BLOCKED — run /gsd-execute-phase 2 (and 3) first` line.
</objective>

<execution_context>
@/Users/machado/Projects/folhario/.claude/get-shit-done/workflows/execute-plan.md
@/Users/machado/Projects/folhario/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-iam-auth-verification-consent/04-CONTEXT.md
@.planning/phases/04-iam-auth-verification-consent/04-RESEARCH.md
@.planning/phases/04-iam-auth-verification-consent/04-PATTERNS.md
@.planning/phases/04-iam-auth-verification-consent/04-UI-SPEC.md

<interfaces>
<!-- The audit checks for the EXISTENCE of these files/tables/exports. Each check below is the contract Plan 02+ assumes is true. -->

Phase 2 expected deliverables (from RESEARCH.md "Phase 2 Inheritance Audit" lines 1086-1107):

1. `src/contexts/iam/infrastructure/db/schema.ts` exists and exports `users` table (Drizzle pgTable) with at least: id, email, age_confirmed_at, timezone, partner_code, deletion_requested_at, created_at columns (Phase 4 ALTER will add `email_verified_at`).
2. `src/contexts/iam/infrastructure/db/schema.ts` (or peer file in iam) exports `consentLogs` (or equivalent) with `purpose` literal union including BOTH "terms_of_service" AND "privacy_policy", and `source` literal union including "signup" AND "settings".
3. `policy_versions` table exists with `is_current` boolean column.
4. `subscriptions` table exists with `status` enum including "trialing".
5. `idempotency_keys` table exists.
6. Database `auth.users → public.users` insert trigger exists (D-35) — visible via `SELECT tgname FROM pg_trigger`.
7. Runtime DB client `src/contexts/iam/infrastructure/db/client.ts` (or `src/shared/db/client.ts`) exists, uses `postgres-js` with `prepare: false`.
8. `src/contexts/iam/infrastructure/auth-adapter.ts` (or equivalent) exists and exports JWT verification functions (`verifyJWT`, `getUserById`).
9. `src/proxy.ts` is API-aware (does NOT skip `/api/*`) — D-34 prerequisite for Phase 4 D-21.
10. RLS enabled on `users`, `consent_logs`, `subscriptions`, `idempotency_keys` tables.

Phase 3 expected deliverables (UI-SPEC.md:38):

1. Design tokens (Paper Cream + Night Cream palettes) defined in code (Tailwind config, CSS variables, or theme module).
2. Source Serif 4 + Plus Jakarta Sans fonts loaded.
3. Focus ring (3px Canopy @ 40% opacity, 2px offset, 8px radius) applied globally.
4. Button, Input, FormField, Alert primitives exist (or hand-rolled equivalents per UI-SPEC §17).
5. Bottom-nav primitive exists.
</interfaces>
</context>

<threat_model>

## Trust Boundaries

| Boundary                      | Description                                                              |
| ----------------------------- | ------------------------------------------------------------------------ |
| Plan 01 audit script ↔ Phase 4 | Audit must NOT silently pass when prereqs are missing — that would surface a phantom-pass to downstream plans. |

## STRIDE Threat Register

| Threat ID | Category   | Component                          | Disposition | Mitigation Plan                                                                                                                                                                                                                                                                                       |
| --------- | ---------- | ---------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-04-01-01 | Tampering  | PREREQ-AUDIT.md verdict line       | mitigate    | The audit task uses `grep` / `psql` / `ls` checks; the final Status line is computed from those checks, not human-judged. Acceptance criteria require the audit file to contain BOTH "Status: GO" or "Status: BLOCKED" verbatim AND a per-check checklist with ✅ / ❌. No subjective approval allowed. |
| T-04-01-02 | Repudiation | Audit decision lacks evidence      | mitigate    | Every check in the audit document references the file path or SQL it ran. Reviewers can re-execute the same commands and verify the verdict.                                                                                                                                                          |
| T-04-01-03 | Information Disclosure | Audit script logs DB credentials   | accept      | The audit uses `DATABASE_POOL_URL` from `.env.local` for local checks only; no credentials are written to the audit document. Sentry scrub already protects logs (Phase 1 D-22 / LGPD-13).                                                                                                              |

</threat_model>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Audit Phase 2 + Phase 3 prerequisites and write PREREQ-AUDIT.md</name>
  <files>.planning/phases/04-iam-auth-verification-consent/04-PREREQ-AUDIT.md</files>
  <read_first>
    - `.planning/STATE.md` — current phase progress (Phase 2 = 0/10 expected per orchestrator handoff)
    - `.planning/phases/04-iam-auth-verification-consent/04-RESEARCH.md` lines 1086-1107 — full Phase 2 Inheritance Audit table
    - `.planning/phases/04-iam-auth-verification-consent/04-UI-SPEC.md` line 38 — Phase 3 prerequisite flag
    - `.planning/phases/04-iam-auth-verification-consent/04-PATTERNS.md` lines 933-965 — Hard Prerequisites the Planner Surfaces
    - `src/contexts/iam/infrastructure/` directory listing — current empty/.gitkeep state confirms Phase 2 hasn't shipped IAM infra
    - `src/proxy.ts` lines 1-15 — confirm whether Phase 2 D-34 (API-aware proxy) shipped
    - Cite decision IDs: D-19 (AuthAdapter scope), D-21 (gate enforcement), D-22 (email_verified_at source of truth), D-25 (signup atomicity)
  </read_first>
  <action>
    Run a series of concrete existence checks and write each result into `.planning/phases/04-iam-auth-verification-consent/04-PREREQ-AUDIT.md` as a markdown checklist. Then compute a verdict on the last line.

    **Phase 2 file/code checks (each must produce ✅ or ❌ with the command output):**

    1. `test -f src/contexts/iam/infrastructure/db/schema.ts && grep -E 'export const users|users.*pgTable' src/contexts/iam/infrastructure/db/schema.ts` — ✅ if file exists AND exports `users` table.
    2. `grep -E 'consentLogs|consent_logs.*pgTable' src/contexts/iam/infrastructure/db/schema.ts` — ✅ if `consentLogs` table is exported.
    3. `grep -E "purpose.*'terms_of_service'.*'privacy_policy'|'privacy_policy'.*'terms_of_service'" src/contexts/iam/infrastructure/db/schema.ts` — ✅ if purpose union includes both literals.
    4. `grep -E "source.*'signup'.*'settings'|'settings'.*'signup'" src/contexts/iam/infrastructure/db/schema.ts` — ✅ if source union includes both.
    5. `grep -E 'policyVersions|policy_versions.*pgTable' src/contexts/iam/infrastructure/db/schema.ts && grep -E 'is_current|isCurrent' src/contexts/iam/infrastructure/db/schema.ts` — ✅ if `policy_versions` table with `is_current` column.
    6. `grep -E 'subscriptions.*pgTable' src/contexts/iam/infrastructure/db/schema.ts && grep -E "'trialing'" src/contexts/iam/infrastructure/db/schema.ts` — ✅ if `subscriptions` table with status `trialing`.
    7. `grep -rE 'idempotencyKeys|idempotency_keys.*pgTable' src/contexts/ src/shared/db/` — ✅ if `idempotency_keys` table exists somewhere.
    8. `test -f src/contexts/iam/infrastructure/db/client.ts || test -f src/shared/db/client.ts` AND `grep -E "prepare:\s*false" {found-file}` — ✅ if runtime DB client exists with `{ prepare: false }`.
    9. `test -f src/contexts/iam/infrastructure/auth-adapter.ts` (or `src/contexts/iam/infrastructure/supabase-auth-adapter.ts`) AND `grep -E 'verifyJWT|getUserById|getCurrentUser' {found-file}` — ✅ if AuthAdapter exists with required exports.
    10. `grep -E 'matcher|/api/v1' src/proxy.ts` — ✅ if proxy is API-aware (covers `/api/v1/*`); ❌ if proxy still excludes `/api/*` per Phase 1 D-24 baseline.
    11. If `pnpm db:start` is running locally, query `psql` for: `SELECT relname, relrowsecurity FROM pg_class WHERE relname IN ('users','consent_logs','subscriptions','idempotency_keys') AND relnamespace = 'public'::regnamespace;` — ✅ if RLS is `t` (true) on all 4 tables. If `pnpm db:start` is NOT running, mark this check as ⚠ NOT VERIFIED with note "run `pnpm db:start` and re-run audit".
    12. `psql -c "SELECT tgname FROM pg_trigger WHERE tgname LIKE '%user%';" | grep -E 'on_auth_user_created|public_users_insert'` — ✅ if Phase 2 D-35 trigger exists.

    **Phase 3 UI primitive checks:**

    13. `find src -type f \( -name '*.css' -o -name 'tailwind.config.*' -o -name 'theme.ts' \) | xargs grep -l 'Paper Cream\|#FBF7EF\|paper-cream'` — ✅ if Paper Cream token defined.
    14. `grep -rE 'Source Serif|Plus Jakarta' src/app/ src/contexts/` — ✅ if fonts referenced.
    15. `find src -type f -name '*.tsx' | xargs grep -l 'role="button"\|<Button\|<Input\|<FormField'` — ✅ if at least one UI primitive exists in code.
    16. `find src/app -name 'layout.tsx' -exec grep -l 'BottomNav\|bottom-nav' {} \;` — ✅ if bottom-nav component is wired in a layout.

    **Audit document structure** (create the file with this exact skeleton):

    ```markdown
    # Phase 4 — Prerequisite Audit

    **Audited:** {ISO-8601 UTC timestamp with Z}
    **Auditor:** Claude (plan 04-01 task 1)

    ## Phase 2 Deliverables

    | # | Check | Status | Evidence |
    |---|-------|--------|----------|
    | 1 | iam/infrastructure/db/schema.ts exports `users` | ✅ / ❌ | {grep output excerpt} |
    | 2 | schema.ts exports `consentLogs` | ✅ / ❌ | {output} |
    | ... | (one row per check 1-12 above) | | |

    ## Phase 3 Deliverables

    | # | Check | Status | Evidence |
    |---|-------|--------|----------|
    | 13 | Paper Cream token defined in code | ✅ / ❌ | {output} |
    | ... | (rows 13-16) | | |

    ## Verdict

    Status: GO

    or

    Status: BLOCKED — run /gsd-execute-phase 2 (and 3) first
    Missing: <comma-separated check numbers from the tables above>

    ## Resolution path (when BLOCKED)

    1. Stop Phase 4 execution.
    2. Run /gsd-execute-phase 2.
    3. (If Phase 3 missing) plan + execute Phase 3.
    4. Re-run /gsd-execute-phase 4 — orchestrator will re-run this audit task as a no-op when the verdict flips to GO.
    ```

    **Verdict computation rule (NON-NEGOTIABLE):**

    - If ALL checks 1-12 are ✅ → Phase 2 prereqs PASS.
    - If ALL checks 13-16 are ✅ → Phase 3 prereqs PASS.
    - Both PASS → write `Status: GO`.
    - Either fails (any ❌, or ⚠ NOT VERIFIED for the RLS check #11) → write `Status: BLOCKED — run /gsd-execute-phase 2 (and 3) first` AND list the failing check numbers in the `Missing:` line.

    **If BLOCKED:** the task ALSO emits `process.exitCode = 1` (or equivalent) so the executor halts. The orchestrator surfaces the audit document path to the user. DO NOT attempt to "patch in" missing Phase 2 deliverables — Phase 4 plans 02-13 assume Phase 2/3 already shipped (per planner_authority_limits in the orchestrator instructions).

    Implementation choices:
    - Use shell commands directly via `Bash` tool (this is an audit, not production code).
    - Capture stdout into the audit document; truncate noisy output to the first 3 lines per check.
    - For psql checks, use `DATABASE_POOL_URL` from `.env.local` (already populated by Phase 1 plan 01-04). If the env var is empty, mark psql-dependent checks as ⚠ NOT VERIFIED.

    Per D-19 (AuthAdapter scope), D-21 (gate enforcement), D-22 (`email_verified_at` source of truth), D-25 (signup atomicity), and D-33 from Phase 2 (JWKS verification): all of these decisions ASSUME Phase 2 shipped. The audit makes that assumption falsifiable.
  </action>
  <what-built>
    A markdown audit document at `.planning/phases/04-iam-auth-verification-consent/04-PREREQ-AUDIT.md` with 16 concrete file/code/database checks and a final binary verdict line.
  </what-built>
  <how-to-verify>
    1. Open `.planning/phases/04-iam-auth-verification-consent/04-PREREQ-AUDIT.md`.
    2. Confirm the document has all 16 checks listed in the two tables.
    3. Confirm the final line is EITHER `Status: GO` or `Status: BLOCKED — run /gsd-execute-phase 2 (and 3) first`.
    4. If GO: phase 4 may proceed to plan 04-02.
    5. If BLOCKED: stop. Run `/gsd-execute-phase 2` first, then re-run this audit, then resume phase 4.
  </how-to-verify>
  <acceptance_criteria>
    - File exists: `test -f .planning/phases/04-iam-auth-verification-consent/04-PREREQ-AUDIT.md`
    - File contains the literal heading: `grep -c '^# Phase 4 — Prerequisite Audit' .planning/phases/04-iam-auth-verification-consent/04-PREREQ-AUDIT.md` returns 1
    - File contains 16 numbered check rows: `grep -cE '^\| [0-9]+ \|' .planning/phases/04-iam-auth-verification-consent/04-PREREQ-AUDIT.md` returns 16
    - File contains exactly one verdict line: `grep -cE '^Status: (GO|BLOCKED)' .planning/phases/04-iam-auth-verification-consent/04-PREREQ-AUDIT.md` returns 1
    - If verdict is `Status: GO`: every check row has ✅ in the Status column (no ❌, no ⚠) — `grep -cE '\| ❌ \|' .planning/phases/04-iam-auth-verification-consent/04-PREREQ-AUDIT.md` returns 0.
    - If verdict is `Status: BLOCKED`: file contains a `Missing:` line listing at least one failing check number — `grep -E '^Missing: ' .planning/phases/04-iam-auth-verification-consent/04-PREREQ-AUDIT.md` returns at least one match.
    - User has explicitly typed "approved" OR "BLOCKED-acknowledged-stopping" in the resume signal.
  </acceptance_criteria>
  <resume-signal>
    Type "approved" if the audit verdict is `Status: GO` (Phase 2/3 are present and Phase 4 may proceed), OR type "BLOCKED-acknowledged-stopping" if the verdict is `Status: BLOCKED` (the user will run /gsd-execute-phase 2 first and resume Phase 4 later).
  </resume-signal>
</task>

</tasks>

<verification>
- `04-PREREQ-AUDIT.md` exists in the phase directory.
- The audit contains all 16 checks with concrete evidence.
- The final verdict is binary (GO or BLOCKED) and matches the per-check states.
- If verdict is BLOCKED, no other Phase 4 plan executes until Phase 2/3 ship.
</verification>

<success_criteria>
- A reviewer reading `04-PREREQ-AUDIT.md` can re-run every command in the Evidence column and reproduce the verdict.
- The verdict is "GO" only when all Phase 2 + Phase 3 deliverables Phase 4 inherits are demonstrably present.
- A "BLOCKED" verdict halts Phase 4 execution before any irreversible work happens.
</success_criteria>

<output>
After completion, create `.planning/phases/04-iam-auth-verification-consent/04-01-SUMMARY.md` capturing:
- The verdict (GO / BLOCKED).
- A list of which Phase 2 / Phase 3 deliverables were missing (if any).
- Any non-trivial discoveries about the prerequisites (e.g. "Phase 2 schema added `email_verified_at` itself — Phase 4 plan 02 ALTER becomes a no-op").
</output>
</content>
</invoke>