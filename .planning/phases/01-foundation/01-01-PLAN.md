---
phase: 01
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .nvmrc
  - .gitignore
  - package.json
  - pnpm-lock.yaml
  - tsconfig.json
  - eslint.config.mjs
  - .prettierrc
  - .prettierignore
  - .commitlintrc.json
  - .husky/pre-commit
  - .husky/commit-msg
  - vitest.config.ts
  - playwright.config.ts
  - .env.example
autonomous: true
requirements:
  - INFRA-01
  - INFRA-17
  - INFRA-23
tags:
  - scaffold
  - tooling
  - test-infrastructure

must_haves:
  truths:
    - "Running `pnpm install --frozen-lockfile` succeeds from a clean clone"
    - "`pnpm typecheck` exits 0 against an empty src/"
    - "`pnpm lint` exits 0 with no src files yet"
    - "`pnpm exec vitest --run` discovers the unit + integration projects and reports 0 tests (no files yet)"
    - "`pnpm exec playwright --version` returns 1.59.1"
    - "Committing a file that violates Prettier rules triggers lint-staged via the pre-commit hook"
    - "Committing with a non-Conventional-Commits message is rejected by the commit-msg hook"
    - "`.env.example` contains every PRD §20 variable name with dummy values"
    - "`.gitignore` blocks `.env`, `.env.local`, `node_modules`, `.next`, `playwright-report`, `test-results`, `coverage`, `.vitest-cache`, `supabase/.temp`, `supabase/.branches`"
  artifacts:
    - path: ".nvmrc"
      provides: "Node 22 LTS version pin (D-01)"
      contains: "22"
    - path: "package.json"
      provides: "Corepack-activated pnpm, dependency pins, npm scripts, lint-staged config"
      exports: ["scripts.dev", "scripts.build", "scripts.start", "scripts.lint", "scripts.typecheck", "scripts.test:unit", "scripts.test:integration", "scripts.test:e2e", "scripts.db:start", "scripts.db:reset", "scripts.db:sync-env", "scripts.supabase", "scripts.prepare"]
    - path: "tsconfig.json"
      provides: "TS strict + noUncheckedIndexedAccess + per-root path aliases @contexts/*, @shared/*, @i18n/*"
      contains: "noUncheckedIndexedAccess"
    - path: "eslint.config.mjs"
      provides: "Flat ESLint config wrapping eslint-config-next + eslint-config-prettier (D-03)"
    - path: ".prettierrc"
      provides: "Prettier 3 formatting rules"
    - path: ".commitlintrc.json"
      provides: "Conventional Commits enforcement via @commitlint/config-conventional"
    - path: ".husky/pre-commit"
      provides: "Pre-commit hook running lint-staged (Husky 9 plain-script idiom)"
    - path: ".husky/commit-msg"
      provides: "Commit-msg hook running commitlint"
    - path: "vitest.config.ts"
      provides: "Two Vitest projects (unit + integration) via test.projects (v4 API, replaces workspace)"
      contains: "projects"
    - path: "playwright.config.ts"
      provides: "Chromium-only E2E runner + webServer against next start"
      contains: "chromium"
    - path: ".env.example"
      provides: "PRD §20 env var template with dummy values, grouped by phase of first use"
      contains: "IDENTIFICATION_PROVIDER_MODE"
    - path: ".gitignore"
      provides: "Full Next + Node + Supabase + test artifact exclusions"
      contains: ".env.local"
  key_links:
    - from: "package.json scripts.prepare"
      to: ".husky/"
      via: "Husky 9 `husky` CLI"
      pattern: "\"prepare\":\\s*\"husky\""
    - from: "package.json lint-staged"
      to: ".husky/pre-commit"
      via: "pnpm lint-staged invocation"
      pattern: "pnpm lint-staged"
    - from: "tsconfig.json paths"
      to: "vitest.config.ts tsconfigPaths plugin"
      via: "vite-tsconfig-paths resolves aliases at test time"
      pattern: "tsconfigPaths\\(\\)"
---

<objective>
Bootstrap the repository tooling: Node 22 pin, Corepack pnpm, TS strict + aliases, ESLint/Prettier, Husky + lint-staged + commitlint, Vitest 4 projects config, Playwright 1.59 config, `.gitignore` extension, and `.env.example` mirroring PRD §20. No source code — this plan lays the surface every later plan lands on.

Purpose: Every subsequent Phase 1 plan (and every future phase) depends on `package.json` dependencies being installed, the TS compiler accepting source files, Prettier/ESLint running without errors, and the test runners discovering projects. Getting the tooling wrong cascades into every downstream plan failing.

Output: A clean clone can run `pnpm install --frozen-lockfile && pnpm typecheck && pnpm lint` without errors. Test runners report empty test suites. Git hooks reject bad commits.
</objective>

<execution_context>
@/Users/machado/Projects/folhario/.claude/get-shit-done/workflows/execute-plan.md
@/Users/machado/Projects/folhario/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/machado/Projects/folhario/CLAUDE.md
@/Users/machado/Projects/folhario/.planning/ROADMAP.md
@/Users/machado/Projects/folhario/.planning/REQUIREMENTS.md
@/Users/machado/Projects/folhario/.planning/phases/01-foundation/01-CONTEXT.md
@/Users/machado/Projects/folhario/.planning/phases/01-foundation/01-RESEARCH.md
@/Users/machado/Projects/folhario/.planning/phases/01-foundation/01-PATTERNS.md
@/Users/machado/Projects/folhario/.planning/phases/01-foundation/01-VALIDATION.md
@/Users/machado/Projects/folhario/.env
@/Users/machado/Projects/folhario/.gitignore

<interfaces>
<!-- Dependency pins (from RESEARCH.md §Standard Stack + PATTERNS.md package.json section, verified against npm registry on 2026-04-23) -->

Runtime dependencies (exact versions):
- next@16.2.3
- react@19.2.5
- react-dom@19.2.5
- @serwist/next@9.5.7
- serwist@9.5.7
- @sentry/nextjs@10.48.0
- posthog-js@1.368.0
- posthog-node@5.29.7
- next-intl@4.9.1
- zod@4

Dev dependencies (caret/pinned):
- typescript@6
- @types/node@22 (NOT 25.x — must match Node 22 runtime)
- @types/react@19
- @types/react-dom@19
- eslint@10
- eslint-config-next@16.2.3
- eslint-config-prettier@10
- prettier@3
- vitest@4.1.4 (pinned — 4.1.5 drift acceptable but CLAUDE.md locks 4.1.4)
- @vitest/ui@4
- vite-tsconfig-paths@5 (resolves @contexts/*, @shared/*, @i18n/* in Vitest)
- @playwright/test@1.59.1
- husky@9
- lint-staged@16
- @commitlint/cli@20
- @commitlint/config-conventional@20
- supabase@2.95.0

TypeScript path aliases (MUST be in tsconfig.json — used by Plans 02/03/05/06/07):
- "@contexts/*" → "./src/contexts/*"
- "@shared/*"   → "./src/shared/*"
- "@i18n/*"     → "./src/i18n/*"   (reconciles R-1 from PATTERNS.md)

NPM scripts (downstream consumers expect these exact names):
- dev, build, start, lint, typecheck
- test:unit, test:integration, test:e2e
- db:start, db:stop, db:reset, db:sync-env, supabase
- prepare (husky registration)

Vitest project names (vitest.config.ts test.projects):
- "unit"        → tests/unit/**/*.test.ts
- "integration" → tests/integration/**/*.integration.test.ts

Playwright project name: "chromium" (D-26, Chromium only; webServer runs `pnpm start`).

Environment variables (PRD §20) — MUST be enumerated in .env.example with dummy values:
Phase 1 infrastructure:
  DATABASE_URL, DATABASE_POOL_URL,
  NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY,
  SUPABASE_SERVICE_ROLE_KEY, SUPABASE_PROJECT_REF, SUPABASE_ACCESS_TOKEN,
  IDENTIFICATION_PROVIDER_MODE
Phase 1 observability (CI-only, empty locally):
  NEXT_PUBLIC_SENTRY_DSN, SENTRY_AUTH_TOKEN, SENTRY_ORG, SENTRY_PROJECT,
  NEXT_PUBLIC_POSTHOG_KEY, NEXT_PUBLIC_POSTHOG_HOST
Phase 4+ (placeholders):
  INNGEST_EVENT_KEY, INNGEST_SIGNING_KEY
Phase 12+ (placeholders):
  VERCEL_ORG_ID, VERCEL_PROJECT_ID, VERCEL_TOKEN
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create .nvmrc, extend .gitignore, write package.json with pins + scripts + lint-staged + install dependencies</name>
  <files>.nvmrc, .gitignore, package.json, pnpm-lock.yaml</files>
  <read_first>
    - /Users/machado/Projects/folhario/CLAUDE.md (stack pins)
    - /Users/machado/Projects/folhario/.gitignore (existing — contains only .DS_Store per CONTEXT.md)
    - /Users/machado/Projects/folhario/.env (existing — source of PRD §20 var names; DO NOT MODIFY)
    - /Users/machado/Projects/folhario/.planning/phases/01-foundation/01-PATTERNS.md (package.json section lines 467-502, .gitignore section lines 397-423)
    - /Users/machado/Projects/folhario/.planning/phases/01-foundation/01-RESEARCH.md §Standard Stack (versions table) + §Code Examples (package.json fragment)
  </read_first>
  <action>
Step 1 — Create `.nvmrc` with the single line `22` (or the exact Node 22 LTS patch you have installed locally — run `node -v` and use the exact version like `22.14.0`). Commit as-is; keep it one line.

Step 2 — Extend `.gitignore`. The existing file contains only `.DS_Store`. Preserve that line and APPEND the following block (do not reorder or remove `.DS_Store`):

```
# deps
node_modules/

# next
.next/
out/
next-env.d.ts

# test + coverage
coverage/
playwright-report/
test-results/
.vitest-cache/

# env (NEVER commit .env — real creds live there)
.env
.env.local
.env.*.local

# supabase CLI
supabase/.temp/
supabase/.branches/
```

Do NOT add `.DS_Store` again — it's already there.

Step 3 — Create `package.json` with the exact shape below (fill `@latest patch` placeholders with literal pnpm latest-stable-9.x version at scaffold time, e.g. `pnpm@9.15.5`):

```json
{
  "name": "folhario",
  "private": true,
  "version": "0.0.0",
  "engines": { "node": ">=22 <23" },
  "packageManager": "pnpm@9.15.5",
  "scripts": {
    "prepare": "husky",
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "typecheck": "tsc --noEmit",
    "test:unit": "pnpm exec vitest --run --project=unit",
    "test:integration": "pnpm exec vitest --run --project=integration",
    "test:e2e": "playwright test",
    "db:start": "supabase start",
    "db:stop": "supabase stop",
    "db:reset": "supabase db reset",
    "db:sync-env": "bash scripts/sync-supabase-env.sh",
    "supabase": "supabase"
  },
  "lint-staged": {
    "*.{ts,tsx,js,jsx,mjs}": ["prettier --write", "eslint --fix"],
    "*.{json,md,yml,yaml}": ["prettier --write"]
  }
}
```

Step 4 — Activate Corepack and install dependencies. Run these commands in order:

```bash
corepack enable
corepack prepare pnpm@9.15.5 --activate

pnpm add next@16.2.3 react@19.2.5 react-dom@19.2.5
pnpm add @serwist/next@9.5.7 serwist@9.5.7
pnpm add @sentry/nextjs@10.48.0
pnpm add posthog-js@1.368.0 posthog-node@5.29.7
pnpm add next-intl@4.9.1
pnpm add zod@4

pnpm add -D typescript@6 @types/node@22 @types/react@19 @types/react-dom@19
pnpm add -D eslint@10 eslint-config-next@16.2.3 eslint-config-prettier@10 prettier@3
pnpm add -D vitest@4.1.4 @vitest/ui@4 vite-tsconfig-paths@5
pnpm add -D @playwright/test@1.59.1
pnpm add -D husky@9 lint-staged@16 @commitlint/cli@20 @commitlint/config-conventional@20
pnpm add -D supabase@2.95.0
```

If pnpm errors on any exact version ("no version matching"), fall back to the latest patch within the same major (`next@16`, `vitest@4`) and document the chosen version in the plan SUMMARY. DO NOT skip dependencies or bump majors.

The `pnpm-lock.yaml` will be generated — commit it.

IMPORTANT — do NOT run `pnpm exec husky init` in this task (Task 4 handles Husky setup). Installing `husky` only places the package in `node_modules`; registering hooks is a separate step.
  </action>
  <verify>
    <automated>test -f .nvmrc && grep -q "^22" .nvmrc && test -f package.json && test -f pnpm-lock.yaml && grep -q '"next": "16.2.3"' package.json && grep -q '"packageManager"' package.json && grep -q "node_modules/" .gitignore && grep -q ".env.local" .gitignore && grep -q ".DS_Store" .gitignore && pnpm install --frozen-lockfile</automated>
  </verify>
  <done>
    - `.nvmrc` exists with Node 22 (or exact 22.x patch) on single line
    - `.gitignore` contains all 12 new entries AND preserves `.DS_Store`
    - `package.json` exists with exact name `folhario`, all 14 scripts, lint-staged config, engines/packageManager fields
    - `pnpm-lock.yaml` exists and `pnpm install --frozen-lockfile` passes
    - All 10 runtime + 16 dev dependencies installed at pinned versions
  </done>
</task>

<task type="auto">
  <name>Task 2: Write tsconfig.json (strict + aliases), eslint.config.mjs, .prettierrc, .prettierignore, .commitlintrc.json</name>
  <files>tsconfig.json, eslint.config.mjs, .prettierrc, .prettierignore, .commitlintrc.json</files>
  <read_first>
    - /Users/machado/Projects/folhario/.planning/phases/01-foundation/01-PATTERNS.md §tsconfig.json (lines 505-540, note R-1 @i18n/* alias), §eslint.config.mjs (lines 543-556), §.prettierrc (lines 559-571), §.commitlintrc.json (lines 574-582)
    - /Users/machado/Projects/folhario/.planning/phases/01-foundation/01-CONTEXT.md (D-06 strictness minimum, D-07 path aliases, D-08 no barrels)
  </read_first>
  <action>
Step 1 — Create `tsconfig.json` exactly (includes the `@i18n/*` alias per R-1 reconciliation — this alias is required by Plan 03's `src/proxy.ts`):

```json
{
  "compilerOptions": {
    "strict": true,
    "noUncheckedIndexedAccess": true,
    "target": "ES2022",
    "lib": ["dom", "dom.iterable", "esnext"],
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "jsx": "preserve",
    "allowJs": true,
    "skipLibCheck": true,
    "esModuleInterop": true,
    "forceConsistentCasingInFileNames": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "incremental": true,
    "noEmit": true,
    "plugins": [{ "name": "next" }],
    "paths": {
      "@contexts/*": ["./src/contexts/*"],
      "@shared/*": ["./src/shared/*"],
      "@i18n/*": ["./src/i18n/*"]
    }
  },
  "include": ["next-env.d.ts", "**/*.ts", "**/*.tsx", ".next/types/**/*.ts"],
  "exclude": ["node_modules"]
}
```

Rationale: `noUncheckedIndexedAccess` is the ONLY strict flag added beyond `strict: true` per D-06. Do NOT add `exactOptionalPropertyTypes`, `noPropertyAccessFromIndexSignature`, or `noImplicitOverride` — explicitly rejected in D-06 to reduce friction in early phases.

Step 2 — Create `eslint.config.mjs` (flat config per D-03):

```javascript
import next from "eslint-config-next";
import prettier from "eslint-config-prettier";

export default [
  ...next(),
  prettier,
  {
    ignores: ["node_modules", ".next", "out", "coverage", "playwright-report", "test-results", "public/sw.js"],
  },
];
```

Note the `public/sw.js` ignore — Serwist writes the compiled SW to that path; linting it would fail on generated output.

Step 3 — Create `.prettierrc`:

```json
{
  "semi": true,
  "singleQuote": false,
  "trailingComma": "all",
  "printWidth": 100
}
```

Step 4 — Create `.prettierignore`:

```
node_modules
.next
out
coverage
playwright-report
test-results
public/sw.js
pnpm-lock.yaml
.planning
supabase/.branches
supabase/.temp
```

Step 5 — Create `.commitlintrc.json`:

```json
{ "extends": ["@commitlint/config-conventional"] }
```

No custom rules at Phase 1 — start from the ecosystem default and let conventions emerge.
  </action>
  <verify>
    <automated>test -f tsconfig.json && grep -q "noUncheckedIndexedAccess" tsconfig.json && grep -q "@i18n/\\*" tsconfig.json && grep -q "@contexts/\\*" tsconfig.json && grep -q "@shared/\\*" tsconfig.json && test -f eslint.config.mjs && grep -q "eslint-config-next" eslint.config.mjs && test -f .prettierrc && test -f .prettierignore && test -f .commitlintrc.json && grep -q "config-conventional" .commitlintrc.json && pnpm typecheck</automated>
  </verify>
  <done>
    - `tsconfig.json` has `strict: true`, `noUncheckedIndexedAccess: true`, and all three path aliases (`@contexts/*`, `@shared/*`, `@i18n/*`)
    - `eslint.config.mjs` imports `eslint-config-next` + `eslint-config-prettier` and ignores `public/sw.js`
    - `.prettierrc` has semi=true, singleQuote=false, trailingComma=all, printWidth=100
    - `.prettierignore` excludes Next/test/supabase artifacts
    - `.commitlintrc.json` extends `@commitlint/config-conventional`
    - `pnpm typecheck` exits 0 (no src/ yet = no errors)
  </done>
</task>

<task type="auto">
  <name>Task 3: Write vitest.config.ts (two projects), playwright.config.ts (Chromium only), and .env.example</name>
  <files>vitest.config.ts, playwright.config.ts, .env.example</files>
  <read_first>
    - /Users/machado/Projects/folhario/.planning/phases/01-foundation/01-RESEARCH.md §Code Examples "vitest.config.ts" (lines 1010-1042), "playwright.config.ts" (lines 1159-1191)
    - /Users/machado/Projects/folhario/.planning/phases/01-foundation/01-PATTERNS.md §.env.example (lines 427-462) and §vitest.config.ts / playwright.config.ts (lines 619-640)
    - /Users/machado/Projects/folhario/.env (source of PRD §20 var names — read-only)
    - /Users/machado/Projects/folhario/.planning/phases/01-foundation/01-CONTEXT.md §Specifics (lines 148-157, warning about not overwriting real creds)
  </read_first>
  <action>
Step 1 — Create `vitest.config.ts` with two projects (Vitest 4 `test.projects` — replaces deprecated `workspace` key). The `unit` project does NOT have its own DB; the `integration` project has 30s timeout for DB work (Phase 2 fills it).

```typescript
import { defineConfig } from "vitest/config";
import tsconfigPaths from "vite-tsconfig-paths";

export default defineConfig({
  plugins: [tsconfigPaths()],
  test: {
    projects: [
      {
        extends: true,
        test: {
          name: { label: "unit", color: "cyan" },
          include: ["tests/unit/**/*.test.ts"],
          environment: "node",
          isolate: false,
        },
      },
      {
        extends: true,
        test: {
          name: { label: "integration", color: "green" },
          include: ["tests/integration/**/*.integration.test.ts"],
          environment: "node",
          testTimeout: 30_000,
        },
      },
    ],
  },
});
```

Step 2 — Create `playwright.config.ts` (Chromium-only per D-26; `webServer` runs `pnpm start` — production build, NOT dev):

```typescript
import { defineConfig, devices } from "@playwright/test";

export default defineConfig({
  testDir: "./tests/e2e",
  timeout: 30_000,
  fullyParallel: true,
  forbidOnly: !!process.env.CI,
  retries: process.env.CI ? 2 : 0,
  workers: process.env.CI ? 2 : undefined,
  reporter: process.env.CI ? [["html"], ["github"]] : "list",

  use: {
    baseURL: "http://localhost:3000",
    trace: "on-first-retry",
  },

  projects: [
    { name: "chromium", use: { ...devices["Desktop Chrome"] } },
  ],

  webServer: {
    command: "pnpm start",
    url: "http://localhost:3000/__diag",
    reuseExistingServer: !process.env.CI,
    timeout: 120_000,
    env: {
      IDENTIFICATION_PROVIDER_MODE: "stub",
    },
  },
});
```

Note: `webServer.url: "/__diag"` — the Playwright startup probe hits the diagnostics page (created in Plan 07). Until Plan 07 lands this will fail `pnpm test:e2e` locally but will not fail `pnpm exec playwright --version` or vitest runs.

Step 3 — Create `.env.example` mirroring PRD §20 with dummy values. Group variables by phase of first use per D-30. DO NOT include real credentials — use literal string `DUMMY-CHANGE-ME` or empty strings for optional CI-only vars:

```dotenv
# ──────────────── Phase 1 (infrastructure) ────────────────
DATABASE_URL=postgres://postgres:postgres@localhost:54322/postgres
DATABASE_POOL_URL=postgres://postgres:postgres@localhost:54322/postgres
NEXT_PUBLIC_SUPABASE_URL=http://localhost:54321
NEXT_PUBLIC_SUPABASE_ANON_KEY=DUMMY-CHANGE-ME
SUPABASE_SERVICE_ROLE_KEY=DUMMY-CHANGE-ME
SUPABASE_PROJECT_REF=DUMMY-CHANGE-ME
SUPABASE_ACCESS_TOKEN=DUMMY-CHANGE-ME
IDENTIFICATION_PROVIDER_MODE=stub

# ──────────────── Phase 1 (observability — disabled locally; CI only) ────────────────
# Empty values intentionally. D-19/D-20: Sentry and PostHog disabled locally.
NEXT_PUBLIC_SENTRY_DSN=
SENTRY_AUTH_TOKEN=
SENTRY_ORG=
SENTRY_PROJECT=
NEXT_PUBLIC_POSTHOG_KEY=
NEXT_PUBLIC_POSTHOG_HOST=https://us.i.posthog.com

# ──────────────── Phase 4 (async + email) — placeholders ────────────────
INNGEST_EVENT_KEY=
INNGEST_SIGNING_KEY=

# ──────────────── Phase 12 (deploy) — placeholders ────────────────
VERCEL_ORG_ID=
VERCEL_PROJECT_ID=
VERCEL_TOKEN=
```

DO NOT copy from `.env`. `.env` in this repo already contains real provisioned credentials (per CONTEXT.md §Code Context) — copying from it would commit secrets. Author `.env.example` by hand from the PRD §20 table, matching variable names exactly.

After writing, sanity-check: `grep -E '^(sk_live_|eyJ[A-Za-z0-9])' .env.example` must return NOTHING (no tokens/JWTs).
  </action>
  <verify>
    <automated>test -f vitest.config.ts && grep -q "projects:" vitest.config.ts && grep -q "\"unit\"" vitest.config.ts && grep -q "\"integration\"" vitest.config.ts && test -f playwright.config.ts && grep -q "chromium" playwright.config.ts && grep -q "pnpm start" playwright.config.ts && test -f .env.example && grep -q "^IDENTIFICATION_PROVIDER_MODE=stub" .env.example && grep -q "^DATABASE_URL=" .env.example && grep -q "^NEXT_PUBLIC_SUPABASE_URL=" .env.example && grep -q "^NEXT_PUBLIC_POSTHOG_HOST=" .env.example && grep -q "^INNGEST_EVENT_KEY=" .env.example && grep -q "^VERCEL_TOKEN=" .env.example && ! grep -qE "^(sk_live_|eyJ[A-Za-z0-9])" .env.example && pnpm exec vitest --version && pnpm exec playwright --version</automated>
  </verify>
  <done>
    - `vitest.config.ts` uses `test.projects` (v4 API) with `unit` and `integration` projects plus `tsconfigPaths()` plugin
    - `playwright.config.ts` has single `chromium` project, `webServer.command: "pnpm start"`, `webServer.url: "http://localhost:3000/__diag"`, `IDENTIFICATION_PROVIDER_MODE: stub` in webServer env
    - `.env.example` contains all 18 PRD §20 variables (8 Phase-1 infra, 6 Phase-1 observability, 2 Phase-4, 3 Phase-12) with dummy/empty values, grouped by phase
    - NO real credentials in `.env.example` (no base64-JWT-like strings, no `sk_live_*`, no long random hex)
    - `pnpm exec vitest --version` and `pnpm exec playwright --version` both succeed
  </done>
</task>

<task type="auto">
  <name>Task 4: Initialize Husky hooks (pre-commit, commit-msg) using Husky 9 idioms</name>
  <files>.husky/pre-commit, .husky/commit-msg</files>
  <read_first>
    - /Users/machado/Projects/folhario/.planning/phases/01-foundation/01-RESEARCH.md §Pitfall 4 (Husky 9 `husky install` vs `husky init`)
    - /Users/machado/Projects/folhario/.planning/phases/01-foundation/01-PATTERNS.md §.husky/pre-commit + §.husky/commit-msg (lines 643-657)
    - /Users/machado/Projects/folhario/package.json (from Task 1 — has `prepare: "husky"` script)
  </read_first>
  <action>
Step 1 — Initialize Husky using the v9 idiom (creates `.husky/` directory, installs the git hooks path):

```bash
pnpm exec husky init
```

This auto-creates `.husky/pre-commit` with a sample command. Overwrite it in Step 2.

Do NOT use `pnpm exec husky install` (deprecated v8 API; silently no-ops in Husky 9). Do NOT add `husky install` to scripts — the `prepare: "husky"` one-word script set in Task 1 is the correct v9 form.

Step 2 — Overwrite `.husky/pre-commit` with (single line, no shebang — Husky 9 runs it directly via sh):

```
pnpm lint-staged
```

Step 3 — Create `.husky/commit-msg` (single line, no shebang):

```
pnpm exec commitlint --edit "$1"
```

Step 4 — Ensure both files are executable:

```bash
chmod +x .husky/pre-commit .husky/commit-msg
```

Step 5 — Verify hook installation by attempting a staged bad commit in a throwaway branch:

```bash
git stash push -u -m "phase-01-plan-01-hook-verify" 2>/dev/null || true
echo "test" > /tmp/hook-verify.txt
cp /tmp/hook-verify.txt hook-verify.txt
git add hook-verify.txt
# Commit with a BAD message; the commit-msg hook MUST reject it
if git commit -m "bad message without conventional prefix" 2>&1 | grep -q "subject"; then
  echo "commit-msg hook WORKS"
else
  echo "commit-msg hook FAILED" && exit 1
fi
# Clean up — the commit failed, so just remove the test file
git reset HEAD hook-verify.txt 2>/dev/null || true
rm -f hook-verify.txt
git stash pop 2>/dev/null || true
```

Do NOT leave the test file or test commit in the repo.
  </action>
  <verify>
    <automated>test -f .husky/pre-commit && test -f .husky/commit-msg && test -x .husky/pre-commit && test -x .husky/commit-msg && grep -q "pnpm lint-staged" .husky/pre-commit && grep -q "commitlint --edit" .husky/commit-msg && ! grep -q "husky install" .husky/pre-commit && [ "$(git config core.hooksPath)" = ".husky/_" ] || [ "$(git config core.hooksPath)" = ".husky" ]</automated>
  </verify>
  <done>
    - `.husky/pre-commit` exists, executable, contains `pnpm lint-staged`
    - `.husky/commit-msg` exists, executable, contains `pnpm exec commitlint --edit "$1"`
    - Neither hook file contains `husky install` (deprecated)
    - `git config core.hooksPath` resolves to `.husky/_` or `.husky` (Husky 9 registered the hooks path)
    - Manually attempting a non-conventional commit is rejected (Step 5 self-check)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer laptop → committed repo | Secrets must not cross this boundary |
| CI environment → dependency registry | Lockfile integrity prevents supply-chain drift |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | `.env` / `.env.example` | mitigate | `.gitignore` adds `.env`, `.env.local`, `.env.*.local` (Task 1). `.env.example` hand-authored from PRD §20 table with dummy values only; Task 3 regex check rejects secret-like values. |
| T-01-02 | Tampering | `pnpm-lock.yaml` | mitigate | Task 1 commits `pnpm-lock.yaml`; CI uses `pnpm install --frozen-lockfile` (Plan 08). Dependencies pinned at exact versions in package.json. |
| T-01-03 | Repudiation | Git commit history | mitigate | Husky `commit-msg` hook enforces Conventional Commits (Task 4). Pre-commit runs lint + format to reduce commit-noise drift. |
| T-01-04 | Denial of Service | Node version mismatch | mitigate | `.nvmrc` pins Node 22; `engines.node` enforces at install time; CI uses `setup-node` with `node-version: 22` (Plan 08). |

Threats deferred: CVE monitoring for pinned deps (requires Dependabot/Renovate — Phase 13 or ongoing).
</threat_model>

<verification>
1. `pnpm install --frozen-lockfile` from a clean clone succeeds (no network fallbacks, no version drift).
2. `pnpm typecheck` exits 0 (empty src/ has no errors).
3. `pnpm lint` exits 0 (no sources to lint yet, eslint passes vacuously).
4. `pnpm exec vitest --run` reports 0 tests discovered (no test files in tests/ yet).
5. `pnpm exec playwright --version` prints `1.59.1`.
6. Committing a file with a non-Conventional-Commits message is rejected with a `subject`-mentioning error (commit-msg hook).
7. `.gitignore` excludes `.env`, `.env.local`, `.env.*.local`, `node_modules`, `.next`, `playwright-report`, `test-results`, `coverage`, `.vitest-cache`, `supabase/.temp`, `supabase/.branches`, preserves `.DS_Store`.
8. `grep -c "^[A-Z_]*=" .env.example` returns 18 (18 PRD §20 variable lines).
</verification>

<success_criteria>
- [ ] `.nvmrc`, `package.json`, `pnpm-lock.yaml` committed; `pnpm install --frozen-lockfile` passes from clean clone
- [ ] `tsconfig.json` enforces `strict: true` + `noUncheckedIndexedAccess: true` and exports three path aliases
- [ ] `eslint.config.mjs`, `.prettierrc`, `.prettierignore`, `.commitlintrc.json` present and functional
- [ ] `vitest.config.ts` declares `unit` + `integration` projects via Vitest 4 `test.projects` API
- [ ] `playwright.config.ts` runs Chromium only against `pnpm start` with `IDENTIFICATION_PROVIDER_MODE=stub`
- [ ] `.husky/pre-commit` runs `pnpm lint-staged`; `.husky/commit-msg` runs `pnpm exec commitlint --edit "$1"`
- [ ] `.env.example` mirrors PRD §20 with dummy values only (18 variables, grouped by phase)
- [ ] `.gitignore` extended without losing `.DS_Store`
- [ ] All automated verification commands in each task's `<verify>` block pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md` documenting:
- Exact Node + pnpm patch versions chosen
- Any dependency version fallbacks taken (e.g., if `next@16.2.3` was unavailable and `16.2.4` was used)
- Any tooling quirks encountered (Husky init output, corepack warnings)
- Reference to next plan (01-02)
</output>
