---
phase: 03-design-system-app-shell
verified: 2026-04-27T18:42:00Z
status: human_needed
score: 15/15 must-haves verified (automated); 4 PWA install / first-paint manual checks pending
overrides_applied: 0
human_verification:
  - test: "Install PWA on iOS Safari via 'Add to Home Screen'"
    expected: "Standalone app launches from home screen; theme color (#FBF7EF light / #1A1613 dark) matches the system preference; pt-BR copy renders; Source Serif 4 + Plus Jakarta Sans visible."
    why_human: "iOS PWA install flow cannot be automated in Playwright (no WebKit + Safari install UI). Source: VALIDATION.md §Manual-Only Verifications row 1."
  - test: "Install PWA on Android Chrome via 'Install app'"
    expected: "Standalone app launches from home screen; theme color matches system preference; pt-BR copy renders."
    why_human: "Android install banner is gated by site-engagement heuristics that are not deterministic in test. Source: VALIDATION.md row 2."
  - test: "Lighthouse PWA audit"
    expected: "Score ≥ 0.9 with no manifest errors. Validates manifest icons, display=standalone, scope=/, theme color in <head>."
    why_human: "Lighthouse runs interactively in Chrome DevTools; CI integration deferred. Source: VALIDATION.md row 3."
  - test: "First-paint flash check (no light flash before dark, and vice-versa)"
    expected: "Cold-load with system preference dark renders Night Cream first paint (no Paper Cream flash). Cold-load with folhario_theme=light cookie + system dark renders Paper Cream first paint."
    why_human: "Flash visibility is human-perception bound; subjective threshold. Source: VALIDATION.md row 4."
---

# Phase 3: Design System & App Shell Verification Report

**Phase Goal:** A PWA-installable app shell renders with the full Paper Cream / Night Cream design system, bottom-nav, dark mode, motion, pt-BR strings, and accessibility guardrails — so every later feature phase just drops composed screens into already-finished chrome.

**Verified:** 2026-04-27T18:42:00Z
**Status:** human_needed (all automated truths VERIFIED; PWA install + first-paint flash require human testing)
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (from ROADMAP.md Success Criteria + Plan must_haves)

| #   | Truth                                                                                                                                                                                                                                | Status     | Evidence                                                                                                                                                                                                                                                                       |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| SC-1a | Manifest exists with icons, `display: "standalone"`, theme color #FBF7EF, lang pt-BR; viewport allows user scaling                                                                                                                  | VERIFIED   | `public/manifest.webmanifest:5,7,8,10` declares `"display": "standalone"`, `"theme_color": "#FBF7EF"`, `"lang": "pt-BR"`, 3 icons (192/512 maskable + 180 Apple). `tests/unit/manifest-shape.test.ts` (9 cases) passes. `src/app/layout.tsx:28-32` — viewport export with `width: device-width, initialScale: 1` (no max-scale lock). |
| SC-1b | Paper Cream / Night Cream design system renders; Source Serif 4 headings; Plus Jakarta Sans body; Lucide icons; every string via next-intl with `<html lang="pt-BR">`                                                              | VERIFIED   | `src/app/globals.css:19-43` `@theme` declares all 12 PRD §17 colors + variable fonts. `src/app/layout.tsx:11-21` loads Source_Serif_4 + Plus_Jakarta_Sans via next/font/google. `src/app/layout.tsx:34-46` `<html lang={locale}>` (locale from `getLocale()` returns `pt-BR`). `src/messages/pt-BR.json` populated with 34 leaf keys. Lucide imports per-icon: HomeIcon/BookmarkIcon/CameraIcon/UserIcon in `bottom-nav.tsx:6`, ChevronDownIcon in `select.tsx:4`, etc. |
| SC-2 | Keyboard users can tab through bottom-nav (4 tabs, 28px Lucide + labels always, Canopy active bar, 56px + safe-area, per-tab scroll preservation); 3px Canopy@40% focus ring 2px offset; route changes move focus to main          | VERIFIED   | `src/shared/ui/bottom-nav.tsx:77-129` — 4 ITEMS array (home/catalog/identify/profile), `<Icon size={28} strokeWidth={1.5}>`, label always, Canopy active 3px top bar via translateX, `min-h-[56px]`, `paddingBottom: var(--spacing-safe-bottom)`. `globals.css:97-101` — `:focus-visible { outline: 3px solid color-mix(...canopy 40%...); outline-offset: 2px; border-radius: 8px }`. `app-shell.tsx:46-53` — `useEffect` calls `mainRef.current?.focus()` on segment change (hash-only ignored via prevPathnameRef). E2E specs: `tests/e2e/axe-route-focus-move.spec.ts` (focus moves to `<main>` on `/` → `/catalog`) and `tests/e2e/axe-skip-to-main.spec.ts` (first Tab lands on skip-link). |
| SC-3 | Skeletal shimmer (never circular spinners) with 300ms threshold + 120ms fade-in; static block + 80ms fade under reduced-motion; capture button 1.00→1.03 bounce; 3.2s breathing loop respects reduced-motion (DROPS animate prop)  | VERIFIED   | `src/shared/ui/skeleton.tsx:23-69` — `useReducedMotion()` gates `animate-shimmer` vs `transition-opacity duration-[80ms]`; `SkeletonGroup` returns `null` for first 300ms via `setTimeout(setShow(true), delay=300)`. `globals.css:126-144` — `@keyframes shimmer` + `animate-shimmer` utility with `linear-gradient` background-image (NOT text fill — passes stylelint). `src/shared/ui/capture-button.tsx:30-56` — under reduced-motion, `animateProps = {}` (animate prop entirely OMITTED, not just `{scale: 1}` — UI-20 critical pitfall). `src/shared/motion/springs.ts:15-33` — frozen springs (primary 120/18/1, captureBounce 400/24/0.8, breathing 3.2s repeat Infinity). `tests/unit/breathing-loop.test.tsx` + `skeleton-group.test.tsx` cover both paths. |
| SC-4 | Composed primitives render: empty state (Sage line-art + Source Serif headline + Calm Slate hint + ONE Canopy CTA); inline calm error (cause+recovery+retry, never full-screen); persistent offline banner; safe-area-respecting `min-h-[100dvh]` no horizontal scroll; pt-BR strings; `dd/MM/yyyy`, 24h, R$ 29,90 | VERIFIED   | `src/shared/ui/empty-state.tsx:47-90` — 4 parts in order, single CTA enforced (no `secondaryCta` prop exists; test in `empty-state.test.tsx` asserts `container.querySelectorAll("button, a").length === 1`). `src/shared/ui/inline-error.tsx:23-41` — required `cause: string`, `recovery: string`, `retry: { label, onClick }` props (TS Required<>); `role="alert"`; uses `text-rust` not `text-poppy` (verified `inline-error.test.tsx`). `src/shared/ui/offline-banner.tsx` — consumes `useOnlineStatus()` hook. `src/app/(app)/app-shell.tsx:135-165` — `min-h-[100dvh]`, `max-w-[480px] mx-auto`. `tests/e2e/horizontal-scroll-guard.spec.ts` — asserts `documentElement.scrollWidth ≤ clientWidth + 1` for all 5 routes. `src/shared/i18n/format.ts:16-44` — `formatCurrencyBRL`, `formatDate` (UTC-pinned dd/MM/yyyy), `formatTime` (HH:mm hour12:false), `formatDateTime` composite, all using literal `LOCALE = "pt-BR"`. |
| SC-5a | New SW version → bottom toast "Nova versão disponível" + "Atualizar"; tap triggers skipWaiting + reload; never auto-reloads mid-session                                                                                              | VERIFIED   | `src/app/sw.ts:54-68` — `skipWaiting: false`, `clientsClaim: true`. `src/app/sw.ts:86-92` — `addEventListener("message", ...)` GUARDS on `event.data?.type === "SKIP_WAITING"` before calling `self.skipWaiting()` (T-03-04-01 mitigation). `next.config.ts:11` — `register: false` (React subscribes to waiting BEFORE register fires). `src/shared/ui/app-update-toast.tsx:60-81` — listens for `waiting` → sonner toast with `app.update.label`/`app.update.cta` translations → `messageSkipWaiting()` on click → `controlling` event triggers `reload()`. pt-BR copy in `pt-BR.json:48-53`: `"label": "Nova versão disponível"`, `"cta": "Atualizar"`. `tests/unit/sw-skip-waiting.test.ts` (3 cases) + `tests/unit/app-update-toast.test.tsx` (4 cases) green. |
| SC-5b | Design-system lint confirms absence of emoji, pure black/white, gradient text, glassmorphism/neumorphism, Inter, generic serifs                                                                                                      | VERIFIED   | `eslint.config.mjs:78-101` — `no-restricted-syntax` rules block emoji ranges (`\u{1F300}-\u{1FAFF}`, `\u{2600}-\u{27BF}`) in Literal + JSXText, and ban `Inter` import from `next/font/google`. `stylelint.config.mjs:20-46` — `declaration-property-value-disallowed-list` bans `#000`/`#000000`/`#fff`/`#ffffff`/`black`/`white` (color + bg-color), Inter/Times/Georgia/Garamond/Palatino (font-family), `backdrop-filter: *` (glassmorphism), `background-clip: text` + `-webkit-text-fill-color: transparent` (gradient text). `tools/stylelint-plugins/keyframes-requires-reduced-motion.cjs` — D-08 layer 3 paired-fallback enforcement. `tests/unit/banned-patterns-snapshot.test.ts` walks `src/shared/ui/**/*.tsx` and asserts no banned literals. `pnpm lint:styles` exits 0; `pnpm lint` exits 0 errors. |
| T-1   | Cookie injection guard (T-03-02-01): `setTheme` rejects values outside allowlist `["auto","light","dark"]`; `getTheme` returns `"auto"` fallback for invalid cookie value                                                            | VERIFIED   | `src/shared/theme/use-theme.ts:13-44` — `ALLOWED_VALUES` const + `isThemeValue()` guard. `setTheme` throws on invalid input; `getTheme` returns `"auto"` for `cookieGet().value` not in allowlist. `tests/unit/use-theme.test.ts` (8 cases) covers `setTheme("malicious'; DROP TABLE--")` rejection, `setTheme(undefined)` rejection, invalid cookie value fallback. |
| T-2   | Heartbeat info-disclosure guard (T-03-02-02): `/api/v1/health/connectivity` body shape exactly `{ ok: true, timestamp: <ISO> }`; no `env` field; no Sentry/PostHog/IP logging                                                        | VERIFIED   | `src/app/api/v1/health/connectivity/route.ts:15-20` — returns `NextResponse.json({ ok: true, timestamp: new Date().toISOString() }, { headers: { "Cache-Control": "no-store" } })`. No imports of Sentry/PostHog/serverEnv. `tests/unit/heartbeat-route-contract.test.ts` (5 cases) asserts `Object.keys(body).sort() === ["ok", "timestamp"]`, no POST/PUT/DELETE/PATCH exports. |
| T-3   | Open Risk #4: `getTheme()` cookie read in root `src/app/layout.tsx` ONLY; `(app)/layout.tsx` MUST NOT call it                                                                                                                        | VERIFIED   | `grep "getTheme" src/app/(app)/layout.tsx` → empty. `src/app/layout.tsx:7,37` imports + calls `getTheme()`. NOTE: `src/app/(app)/profile/page.tsx:2,7` also imports/calls `getTheme()` to seed the Select component's initial value — this does NOT set `<html data-theme>` (root layout does); it only reads the value to render the segmented control. Open Risk #4's specific concern (competing read in `(app)/layout.tsx` re-render path) is not violated. |
| T-4   | Heartbeat URL is literal `/api/v1/health/connectivity` (Cross-Cutting Truth #12)                                                                                                                                                     | VERIFIED   | `src/shared/online/use-online-status.ts:24` — `const HEARTBEAT_URL = "/api/v1/health/connectivity" as const`. `tests/unit/use-online-status.test.tsx` enforces literal string match. Route file at `src/app/api/v1/health/connectivity/route.ts` matches exactly. |
| T-5   | Heartbeat backoff: 30s initial, 60s after success, 5s after window-online reconnect; setTimeout-only (NOT setInterval)                                                                                                               | VERIFIED   | `src/shared/online/use-online-status.ts:25-77` — `POLL_INITIAL = 30_000`, `POLL_AFTER_SUCCESS = 60_000`, `POLL_AFTER_RECONNECT = 5_000`; all references are `setTimeout` (no `setInterval` in file). `handleOnline` clears timer + reschedules at `POLL_AFTER_RECONNECT`. `tests/unit/use-online-status.test.tsx` (8 cases) covers all branches. |
| T-6   | SW SKIP_WAITING handler GUARDS `event.data?.type === "SKIP_WAITING"` (T-03-04-01 mitigation)                                                                                                                                         | VERIFIED   | `src/app/sw.ts:86-92` — guard expression literally `if (event.data?.type === "SKIP_WAITING")`. `tests/unit/sw-skip-waiting.test.ts` (3 cases) covers SKIP_WAITING type → invokes; OTHER type → no-op; undefined data → no throw. |
| T-7   | Per-tab scroll restoration: sessionStorage keyed by pathname + useLayoutEffect on tab change RESTORE; hash-only changes IGNORED                                                                                                       | VERIFIED   | `src/shared/ui/bottom-nav.tsx:26-42` — `scrollKey()` strips `#` + `?` from pathname; `saveScroll`/`restoreScroll` use `scrollKey()`. `src/app/(app)/app-shell.tsx:66-95` — `useLayoutEffect([pathname])` reads `restoreScroll(pathname)` + calls `window.scrollTo(0, targetY)`. AppShell's pathname focus effect uses `prevPathnameRef` to ignore hash-only changes. `tests/unit/scroll-restore.test.ts` (5 cases) + `tests/e2e/bottom-nav-scroll-restore.spec.ts` cover save → switch → restore. |
| T-8   | TextInput/Select aria attrs cannot be overridden by caller spread (CR-01 fix; WCAG 2.1 AA contract)                                                                                                                                  | VERIFIED   | `src/shared/ui/text-input.tsx:46-50` — `{...rest}` spread BEFORE `aria-invalid={Boolean(error)}` and `aria-describedby={errorId}`, so computed values WIN. Same in `src/shared/ui/select.tsx:31-35`. CR-01 fix confirmed in commit `5dc6f82` (HEAD). |

**Score:** 15/15 truths VERIFIED via automated checks. 4 manual checks remain for PWA install + first-paint flash (see `human_verification` frontmatter).

### Required Artifacts

| Artifact                                                          | Expected                                                                | Status                | Details                                                                                          |
| ----------------------------------------------------------------- | ----------------------------------------------------------------------- | --------------------- | ------------------------------------------------------------------------------------------------ |
| `src/app/globals.css` (164 lines)                                 | Tailwind v4 entrypoint + Paper Cream / Night Cream tokens               | VERIFIED              | 164 lines (≥80); contains `@import "tailwindcss"`, `@theme`, dual `[data-theme]` blocks, `@keyframes shimmer` + reduced-motion fallback. |
| `src/shared/theme/tokens.ts`                                      | JS mirror of CSS custom properties                                      | VERIFIED              | Exports `tokens.colors.{light,dark}`, `spacing`, `radii`, `focus`. Frozen via `Object.freeze`. Cross-checked vs `globals.css` by `banned-patterns-snapshot.test.ts`. |
| `postcss.config.mjs`                                              | Auto-discovery for `@tailwindcss/postcss`                               | VERIFIED              | 1 line: `export default { plugins: { '@tailwindcss/postcss': {} } };`                            |
| `stylelint.config.mjs` + custom plugin                            | Banned-pattern enforcement + D-08 layer 3 keyframe rule                 | VERIFIED              | All required disallowed lists present (color/bg/font/backdrop-filter/gradient text). Custom plugin at `tools/stylelint-plugins/keyframes-requires-reduced-motion.cjs`. |
| `eslint.config.mjs`                                               | better-tailwindcss + emoji ban + Inter ban                              | VERIFIED              | `eslint-plugin-better-tailwindcss@4.4.1` configured with `entryPoint: "src/app/globals.css"`. Custom emoji + Inter ban via `no-restricted-syntax`. |
| `vitest.config.ts` + `tests/unit/setup-env.ts`                    | jsdom project + matchMedia mock                                         | VERIFIED              | `unit-dom` project added; 447/447 tests green across `unit` + `unit-dom`.                        |
| `playwright.config.ts`                                            | Snapshot defaults + `NEXT_PUBLIC_ENABLE_TEST_ROUTES=1` for /modal-sheet | VERIFIED              | `expect.toHaveScreenshot { animations: "disabled", maxDiffPixels: 100 }`. Env var set at line 67. |
| `src/shared/theme/use-theme.ts`                                   | Server Action setTheme + getTheme with allowlist guard                  | VERIFIED              | 45 lines (≥20). Exports `setTheme`, `getTheme`, `ThemeValue`. `"use server"` directive line 1. No `revalidatePath` import. |
| `src/shared/i18n/format.ts`                                       | pt-BR Intl helpers                                                      | VERIFIED              | 44 lines (≥25). Exports formatCurrencyBRL/Date/Time/DateTime; literal `LOCALE = "pt-BR"`.        |
| `src/shared/typography/scientific-name.tsx`                       | Latin name wrapper `<i lang="la">`                                      | VERIFIED              | 22 lines (≥6). Exports `ScientificName` with `lang="la"`, `font-sans text-sm italic text-slate`. |
| `src/app/api/v1/health/connectivity/route.ts`                     | Public GET heartbeat                                                    | VERIFIED              | 20 lines (≥6). GET-only; body `{ ok: true, timestamp: <ISO> }`; `Cache-Control: no-store`.       |
| `src/messages/pt-BR.json`                                         | ~30-50 nested pt-BR keys                                                | VERIFIED              | 34 leaf keys (≥30 required). All 35 REQUIRED_KEYS in `messages-coverage.test.ts` resolve to non-empty strings. |
| `src/app/layout.tsx`                                              | Root layout with cookie + fonts + dual theme-color + globals.css        | VERIFIED              | Imports `getTheme` from `@shared/theme/use-theme`, `Source_Serif_4` + `Plus_Jakarta_Sans` with `--font-source-serif`/`--font-plus-jakarta`, `<meta name="theme-color">` × 2 (light #FBF7EF, dark #1A1613), `import "./globals.css"`. Stays a server component. |
| `src/shared/motion/springs.ts`                                    | Frozen spring presets                                                   | VERIFIED              | Exports `springs` with `primary`/`captureBounce`/`breathing`. Deep-frozen via nested `Object.freeze`. |
| `src/shared/motion/use-reduced-motion.ts`                         | Re-export of motion/react useReducedMotion                              | VERIFIED              | Single re-export line.                                                                            |
| `src/shared/ui/button.tsx`                                        | 4 variants                                                              | VERIFIED              | primary/secondary/tertiary/destructive variants; 48px tall; 8px radius (rounded-lg).             |
| `src/shared/ui/text-input.tsx`                                    | Floating label + autocomplete-mandatory + 1.5px stroke + validate-on-blur | VERIFIED              | `autoComplete: string` is REQUIRED in props; 1.5px border via `border-[1.5px]`; 48px min-height; CR-01 aria attrs after spread. |
| `src/shared/ui/select.tsx`                                        | Same geometry as TextInput + chevron                                    | VERIFIED              | Same 1.5px/48px geometry; `<ChevronDownIcon size={20}>`; CR-01 aria attrs after spread.          |
| `src/shared/ui/toggle.tsx`                                        | 52x32 track / 28x28 knob; ON/OFF text label                             | VERIFIED              | `w-[52px] h-[32px]` track; `w-[28px] h-[28px]` knob; label always visible (UI-22 redundant cue). |
| `src/shared/ui/modal-sheet.tsx`                                   | Radix Dialog + 24px radius + drag handle + scrim + onCloseAutoFocus    | VERIFIED              | `rounded-t-[24px]`, `h-[4px] w-[36px]` drag handle, `bg-forest/50` scrim, z-50; `onCloseAutoFocus` prop forwarded for non-Trigger callers (commit `c0857f3`). |
| `src/shared/ui/skeleton.tsx`                                      | Skeleton + SkeletonGroup with 300ms gate / 80ms reduced-motion fade     | VERIFIED              | `useReducedMotion()` gates `animate-shimmer` vs `transition-opacity duration-[80ms]`; `setTimeout(..., delay=300)` 300ms gate; cleanup on unmount. |
| `src/shared/ui/empty-state.tsx`                                   | Sage SVG + Source Serif headline + Calm Slate hint + ONE Canopy CTA     | VERIFIED              | `<PlaceholderLeafSvg>` fallback (Open Risk #3); `font-serif text-2xl text-forest` headline; `text-sm text-slate` hint; single CTA (no `secondaryCta` prop). |
| `src/shared/ui/inline-error.tsx`                                  | Required cause+recovery+retry + role=alert + Overdue Rust               | VERIFIED              | `Required<{cause, recovery, retry}>` enforced via TS interface; `role="alert"` on container; `border-rust`/`text-rust` (NOT `text-poppy`). |
| `src/shared/ui/capture-button.tsx`                                | 72px Canopy circle; reduced-motion DROPS animate prop                   | VERIFIED              | `w-[72px] h-[72px]` circle; conditional spread `animateProps = breathing && !reduced ? {...} : {}` — animate prop omitted entirely under reduced-motion. |
| `src/shared/online/use-online-status.ts`                          | Heartbeat hook with 30s/60s/5s backoff                                  | VERIFIED              | All 3 constants used (no dead code); literal HEARTBEAT_URL; setTimeout-only.                     |
| `src/shared/ui/offline-banner.tsx`                                | Banner consuming useOnlineStatus + icon + text                          | VERIFIED              | `useOnlineStatus()` consumed; `<WifiOffIcon>` + label (UI-22 redundant cue); `role="status"`.    |
| `src/shared/ui/read-only-banner.tsx`                              | Prop-driven `<ReadOnlyBanner active={false}>`                           | VERIFIED              | Prop `active: boolean`; returns null when false (Phase 10 wires).                                |
| `src/shared/ui/discard-summary-toast.tsx`                         | Function helper                                                         | VERIFIED              | Exports `showDiscardSummaryToast` invoking sonner `toast()`.                                     |
| `src/shared/ui/app-update-toast.tsx`                              | Subscribes to Serwist waiting → sonner → messageSkipWaiting → reload   | VERIFIED              | OFF-10 contract; injectable `createSource`/`reload` props for tests; defaults wire `@serwist/window`. |
| `src/shared/ui/bottom-nav.tsx`                                    | 4 tabs, 28px Lucide + label, 56px + safe-area, Canopy active 3px       | VERIFIED              | All 4 tabs (`/`, `/catalog`, `/identify`, `/profile`); icons sized 28; min-h-[56px]; padding-bottom safe-area inset; Canopy 3px top bar. |
| `src/app/(app)/app-shell.tsx`                                     | Client orchestrator: main + BottomNav + banners + toast + scroll/focus | VERIFIED              | `<main tabIndex={-1} aria-label>`, BottomNav, OfflineBanner, ReadOnlyBanner, AppUpdateToast, sonner Toaster z-60. |
| `src/app/(app)/layout.tsx`                                        | Server-component layout mounting AppShell                               | VERIFIED              | 19 lines; pure server component; mounts `<AppShell>` only.                                       |
| `src/app/(app)/{,catalog,identify,profile}/page.tsx`              | 4 placeholder pages composed of EmptyState                              | VERIFIED              | All 4 pages exist; `/`, `/catalog`, `/identify` compose `EmptyState` from `@shared/ui/empty-state`. `/profile` mounts ProfileThemeSelect. |
| `src/app/(app)/profile/profile-theme-toggle.tsx`                  | ProfileThemeSelect 3-state segmented control (auto/light/dark)         | VERIFIED              | Renders `<Select>` with 3 options; calls `setTheme(next)` Server Action via `useTransition`.     |
| `src/app/offline/page.tsx` + `offline-fallback.tsx`               | Composed empty-state offline fallback                                   | VERIFIED              | Server page reads translations + delegates to client `OfflineFallback` which renders `<EmptyState>` with `location.reload()` CTA. |
| `src/app/sw.ts`                                                   | User-controlled SW with skipWaiting:false + SKIP_WAITING handler        | VERIFIED              | skipWaiting:false; clientsClaim:true (commit `bfeddbd` — required for offline navigation fallback to fire); NetworkOnly /api/*; NetworkFirst navigations; SKIP_WAITING type guard. |
| `next.config.ts`                                                  | Serwist register: false                                                 | VERIFIED              | Line 11: `register: false`.                                                                       |
| `public/manifest.webmanifest`                                     | Theme #FBF7EF, icons 192/512/maskable + 180 Apple, lang pt-BR           | VERIFIED              | Asserted by `manifest-shape.test.ts` (9 cases pass).                                              |
| `public/icons/manifest-icon-{192,512}.maskable.png`, `apple-icon-180.png` | PWA icon assets                                                 | VERIFIED              | All 3 PNGs present.                                                                               |
| `tests/e2e/visual-snapshots.spec.ts` + 19 baseline PNGs           | 5 routes × 4 combos = 20 snapshots; baselines committed                 | VERIFIED              | 20 baseline PNGs in `tests/e2e/visual-snapshots.spec.ts-snapshots/` (Linux Docker-generated per Open Risk #5). Spec iterates 5 ROUTES × 4 COMBOS. |
| `tests/e2e/horizontal-scroll-guard.spec.ts`                       | UI-21: scrollWidth ≤ clientWidth + 1 across 5 routes                    | VERIFIED              | Spec iterates ROUTES = ['/', '/catalog', '/identify', '/profile', '/offline'].                    |
| `tests/e2e/offline-fallback.spec.ts`                              | Register SW → setOffline(true) → uncached route → /offline renders     | VERIFIED              | HIGH 3 (codex review) sequence implemented; `page.goto("/profile/never-precached-edge")` after setOffline. |
| `tests/e2e/bottom-nav-scroll-restore.spec.ts`                     | UI-14: scroll → tab switch → scroll restored within 1px                 | VERIFIED              | Polls sessionStorage `scroll:/` key for ≥399; replays via window.scrollTo + asserts within 1px tolerance. |
| `tests/e2e/axe-{placeholder-pages,modal-focus-trap,route-focus-move,skip-to-main}.spec.ts` | 4 axe specs                                | VERIFIED              | All 4 spec files present and substantive (real `AxeBuilder({page}).analyze()` calls, real navigation + Tab assertions). |

### Key Link Verification

| From                                              | To                                       | Via                                          | Status   | Details                                                                                                                                              |
| ------------------------------------------------- | ---------------------------------------- | -------------------------------------------- | -------- | ---------------------------------------------------------------------------------------------------------------------------------------------------- |
| `src/app/layout.tsx`                              | `src/shared/theme/use-theme.ts`          | `import { getTheme }` + `data-theme` attr    | WIRED    | `layout.tsx:7,37,46` — imports + awaits + applies `data-theme` conditionally.                                                                        |
| `src/shared/theme/use-theme.ts`                   | `next/headers cookies()`                 | Server Action `setTheme` writes `folhario_theme` | WIRED    | `use-theme.ts:3,26-32` — `cookieStore.set("folhario_theme", ...)`.                                                                                   |
| `src/shared/online/use-online-status.ts`          | `/api/v1/health/connectivity`            | fetch URL literal                            | WIRED    | `use-online-status.ts:24,40` — literal `HEARTBEAT_URL = "/api/v1/health/connectivity"` used in `fetch(HEARTBEAT_URL, ...)`.                          |
| `src/app/(app)/app-shell.tsx`                     | bottom-nav + offline-banner + read-only-banner + app-update-toast | client component composition           | WIRED    | All 4 imports + JSX usage in `app-shell.tsx:8-16,145-159`.                                                                                            |
| `src/app/sw.ts`                                   | `self.skipWaiting()`                     | message event handler with type guard        | WIRED    | `sw.ts:86-92` — addEventListener("message", ...) → guard → `self.skipWaiting()`.                                                                     |
| `src/shared/ui/app-update-toast.tsx`              | `@serwist/window Serwist`                | client SW registration + waiting subscription | WIRED    | `app-update-toast.tsx:5,47-48` — `import { Serwist } from "@serwist/window"`; default factory wires `new Serwist("/sw.js", ...)`.                    |
| `eslint.config.mjs`                               | `src/app/globals.css`                    | `settings.better-tailwindcss.entryPoint`     | WIRED    | `eslint.config.mjs:69` — `entryPoint: "src/app/globals.css"`.                                                                                         |
| `vitest.config.ts`                                | `tests/unit/**/*.test.tsx`               | jsdom environment for component tests        | WIRED    | unit-dom project added; 447/447 pass including all `.test.tsx` files in DOM env.                                                                     |

### Data-Flow Trace (Level 4)

| Artifact                          | Data Variable      | Source                                                              | Produces Real Data | Status   |
| --------------------------------- | ------------------ | ------------------------------------------------------------------- | ------------------ | -------- |
| `app-shell.tsx`                   | `online`           | `useOnlineStatus()` → fetch `/api/v1/health/connectivity` → setOnline(ok) | YES (real route returns `{ok: true, timestamp}`) | FLOWING  |
| `offline-banner.tsx`              | `online`           | `useOnlineStatus()` returns derived boolean                         | YES                | FLOWING  |
| `bottom-nav.tsx` active indicator | `pathname`         | `usePathname()`                                                     | YES                | FLOWING  |
| `app-shell.tsx` scroll restore    | `targetY`          | `restoreScroll(pathname)` reads sessionStorage                      | YES                | FLOWING  |
| `profile-theme-toggle.tsx`        | `value` (initial)  | `getTheme()` reads cookie, seeds Select state                       | YES                | FLOWING  |
| `home/catalog/identify pages`     | translations       | `getTranslations("home.empty")` reads pt-BR.json                    | YES (34 keys populated) | FLOWING  |
| `read-only-banner.tsx`            | `active`           | Prop-driven; ALWAYS `false` in Phase 3 (Phase 10 wires)             | INTENTIONALLY STATIC | FLOWING (by design — banner is meant to render only when subscription state flips, which Phase 3 does not own) |

### Behavioral Spot-Checks

Skipped: Server-side spot-checks would require `pnpm start`. The orchestrator already verified end-to-end via Playwright (64/66 passing; 2 pre-existing diagnostics-consent failures excluded from Phase 3 scope per Plan 05 SCOPE BOUNDARY rule). The 64 passing E2E tests INCLUDE the 4 axe specs, visual snapshots, horizontal-scroll guard, offline-fallback, and bottom-nav scroll-restore — exact behaviors that prove the goal end-to-end.

### Requirements Coverage

| Requirement | Source Plan       | Description (REQUIREMENTS.md)                                                                                                       | Status     | Evidence                                                                                                                                                                       |
| ----------- | ----------------- | ----------------------------------------------------------------------------------------------------------------------------------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| UI-01       | 03-01             | Global design tokens — Paper Cream / Night Cream + Source Serif 4 + Plus Jakarta Sans + Lucide                                       | SATISFIED  | `globals.css` `@theme` block + `tokens.ts` JS mirror + next/font/google in `layout.tsx` + Lucide imports across primitives.                                                    |
| UI-02       | 03-02             | Dark mode via `color-scheme: light dark` following system pref with manual override in Settings; every screen reviewed in both       | SATISFIED  | Hybrid `@custom-variant dark` in globals.css; `setTheme/getTheme` Server Action + cookie; `ProfileThemeSelect` 3-state control; visual snapshots × 4 combos prove both themes. |
| UI-03       | 03-04, 03-05      | Global focus ring 3px Canopy@40% / 2px offset / 8px radius; tab order matches visual order; focus moves to main on route change      | SATISFIED  | `globals.css:97-101` global focus ring; `app-shell.tsx:46-53` focus-on-route-change; `axe-route-focus-move.spec.ts` E2E.                                                       |
| UI-14       | 03-04, 03-05      | Bottom nav 4 items + 28px Lucide + label + Canopy active bar + 56px + safe-area + per-tab scroll preservation                       | SATISFIED  | `bottom-nav.tsx` + `app-shell.tsx` scroll save/restore + `bottom-nav-scroll-restore.spec.ts` + UI-14 unit tests.                                                                |
| UI-17       | 03-03             | Skeletal shimmer (never spinners) + 300ms threshold + 120ms fade-in + 80ms reduced-motion fade                                       | SATISFIED  | `skeleton.tsx` SkeletonGroup gate + `globals.css` shimmer keyframes + reduced-motion paired fallback.                                                                          |
| UI-18       | 03-03             | Empty states — Sage line-art + Source Serif headline + Calm Slate hint + EXACTLY ONE Canopy CTA                                      | SATISFIED  | `empty-state.tsx` single-CTA contract; `empty-state.test.tsx` asserts `.querySelectorAll("button, a").length === 1`. Used on Home/Catalog/Identify/Offline placeholders.       |
| UI-19       | 03-03             | Error states — inline + calm + cause + recovery + retry; never full-screen red wall                                                  | SATISFIED  | `inline-error.tsx` Required<{cause,recovery,retry}> + role=alert + text-rust (not text-poppy); `inline-error.test.tsx`.                                                        |
| UI-20       | 03-03             | Spring-physics motion (120/18/1) + capture button 1.00→1.03 + 3.2s breathing loop; reduced-motion fallbacks for every motion          | SATISFIED  | `springs.ts` frozen presets; `capture-button.tsx` DROPS animate prop under reduced-motion; `breathing-loop.test.tsx` covers both branches.                                     |
| UI-21       | 03-04, 03-05      | Safe areas — `min-h-[100dvh]`, `env(safe-area-inset-*)`, no `h-screen`, no horizontal scroll                                          | SATISFIED  | `app-shell.tsx:136` min-h-[100dvh]; `--spacing-safe-*` tokens in globals.css + bottom-nav padding; `horizontal-scroll-guard.spec.ts` (5 routes).                               |
| UI-22       | 03-04, 03-05      | Color never sole signal; alt text from context; live regions; modal focus trap; decorative illustrations aria-hidden                 | SATISFIED  | OfflineBanner = icon + text (redundant cue); ReadOnlyBanner = lock icon + text; Toggle has ON/OFF label; ModalSheet uses Radix focus trap; `axe-modal-focus-trap.spec.ts`, `axe-skip-to-main.spec.ts`, `axe-placeholder-pages.spec.ts` (20 combos × 0 serious/critical violations). |
| UI-23       | 03-02             | next-intl day-one + `<html lang="pt-BR">` + Intl.* pt-BR for numbers/dates/currency                                                  | SATISFIED  | `format.ts` + `pt-BR.json` (34 keys) + `<html lang={locale}>` in root layout + `messages-coverage.test.ts` gate.                                                               |
| UI-24       | 03-02, 03-04, 03-05 | Persistent banners — offline / read-only / discard summary toast                                                                    | SATISFIED  | OfflineBanner + ReadOnlyBanner + showDiscardSummaryToast helper; pt-BR copy in `pt-BR.json`. `axe-placeholder-pages.spec.ts` W-3 block proves OfflineBanner end-to-end.       |
| UI-25       | 03-01, 03-05      | Design system guardrails — no emoji, pure black/white, gradient text, glassmorphism, Inter, generic serifs, centered hero stacks    | SATISFIED  | Stylelint disallowed-list + ESLint `no-restricted-syntax` + `banned-patterns-snapshot.test.ts` + custom keyframes-requires-reduced-motion plugin.                              |
| OFF-09      | 03-04, 03-05      | Service worker + PWA manifest (installable, display standalone, all icon sizes, theme color matches brand, viewport allows scaling) | SATISFIED (auto) + NEEDS HUMAN | `sw.ts` + `manifest.webmanifest` + `manifest-shape.test.ts` (9 cases) + `offline-fallback.spec.ts`. **Manual install on iOS Safari + Android Chrome + Lighthouse PWA audit listed under `human_verification`.** |
| OFF-10      | 03-04             | App-update toast: new SW waiting → "Nova versão disponível" + "Atualizar" + skipWaiting + reload; honors prefers-reduced-motion; never auto-reloads | SATISFIED  | `app-update-toast.tsx` + `sw.ts` SKIP_WAITING handler + `pt-BR.json` copy + `app-update-toast.test.tsx` (4 cases) + `sw-skip-waiting.test.ts` (3 cases). |

**Orphan check:** REQUIREMENTS.md maps Phase 3 to UI-01,02,03,14,17-25 + OFF-09,10 = 15 IDs. All 15 appear in plan frontmatter `requirements:` arrays (Plans 1-5 collectively). No orphans.

### Anti-Patterns Found

| File                                       | Line | Pattern                                                                            | Severity | Impact                                                         |
| ------------------------------------------ | ---- | ---------------------------------------------------------------------------------- | -------- | -------------------------------------------------------------- |
| `src/app/(app)/app-shell.tsx`              | 108-126 | (WR-01 from 03-REVIEW.md) Scroll-save cleanup re-reads `window.scrollY` after layout-effect overwrites it; `pending` flag does not capture original value | INFO     | Theoretical mid-debounce race could clobber saved Y. E2E `bottom-nav-scroll-restore.spec.ts` works around by manual replay. Reviewer-flagged advisory; not gating Phase 3. |
| `src/shared/online/use-online-status.ts`   | 38-69 | (WR-02) Parallel-timer race when `handleOnline` fires during in-flight ping        | INFO     | Theoretical double heartbeat; unlikely in practice. Advisory.   |
| `src/shared/ui/app-update-toast.tsx`       | 60-81 | (WR-03) No removeEventListener cleanup; effect deps cause re-attach in Strict Mode | INFO     | Could fire toast/reload twice in dev. Advisory.                 |
| `src/shared/ui/empty-state.tsx`            | 20-28 | (WR-07) `ctaHref` and `ctaOnClick` both optional → no-op render if neither provided | INFO     | Caller-error trap. All current callers pass one or the other. Advisory. |
| `src/shared/ui/{text-input,select,toggle}.tsx` | 31, 20, 19 | (WR-08) Auto-generated form IDs collide on duplicate labels                | INFO     | Foot-gun; useId() would be cleaner. Advisory.                   |
| `src/shared/ui/discard-summary-toast.tsx`  | 15-32 | (WR-09) `ctaLabel`/`onClick` independently optional                                | INFO     | Caller-error trap. Advisory.                                    |
| `src/app/(app)/profile/profile-theme-toggle.tsx` | 24-30 | (WR-05) `void setTheme(next)` swallows server-action rejection                | INFO     | Optimistic state can desync if Server Action throws. Advisory.   |
| `src/shared/ui/app-update-toast.tsx`       | 80   | (WR-06) `void source.register()` swallows registration errors                      | INFO     | Silent SW registration failure. Advisory.                       |

All 9 advisory warnings are documented in `03-REVIEW.md` and accepted as not-blocking by the orchestrator brief ("9 warnings remain advisory in 03-REVIEW.md"). The single BLOCKER (CR-01) was fixed inline in commit `5dc6f82`. No new anti-patterns found beyond the review.

### Human Verification Required

#### 1. PWA Install — iOS Safari

**Test:** On an iPhone, navigate Safari to the dev URL → tap Share → tap "Add to Home Screen" → confirm the icon appears on the home screen → tap the icon to launch.
**Expected:** App opens in standalone display (no Safari chrome). Theme color matches Paper Cream `#FBF7EF` (light system) or Night Cream `#1A1613` (dark system). Fonts render Source Serif 4 + Plus Jakarta Sans. All visible text is in pt-BR. Bottom nav has 4 tabs (Início, Catálogo, Identificar, Perfil). The app shell renders without console errors.
**Why human:** iOS Safari install flow cannot be automated (no WebKit/Safari install API in Playwright). Source: `03-VALIDATION.md §Manual-Only Verifications` row 1.

#### 2. PWA Install — Android Chrome

**Test:** On an Android device, navigate Chrome to the dev URL. Wait 30s for the install banner; if it doesn't appear, open the menu and tap "Install app". Launch the installed app from the home screen.
**Expected:** Same as #1 — standalone, theme color, fonts, pt-BR copy, 4-tab bottom nav.
**Why human:** Android install banner triggers off site-engagement heuristics that are not deterministic in test. Source: VALIDATION.md row 2.

#### 3. Lighthouse PWA Audit

**Test:** Open Chrome DevTools → Lighthouse tab → check the PWA category → click "Generate report" against the running app at `localhost:3000` (run via `pnpm start`, NOT `pnpm dev`, since SW is disabled in dev).
**Expected:** PWA score ≥ 0.9 with NO manifest errors. Validates: `display: "standalone"`, `start_url: "/"`, `theme_color`, valid icons array, manifest reachable at `/manifest.webmanifest`, service worker registered.
**Why human:** Lighthouse runs interactively in DevTools; CI integration deferred. Source: VALIDATION.md row 3.

#### 4. First-Paint Flash Check

**Test (a):** Set system preference to Dark mode → close any open browser tabs → cold-load the dev URL. Visually confirm Night Cream renders on first paint (no Paper Cream flash).
**Test (b):** Manually set cookie `folhario_theme=light` (DevTools → Application → Cookies → Add `folhario_theme = light`, path `/`) → set system preference to Dark → cold-load. Visually confirm Paper Cream renders on first paint (no Night Cream flash).
**Expected:** No theme-flip flicker on cold load in either configuration. The hybrid `@custom-variant dark` pattern + cookie-driven `data-theme` attribute should yield flash-free first paint per UI-02 contract.
**Why human:** Flash visibility is human-perception bound; subjective threshold. Source: VALIDATION.md row 4.

### Gaps Summary

No gaps found. All 15 Phase 3 requirements (UI-01,02,03,14,17,18,19,20,21,22,23,24,25 + OFF-09,10) are SATISFIED by code-level evidence. The single code-review BLOCKER (CR-01: `aria-invalid`/`aria-describedby` overridable via spread in TextInput/Select) was fixed inline in commit `5dc6f82`. The 9 remaining code-review WARNINGS are advisory and do not gate Phase 3 closure.

The phase goal text "PWA-installable app shell renders" requires human verification of the PWA install flow on iOS/Android + Lighthouse PWA audit + first-paint flash check, per `03-VALIDATION.md §Manual-Only Verifications`. These are inherent manual checkpoints — not automation gaps. Status `human_needed` reflects this; once the human runs the 4 checks and signs off, Phase 3 is fully complete.

---

*Verified: 2026-04-27T18:42:00Z*
*Verifier: Claude (gsd-verifier)*
