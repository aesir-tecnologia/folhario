---
quick_id: 260509-s0x
status: planned
created: 2026-05-09
---

# Fix Internal Links That Trigger Full Page Reloads

## Goal

Replace internal plain-anchor navigation with Next.js `Link` where the app expects client-side transitions.

## Tasks

1. Update shared empty-state CTAs.
   - Files: `src/shared/ui/empty-state.tsx`
   - Verify: internal `ctaHref` values use `Link`; external URLs still use anchors.

2. Update public auth internal links.
   - Files: `src/contexts/iam/api/components/login-form.tsx`
   - Verify: login secondary links use `Link`.

3. Run static checks.
   - Verify: `pnpm lint`, `pnpm typecheck`
