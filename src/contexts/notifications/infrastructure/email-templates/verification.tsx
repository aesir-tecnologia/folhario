// Phase 4 NOTIF-02: verification email template — Task 1 skeleton (Task 2 GREEN fills in).
import { Html } from "@react-email/components";

import type { VerificationProps } from "@contexts/notifications/domain/events";

export function VerificationEmail(_props: VerificationProps) {
  return <Html lang="pt-BR" />;
}
