---
phase: 06-identification-flow-cost-controls
plan: 14
type: execute
wave: 6
depends_on: ["06-04", "06-10"]
files_modified:
  - src/shared/ui/identification-history-item.tsx
  - src/app/(app)/identify/history/page.tsx
  - src/app/(app)/catalog/[plantId]/identifications/page.tsx
  - src/app/(app)/identify/history/_history-list.tsx
  - src/app/(app)/identify/history/_history-detail-modal.tsx
  - src/app/(app)/catalog/[plantId]/_id-history-section.tsx
  - tests/unit/identification/identification-history-item.unit.test.tsx
autonomous: true
requirements:
  - IDENT-08
  - UI-12
must_haves:
  truths:
    - "D-26: Phase 6 ships both global /identify/history and per-plant /catalog/{plantId}/identifications surfaces over the shared paginated endpoint, including failed attempts and reassociation affordances"
    - "D-27: GET /api/v1/identifications uses cursor pagination by created_at and id descending, default limit 20, optional plantId filter, and 24h signed first-photo thumbnails"
    - "IdentificationHistoryItem composer renders status badge (timeout/provider_unavailable/cap_hit pills with Lucide Clock 18px Overdue Rust + label) + thumbnail (signed URL with fallback) + middle row (success: common name + scientific; failure: 'Falhou: {reason}') + date + provider tag (dev/staging only)"
    - "Re-associate CTA visible ONLY when plantId === null && status === 'success' && selectedResult !== null (CONTEXT D-22); tap opens result-selection sheet pre-loaded"
    - "Per-plant history empty state uses RAW composition (NOT <EmptyState>) per UI-SPEC Conflicts Resolved row 6: Sage line-art + Source Serif 4 24/30 headline + Calm Slate hint, NO Canopy CTA"
    - "Global history empty state uses <EmptyState> composer with 'Identificar planta' CTA → /identify"
    - "Both history pages: cursor-driven pagination via IntersectionObserver sentinel (Phase 5 catalog grid pattern); 6-skeleton initial load; 3-skeleton append-on-next-page"
    - "Per-plant history page wires Phase 5 plant profile placeholder link 'Histórico de Identificação' → /catalog/{plantId}/identifications"
    - "Plant profile section: HISTÓRICO DE IDENTIFICAÇÃO section with 'Ver tudo' link to /catalog/{plantId}/identifications + empty copy 'Esta planta foi adicionada manualmente.' when plant has species_id=null AND zero linked identifications"
  artifacts:
    - path: "src/shared/ui/identification-history-item.tsx"
      provides: "IdentificationHistoryItem composer (used by both history pages)"
      exports: ["IdentificationHistoryItem"]
    - path: "src/app/(app)/identify/history/page.tsx"
      provides: "Global identification history page (server component shell)"
    - path: "src/app/(app)/catalog/[plantId]/identifications/page.tsx"
      provides: "Per-plant identification history page"
    - path: "src/app/(app)/catalog/[plantId]/_id-history-section.tsx"
      provides: "Plant profile section wired to Phase 5 placeholder"
      exports: ["IdHistorySection"]
  key_links:
    - from: "src/app/(app)/identify/history/_history-list.tsx"
      to: "useIdentificationHistory hook (06-04)"
      via: "TanStack Query infinite query"
      pattern: "useIdentificationHistory|useInfiniteQuery"
---

<objective>
Land Phase 6's history surfaces:
1. **`IdentificationHistoryItem`** primitive — Group B last component (UI-SPEC § 9 lines 413-456)
2. **Global history page** at `/identify/history` — used by Identify tab header link (`Ver histórico`)
3. **Per-plant history page** at `/catalog/{plantId}/identifications` — wires Phase 5 placeholder link
4. **Plant profile section** wiring — adds the `HISTÓRICO DE IDENTIFICAÇÃO` section to plant profile (Phase 5 shipped placeholder; Phase 6 shows "Ver tudo" link + empty copy)

Both list pages use the same `useIdentificationHistory` TanStack Query hook from 06-04 (with optional `plantId` filter); pagination via IntersectionObserver sentinel matches Phase 5's catalog grid pattern. Empty states diverge per UI-SPEC § Conflicts Resolved row 6 (per-plant uses raw composition, NOT `<EmptyState>`, because the plant profile is a read-only window).

Output: 1 primitive + 2 pages + 2 client islands + 1 plant profile section + 1 unit-DOM test.
</objective>

<execution_context>
@/Users/machado/Projects/folhario/.claude/get-shit-done/workflows/execute-plan.md
@/Users/machado/Projects/folhario/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-identification-flow-cost-controls/06-UI-SPEC.md
@.planning/phases/06-identification-flow-cost-controls/06-PATTERNS.md
@src/app/(app)/catalog/[plantId]/page.tsx
@src/app/(app)/catalog/_catalog-grid.tsx
@src/contexts/identification/queries/index.ts
@src/contexts/identification/queries/hooks.ts
@src/shared/ui/empty-state.tsx
@messages/pt-BR.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: IdentificationHistoryItem primitive</name>
  <files>src/shared/ui/identification-history-item.tsx, tests/unit/identification/identification-history-item.unit.test.tsx</files>
  <read_first>
    - .planning/phases/06-identification-flow-cost-controls/06-UI-SPEC.md (§ IdentificationHistoryItem lines 413-456 — verbatim composition + status badge map)
    - src/shared/ui/scientific-name.tsx (composed inside)
    - src/app/(app)/catalog/_plant-card.tsx (closest analog for list-item composer)
  </read_first>
  <behavior>
    - Test 1: status='success' + selectedResult set + plantId set → renders common name + scientific name + date + plant_name (joined); NO status badge
    - Test 2: status='success' + plantId === null + selectedResult set → renders Calm Slate dot ambient signal + "Associar à planta" Re-associate CTA
    - Test 3: status='timeout' → status badge pill with Lucide Clock 18px + "Tempo esgotado"
    - Test 4: status='success' + selectedResult === null → "Sugestão: {topResult.commonName}" prefix
    - Test 5: thumbnailSignedUrl null OR <img> onError → fallback Hairline Beige bg + Lucide Image 24px
    - Test 6 (axe): all branches axe-clean
  </behavior>
  <action>
    Implement per UI-SPEC §9. The component takes `{item: IdentificationHistoryItemSnakeCase, onReassociate?: (item) => void}` props. Composition:
    - Top row: thumbnail + status badge (right)
    - Middle row: success-with-selection / success-without-selection / failure variants
    - Bottom row: date (Intl.DateTimeFormat pt-BR) + provider tag (gated by `process.env.NODE_ENV !== 'production'` OR a `showProviderTag` prop, default false)
    - Re-associate button (conditional)

    `"use client"` because of onClick handler for re-associate.

    Provider tag visibility: per UI-SPEC line 432 "production users do NOT see provider names". Wire as conditional rendering on a `showProviderTag` prop OR check `process.env.NODE_ENV !== 'production'`. Recommend the prop approach (clearer test seam); the page server component reads `process.env.NODE_ENV` and passes the boolean.

    RED → GREEN.
  </action>
  <verify>
    <automated>pnpm exec vitest run --project=unit-dom tests/unit/identification/identification-history-item.unit.test.tsx 2>&1 | tee /tmp/06-14-task1.log | grep -E "Tests +[0-9]+ passed" && grep -c "Lucide Clock\|Clock\|status='timeout'\|Tempo esgotado" src/shared/ui/identification-history-item.tsx | awk '$1 < 2 { exit 1 }'</automated>
  </verify>
  <acceptance_criteria>
    - 6 tests pass including axe assertion
    - Component supports all 4 status variants (success, timeout, provider_unavailable, cap_hit)
    - Re-associate CTA gated by D-22 condition (plantId null + status success + selectedResult set)
  </acceptance_criteria>
  <done>Last UI primitive shipped; identification namespace covered.</done>
</task>

<task type="auto">
  <name>Task 2: Global + per-plant history pages</name>
  <files>src/app/(app)/identify/history/page.tsx, src/app/(app)/identify/history/_history-list.tsx, src/app/(app)/identify/history/_history-detail-modal.tsx, src/app/(app)/catalog/[plantId]/identifications/page.tsx</files>
  <read_first>
    - src/app/(app)/catalog/[plantId]/page.tsx (server-component shell pattern with HydrationBoundary)
    - src/app/(app)/catalog/_catalog-grid.tsx (cursor + IntersectionObserver pattern)
    - .planning/phases/06-identification-flow-cost-controls/06-UI-SPEC.md (§ Pages lines 461-519 — both history pages composition + empty states)
    - src/contexts/identification/queries/index.ts + hooks.ts (06-04)
    - src/shared/ui/empty-state.tsx
  </read_first>
  <action>
    1. **`/identify/history/page.tsx`** — server component shell:
       - Auth check + prefetch first page of `listIdentifications` into TanStack Query cache
       - Render top bar (Back chevron + title "Histórico de identificações") + `<HistoryList plantId={undefined} />` client island
    2. **`/catalog/[plantId]/identifications/page.tsx`** — same pattern with `plantId` param:
       - Auth + plant ownership check (call existing getPlant or getPlantForUser)
       - Prefetch first page with the plantId filter
       - Render title `Histórico de identificações — {plantName}` + `<HistoryList plantId={plantId} />`
       - Empty state: RAW composition (NOT EmptyState) per UI-SPEC Conflicts Resolved row 6 — Sage line-art + Source Serif 4 24/30 weight 500 headline `Esta planta ainda não tem identificações no histórico.` + Calm Slate hint `As identificações associadas a esta planta aparecem aqui.`. NO primary CTA.
    3. **`_history-list.tsx`** — client component (`"use client"`):
       - Uses `useInfiniteQuery` from TanStack with `identificationKeys.history({plantId, cursor})` — verify the factory in 06-04 supports infinite (it returns a queryKey + queryFn shape — wrap with `useInfiniteQuery({getNextPageParam: (last) => last.next_cursor})` in the hook).
       - 6 skeleton cards on initial load; 3 appended at bottom on next-page load.
       - IntersectionObserver sentinel at the bottom (Phase 5 catalog grid pattern).
       - Empty state for `/identify/history` (when plantId undefined): use Phase 3 `<EmptyState>` composer — title `Você ainda não identificou nenhuma planta.` + hint `Use o botão de identificar para começar.` + primary CTA `Identificar planta` → `/identify`.
       - Whole-card tap opens `<HistoryDetailModal item={item} onClose={...} onReassociate={...} />`.
       - Re-associate button (when D-22 condition) opens the result-selection sheet from 06-13 — but the result-selection sheet is currently INSIDE `_identify-flow.tsx`. Two options:
         - (a) Lift `_result-sheet.tsx` to a more shared location (e.g. `src/app/(app)/identify/_result-sheet.tsx` is fine — both pages are inside `(app)/`, just import directly).
         - (b) Build a smaller re-associate flow specific to history.
         Pick (a) — cleaner. The history list imports `_result-sheet` directly. If route boundaries are an issue at runtime, ship a thin wrapper in `_history-list.tsx`.
    4. **`_history-detail-modal.tsx`** — Radix Dialog showing the full Identification record (per UI-SPEC line 449 "Phase 6 ships the modal as the MVP; future: detail page"). Renders all fields snake_case-formatted; close on scrim/escape.
    5. **`useInfiniteQuery` wiring**: extend `src/contexts/identification/queries/hooks.ts` (created in 06-04) to add a `useIdentificationHistoryInfinite(params)` hook that wraps `useInfiniteQuery`. The factory's `history()` entry already returns the queryKey shape; just extend the factory to add a new entry that returns `{getNextPageParam}` config. Plan: add `historyInfinite(params)` next to `history(params)` in the factory.
  </action>
  <verify>
    <automated>test -f src/app/\(app\)/identify/history/page.tsx && test -f src/app/\(app\)/catalog/\[plantId\]/identifications/page.tsx && pnpm exec tsc --noEmit 2>&1 | (grep -E "(identify/history|catalog/\[plantId\]/identifications)" && exit 1 || exit 0) && grep -c "useInfiniteQuery\|getNextPageParam" src/app/\(app\)/identify/history/_history-list.tsx | awk '$1 < 1 { exit 1 }' && grep -c "EmptyState\|empty-state" src/app/\(app\)/catalog/\[plantId\]/identifications/page.tsx | awk '$1 < 0 { exit 0 }'</automated>
  </verify>
  <acceptance_criteria>
    - Both pages compile + render
    - Per-plant page does NOT use `<EmptyState>` (raw composition only — UI-SPEC Conflicts Resolved row 6)
    - Global page DOES use `<EmptyState>` (with Canopy CTA "Identificar planta")
    - `useInfiniteQuery` is wired in `_history-list.tsx`
    - `tsc --noEmit` clean
  </acceptance_criteria>
  <done>Both history pages live; cursor pagination wired; per-plant empty-state divergence respected.</done>
</task>

<task type="auto">
  <name>Task 3: Plant profile ID-history section wiring</name>
  <files>src/app/(app)/catalog/[plantId]/_id-history-section.tsx, src/app/(app)/catalog/[plantId]/page.tsx (or _plant-profile.tsx — find the existing component that renders the plant profile sections)</files>
  <read_first>
    - src/app/(app)/catalog/[plantId]/page.tsx (find the existing plant profile composition; Phase 5 shipped a placeholder section "Histórico de Identificação" — find the placeholder and replace)
    - .planning/phases/06-identification-flow-cost-controls/06-UI-SPEC.md (§ Catalog Profile Extension lines 681-689)
  </read_first>
  <action>
    1. Find the Phase 5 plant profile placeholder for ID-history (search for `Histórico de Identificação` or `idHistory` in `src/app/(app)/catalog/[plantId]/`).
    2. Replace placeholder with `<IdHistorySection plantId={plantId} hasSpecies={plant.species_id !== null} />`. The section:
       - Header: `HISTÓRICO DE IDENTIFICAÇÃO` Plus Jakarta Sans 14 weight 600 UPPERCASE +4% tracking
       - When the count of identifications linked to this plantId > 0: top 3 history items as compact cards + "Ver tudo" tertiary text link → `/catalog/{plantId}/identifications`
       - When the count === 0 AND `species_id === null`: empty copy `Esta planta foi adicionada manualmente.` (UI-SPEC line 689)
       - When count === 0 AND `species_id` is set (rare; identification confirmation succeeded but the row was deleted): show "Ver tudo" link + the empty hint text only
    3. Component is `"use client"` if it uses TanStack Query directly, OR a server component if the parent page prefetches the count and passes it as a prop. Recommend the latter — keeps the section composable.
    4. NO unit test for this section — visual + structural coverage in 06-15 E2E.
  </action>
  <verify>
    <automated>test -f src/app/\(app\)/catalog/\[plantId\]/_id-history-section.tsx && grep -c "HISTÓRICO DE IDENTIFICAÇÃO\|Ver tudo\|adicionada manualmente" src/app/\(app\)/catalog/\[plantId\]/_id-history-section.tsx | awk '$1 < 2 { exit 1 }' && pnpm exec tsc --noEmit 2>&1 | (grep -E "_id-history-section" && exit 1 || exit 0)</automated>
  </verify>
  <acceptance_criteria>
    - Section component compiles
    - Phase 5 placeholder text is replaced (the previous placeholder section's text is no longer in the plant profile component, OR is replaced by `<IdHistorySection>`)
    - Empty copy "Esta planta foi adicionada manualmente." is in the section file
  </acceptance_criteria>
  <done>Plant profile shows ID-history section; Phase 5 placeholder superseded.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server prefetch → HydrationBoundary | TanStack Query state crosses server→client; user-scoped via auth check before prefetch |
| _history-list.tsx → /api/v1/identifications GET | Client fetch; cursor opaque, server enforces user_id filter via RLS |
| Re-associate flow → /api/v1/identifications/{id}/confirm | JSON POST with Idempotency-Key |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-14-01 | I (Information Disclosure) | Cross-user history leak | mitigate | listIdentifications filters by user_id (06-09); RLS as defense in depth (Phase 2) |
| T-06-14-02 | I | Provider tag leaks to production users | mitigate | provider tag conditionally rendered via `showProviderTag` prop, default false; production page passes false (UI-SPEC line 432) |
| T-06-14-03 | T | Cursor manipulation to skip pages | accept | Cursor is opaque base64-encoded `{createdAt, id}`; corrupted cursor → 400 validation_failed; can only skip BACKWARD (in time order) which is an information-disclosure non-issue (you'd skip your own data) |
| T-06-14-04 | E (Elevation) | Re-associate to another user's plant | mitigate | confirmIdentification verifies identification ownership and only allows confirming a persisted selected result whose local `speciesId` matched during identify; `createPlant` creates a new plant for the authenticated user |

</threat_model>

<verification>
- 6 unit tests for IdentificationHistoryItem pass (including axe)
- All four pages/components compile
- Per-plant page raw-empty-state respected (no EmptyState import in that file)
- Global page does use EmptyState
- `tsc --noEmit` clean
</verification>

<success_criteria>
- IDENT-08 (history list with status + re-associate) covered by IdentificationHistoryItem unit tests + UI-SPEC pages
- Per-plant ID history wiring (UI-12 sub-deliverable) covered by Task 3
- UI-12 (history screens) shipped — global + per-plant + plant-profile section
- Phase 5 plant profile placeholder retired
</success_criteria>

<output>
After completion, create `.planning/phases/06-identification-flow-cost-controls/06-14-SUMMARY.md`. SUMMARY MUST note:
1. Last UI primitive shipped (IdentificationHistoryItem)
2. Two history pages + plant-profile section
3. Per-plant empty state uses raw composition (NOT EmptyState) per Conflicts Resolved row 6
4. Re-associate flow shares `_result-sheet.tsx` with the identify page (no duplication)
5. Reminder for 06-15: E2E specs include identify-history.spec.ts + identify-states.spec.ts (52 visual baselines × 4 combos)
</output>
