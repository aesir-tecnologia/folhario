# Phase 5: Catalog — Meu Jardim - Pattern Map

**Mapped:** 2026-04-29
**Files analyzed:** 37 new/modified files
**Analogs found:** 28 / 37 (9 no-analog — noted below)

---

## Flat Pattern Table

| New / Modified File | Role / Data Flow | Closest Analog (path:line) | Excerpt (≤6 lines) | Notes for Planner |
|---------------------|-----------------|----------------------------|--------------------|-------------------|
| `src/contexts/catalog/infrastructure/db/schema.ts` | model / CRUD (extend) | `src/contexts/catalog/infrastructure/db/schema.ts:1-64` | `import { pgTable, uuid, varchar, text, timestamp, index } from "drizzle-orm/pg-core"`; `export const plants = pgTable("plants", { id: uuid("id").primaryKey().defaultRandom(), userId: uuid("user_id").notNull().references(() => users.id, { onDelete: "cascade" }), ... }, (t) => [index("plants_user_id_idx").on(t.userId)])` | Add `pgEnum("pending_deletion_status", ["pending","in_progress","completed","failed"])` + two new tables (`pending_storage_deletions`, `location_suggestions`). Composite PK on `location_suggestions(user_id, label_normalized)` per D-09. Copy index/FK pattern from existing tables. |
| `src/contexts/catalog/infrastructure/db/plants.ts` | repository / CRUD (extend) | `src/contexts/catalog/infrastructure/db/plants.ts:1-31` | `import { and, eq } from "drizzle-orm"; import { type DbClient } from "@shared/db/client"; import { type TransactionalDb } from "@shared/db/unit-of-work"; type PlantsDb = DbClient \| TransactionalDb; export async function findByIdForUser(db: PlantsDb, userId: string, plantId: string): Promise<PlantRow \| null> { const rows = await db.select().from(plants).where(and(eq(plants.userId, userId), eq(plants.id, plantId))).limit(1); return rows[0] ?? null; }` | Add `list`, `create`, `update`, `delete`, `countForUser`, `getCascadeCounts`. All functions accept `PlantsDb = DbClient \| TransactionalDb`. Ownership filter `WHERE user_id = $1` mandatory on every query (D-20/T-02-12). |
| `src/contexts/catalog/infrastructure/db/photo-entries.ts` | repository / CRUD (extend) | `src/contexts/catalog/infrastructure/db/photo-entries.ts:1-32` | `type PhotoEntriesDb = DbClient \| TransactionalDb; export type PhotoEntryInsert = typeof photoEntries.$inferInsert; export async function create(db: PhotoEntriesDb, input: PhotoEntryInsert): Promise<PhotoEntryRow> { const [row] = await db.insert(photoEntries).values(input).returning(); if (!row) throw new Error("photoEntries.create: insert returned no row"); return row; }` | Add `list({plantId})`, `delete`, `bumpCoverFor(plantId)` (D-03). Same `DbClient \| TransactionalDb` union type. `bumpCoverFor` runs inside the same TX as delete — always pass `tx`, not bare `db`. |
| `src/contexts/catalog/infrastructure/db/pending-storage-deletions.ts` | repository / CRUD | `src/contexts/catalog/infrastructure/db/photo-entries.ts:1-32` | (same shape as photo-entries.ts — insert returning, typed inferred types) | NEW. No existing analog for this exact table. Copy `DbClient \| TransactionalDb` union, typed `$inferSelect`/`$inferInsert`, `create(db, input)` returning row. Add `listPending({limit, before})` for reconciler cursor with `FOR UPDATE SKIP LOCKED` raw SQL. `updateStatus(db, id, status, errorMsg?)` for Inngest worker. |
| `src/contexts/catalog/infrastructure/db/location-suggestions.ts` | repository / CRUD | `src/contexts/catalog/infrastructure/db/photo-entries.ts:1-32` | (same shape) | NEW. Upsert on `(user_id, label_normalized)` conflict: `INSERT ... ON CONFLICT (user_id, label_normalized) DO UPDATE SET usage_count = usage_count + 1, last_used_at = now()`. `listForUser(db, userId, limit: 20)` returns `label_display[]` ordered by `usage_count DESC, last_used_at DESC`. |
| `src/contexts/catalog/infrastructure/photo-storage.ts` | utility / file-I/O (extend) | `src/contexts/catalog/infrastructure/photo-storage.ts:1-215` | `export function __setStorageAdapterForTests(adapter: StorageAdapter \| null): void { cachedAdapter = adapter; ... }` + `export async function deleteAllPlantMediaForUser(userId: string): Promise<void> { const adapter = getAdapter(); await adapter.deletePrefix({ bucket: PLANT_PHOTOS_BUCKET, prefix: \`${userId}/\` }); ... }` | Add `deleteAllPlantMediaByPrefix({userId, plantId})` for per-plant deletion (D-22). Add `signCatalogPhotoUrl({key, ttl: 24*3600})` for 24h signed reads (D-20). Mirror `deleteSinglePlantPhotoBestEffort` error-swallow + Sentry pattern for the new helpers. |
| `src/contexts/catalog/application/create-plant.ts` | use-case / CRUD | `src/contexts/catalog/application/upload-photo.ts:1-228` | `export async function uploadPhoto(input: UploadPhotoInput): Promise<UploadPhotoResult> { ... photoEntry = await withUnitOfWork(input.userId, async (tx) => photoEntriesRepo.create(tx, {...})); } catch (err) { await deleteSinglePlantPhotoBestEffort({...}); throw err; }` | NEW. Same compensating-delete pattern: upload bytes THEN `withUnitOfWork` insert of both Plant + PhotoEntry in one TX; catch + `deleteSinglePlantPhotoBestEffort` on UoW failure. Emit `plant.created` event post-commit. Capture PostHog `plant_added` AFTER TX commit (D-28). |
| `src/contexts/catalog/application/update-plant.ts` | use-case / CRUD | `src/contexts/catalog/application/upload-photo.ts:93-131` | `export async function uploadPhoto(input): Promise<UploadPhotoResult> { if (!isAllowedMime(input.contentType)) { return { ok: false, code: ErrorCode.ValidationFailed, reason: "..." }; } const plantRow = await plantsRepo.findByIdForUser(defaultDb, input.userId, input.plantId); if (!plantRow) { return { ok: false, code: ErrorCode.NotFound, reason: "plant not found" }; }` | NEW. Ownership check (`findByIdForUser`) OUTSIDE UoW. Discriminated-union result shape (`{ ok: true } \| { ok: false, code, reason }`). Upsert location suggestion inside same UoW. Fire PostHog `plant_edited` after commit. |
| `src/contexts/catalog/application/delete-plant.ts` | use-case / CRUD | `src/contexts/catalog/application/upload-photo.ts:200-228` | `photoEntry = await withUnitOfWork(input.userId, async (tx) => { ... INSERT photo_entries ... }); } catch (err) { await deleteSinglePlantPhotoBestEffort({...}); throw err; }` | NEW. No existing `delete-*` use case in codebase. Use `withUnitOfWork` TX: cascade deletes from photo_entries + reminders (via FK cascade), SET identifications.plant_id = NULL, INSERT two `pending_storage_deletions` rows, then COMMIT. After commit: emit `plant.deleted` Inngest event. PostHog `plant_deleted` after event emit. Ownership check outside UoW first. |
| `src/contexts/catalog/application/list-plants.ts` | use-case / CRUD | `src/shared/api/cursor.ts:24-108` | `export const DEFAULT_LIMIT = 50; export const MAX_LIMIT = 200; export function encodeCursor(payload: CursorPayload): string { return Buffer.from(JSON.stringify(payload), "utf8").toString("base64url"); } export function decodeCursor(encoded: string): DecodeResult { ... const result = cursorPayloadSchema.safeParse(parsed); if (!result.success) { return { ok: false, error: ErrorCode.ValidationFailed }; } return { ok: true, value: result.data }; }` | NEW. Use `decodeCursor`/`encodeCursor` from `@shared/api/cursor`. Extend cursor payload with `{sort_id, last_value, last_id}` (D-12). Use `normalizeLimit`. Returns `{plants, nextCursor, totalCount?}`. |
| `src/contexts/catalog/application/get-plant.ts` | use-case / request-response | `src/contexts/catalog/infrastructure/db/plants.ts:20-31` | `export async function findByIdForUser(db: PlantsDb, userId: string, plantId: string): Promise<PlantRow \| null>` | NEW. Ownership check via `plantsRepo.findByIdForUser`. Returns plant + `_meta: { photo_entry_count, reminder_count }` for D-07 delete-confirm cascade counts. Discriminated-union result. |
| `src/contexts/catalog/application/list-photo-entries.ts` | use-case / CRUD | `src/contexts/catalog/infrastructure/db/photo-entries.ts:1-32` | (same repo pattern; returns typed array) | NEW. Verifies plant ownership first (`plantsRepo.findByIdForUser`). Returns signed thumbnail URLs (24h TTL per D-20) by calling `photo-storage.signCatalogPhotoUrl`. |
| `src/contexts/catalog/application/create-photo-entry.ts` | use-case / file-I/O | `src/contexts/catalog/application/upload-photo.ts:1-228` | (full upload-photo pattern — same steps 1-7) | NEW. Delegates to upload-photo flow (or extracts common logic). Ownership check, MIME/GPS/size validation, thumbnail, UoW insert, compensating delete on failure. |
| `src/contexts/catalog/application/delete-photo-entry.ts` | use-case / CRUD | `src/contexts/catalog/application/upload-photo.ts:200-228` | `} catch (err) { await deleteSinglePlantPhotoBestEffort({...}); throw err; }` | NEW. Ownership check, delete photo_entries row in UoW, call `bumpCoverFor(plantId)` in same TX (D-03 auto-promote), schedule storage deletion via `pending_storage_deletions` INSERT. No event emit required (within-plant action). |
| `src/contexts/catalog/application/list-locations.ts` | use-case / CRUD | `src/contexts/catalog/infrastructure/db/photo-entries.ts:23-32` | (simple read use-case pattern) | NEW. Merges `locationSuggestionsRepo.listForUser(db, userId, 20)` with i18n defaults from `messages/pt-BR.json` (`catalog.locations.defaults`). De-dupe by normalized label. Returns `{ label_display: string }[]`. |
| `src/contexts/catalog/inngest/functions.ts` | event-handler + cron / event-driven | `src/contexts/notifications/inngest/functions.ts:14-53` | `const notificationsSendEmail = inngest.createFunction({ id: "notifications-send-email", retries: 3, triggers: [{ event: "notifications/email.requested" }] }, async ({ event, step }) => { const data = event.data as ...; const rendered = await step.run("render-email", async () => renderEmail(...)); const result = await step.run("send-resend", async () => resendAdapter.send({...})); return { messageId: result.id }; })` | NEW. `cleanupStorage` event handler: `triggers: [{ event: "plant.deleted" }]`, reads `pending_storage_deletions` IDs from event data, calls `storageAdapter.deletePrefix`, updates row status. `cleanupStorageReconciler` cron: `triggers: [{ cron: "0 * * * *" }]`, batch-50 `FOR UPDATE SKIP LOCKED` query, exponential backoff via `step.sleep`. Export as `catalogFunctions` array. Register in `src/shared/inngest/registry.ts`. No real cron analog exists — `billingTrialEndingNotifier` at `src/contexts/billing/inngest/functions.ts:11-14` is a stub cron; use notifications function shape for real event handler. |
| `src/contexts/catalog/queries/index.ts` | utility / request-response | `src/app/posthog-provider.tsx` (nearest client-only module shape) | `"use client"; import { useEffect, type ReactNode } from "react"; ...` | NEW. No TanStack Query factory exists in codebase. Use `"use client"` directive. Follow Tkdodo query-factory pattern: `export const plantsKeys = { lists: (sort: string) => ({ queryKey: ["catalog","plants","list", sort], queryFn: ..., staleTime: 30_000 }), detail: (id: string) => ({ queryKey: ["catalog","plant", id], queryFn: ..., staleTime: 60_000 }), ... }`. RESEARCH.md is authoritative for TanStack v5 API. |
| `src/shared/ui/query-provider.tsx` | provider / request-response | `src/app/posthog-provider.tsx:1-12` | `"use client"; import { useEffect, type ReactNode } from "react"; import { initPostHog } from "@shared/telemetry/posthog-client"; export function PostHogProvider({ children }: { children: ReactNode }) { useEffect(() => { initPostHog(); }, []); return <>{children}</>; }` | NEW. No TanStack Query provider exists. Mount point: wrap children in `(app)/layout.tsx` after auth gate (per D-16). Copy `"use client"` + `ReactNode` pattern. Wrap `QueryClientProvider` + `PersistQueryClientProvider` from `@tanstack/react-query-persist-client`. IDB persister via `idb-keyval` + `createAsyncStoragePersister`. |
| `src/contexts/billing/application/use-subscription.ts` | hook / request-response | `src/shared/online/use-online-status.ts` (nearest stub hook pattern) | (hook returning boolean state) | NEW stub. Returns `{ active: true, readOnly: false }` constant. No real implementation in Phase 5. Follow hook file conventions from `use-online-status.ts`. |
| `src/shared/ui/storage-budget-guard.ts` | utility / event-driven | `src/shared/api/cursor.ts:94-108` (normalizeLimit style) | `export function normalizeLimit(input: string \| number \| null \| undefined): number { if (input === undefined \| \| input === null \| \| input === "") return DEFAULT_LIMIT; const parsed = ...; }` | NEW. No analog for `navigator.storage.estimate()`. Pure utility (no JSX). Export `checkStorageBudget(): Promise<{ shouldEvict: boolean; usageRatio: number }>`. Returns `shouldEvict: true` when `used/quota > 0.8`. |
| `src/shared/ui/combobox.tsx` | component / request-response | NO ANALOG | — | NEW (D-08). No headless Combobox primitive exists. Closest shape reference: `src/shared/ui/modal-sheet.tsx` for `"use client"`, Radix-free `useId`/`aria-*` prop convention, and `src/shared/ui/select.tsx` for option rendering. RESEARCH.md WAI-ARIA APG 1.2 patterns are authoritative. |
| `src/shared/ui/lightbox.tsx` | component / event-driven | `src/shared/ui/modal-sheet.tsx:1-81` | `"use client"; import * as Dialog from "@radix-ui/react-dialog"; import { XIcon } from "lucide-react"; import type { ReactNode } from "react"; export function ModalSheet({ open, onOpenChange, title, closeLabel, children, onCloseAutoFocus }: ModalSheetProps) { return (<Dialog.Root open={open} onOpenChange={onOpenChange}><Dialog.Portal><Dialog.Overlay .../><Dialog.Content onCloseAutoFocus={onCloseAutoFocus} ...>` | NEW (D-14). Use Radix Dialog for focus trap + Esc dismiss (same as ModalSheet). Add full-screen positioning class, horizontal swipe via `onTouchStart`/`onTouchEnd`, `transform`+`opacity` animation only. `prefers-reduced-motion` fallback via `useReducedMotion` (already used in `src/shared/ui/skeleton.tsx:24`). |
| `src/shared/ui/inline-edit-field.tsx` | component / request-response | `src/shared/ui/text-input.tsx` + `src/shared/ui/inline-error.tsx` | (see text-input/inline-error for existing field primitives) | NEW (D-05). No tap-to-edit primitive exists. Compose `<input>` toggled by `isEditing` state. Save on blur/Enter, cancel on Esc. Optimistic value via controlled state. Tailwind hover affordance per CONTEXT specifics. Import `useReducedMotion` for transition suppression. |
| `src/app/sw.ts` | config / event-driven (extend) | `src/app/sw.ts:1-94` | `import { Serwist, NetworkOnly, NetworkFirst } from "serwist"; ... serwist.registerCapture(({ url }) => url.pathname.startsWith("/api/"), new NetworkOnly()); serwist.registerCapture(({ request }) => request.mode === "navigate", new NetworkFirst({ cacheName: "pages", networkTimeoutSeconds: 3 }));` | Extend: add `StaleWhileRevalidate` capture BEFORE the existing `NetworkOnly("/api/")` capture for the whitelist `/api/v1/plants*`, `/api/v1/photo-entries*`, `/api/v1/locations`. SWR entry must precede the catch-all NetworkOnly. `maxAgeSeconds: 7 * 24 * 3600` per D-19. |
| `src/app/(app)/catalog/page.tsx` | component / request-response (replace) | `src/app/(app)/catalog/page.tsx:1-14` | `import { getTranslations } from "next-intl/server"; import { EmptyState } from "@shared/ui/empty-state"; export default async function CatalogPage() { const t = await getTranslations("catalog.empty"); return (<EmptyState headline={t("title")} hint={t("hint")} ctaLabel={t("cta")} ctaHref="/catalog/add" />); }` | Replace empty-only render with server-component first-page count fetch (D-13). Keep `async` server component. Add `dehydrate(queryClient)` + `<HydrationBoundary>` pattern for TQ SSR handoff. Zero rows → `<CatalogEmpty />`; non-zero → `<CatalogGrid initialData={firstPage} />`. |
| `src/app/(app)/catalog/add/page.tsx` | component / CRUD | `src/app/(public)/auth/signup/page.tsx` (nearest form page) | (`async function Page()` server component that imports a `"use client"` form component) | NEW (D-01). Server component shell importing a `"use client"` form child (`<AddPlantForm>`). Photo picker renders local preview immediately; bytes deferred to submit. Validation per D-04 (per-field Overdue Rust, summary block at ≥2 errors). |
| `src/app/(app)/catalog/[plantId]/page.tsx` | component / request-response | `src/app/(app)/catalog/page.tsx:1-14` | (server component pattern) | NEW (D-05/D-07). Server component fetches plant detail via `getPlant` use-case. Renders `<PlantProfile>` client component with inline-edit fields, overflow-menu delete CTA, placeholder sections for Phase 6/7/8 surfaces. |
| `src/app/(app)/catalog/[plantId]/journal/page.tsx` | component / CRUD | `src/app/(app)/catalog/page.tsx:1-14` | (server component pattern) | NEW (D-14/D-15). Server component fetches photo entries. Renders `<PhotoJournal>` with `<Lightbox>` + "+ Foto" ModalSheet trigger. Read-only variant when `useSubscription().readOnly`. |
| `src/app/api/v1/plants/route.ts` | route / request-response | `src/app/api/v1/photos/upload/route.ts:1-104` + `src/shared/api/auth.ts:55-66` | `export const runtime = "nodejs"; export async function POST(request: Request): Promise<Response> { const auth = await requireApiUser(request); if (!auth.ok) { return errorResponse(auth.code, "missing or invalid bearer token"); } let formData: FormData; try { formData = await request.formData(); } catch (error) { ... } ... const result = await uploadPhoto({...}); if (!result.ok) { return errorResponse(result.code, result.reason); } return Response.json({...}, { status: 201 }); }` | NEW. POST uses `requireVerifiedUser` (not `requireApiUser` — verified email required per Phase 4 D-21). Multipart parse same as photos/upload. GET uses JSON query params + cursor/limit from `@shared/api/cursor`. No Drizzle imports (D-17). `withIdempotency` is the contract but has NO prior route consumer — first wiring in this phase. |
| `src/app/api/v1/plants/[plantId]/route.ts` | route / request-response | `src/app/api/v1/iam/me/route.ts:1-84` | `const patchSchema = z.object({ timezone: z.string().refine(...) }).strict(); export async function PATCH(request: Request) { const r = await requireApiUser(request); if (!r.ok) { return errorResponse(r.code, "Sessão inválida."); } let body: unknown; try { body = await request.json(); } catch { return errorResponse(ErrorCode.ValidationFailed, "Corpo inválido."); } const parsed = patchSchema.safeParse(body); if (!parsed.success) { return errorResponse(ErrorCode.ValidationFailed, "Validação falhou."); }` | NEW. GET + PATCH + DELETE. All three: `requireVerifiedUser` first. PATCH: JSON body, Zod schema for single-field updates (D-05). DELETE: no body. Response shape: snake_case keys per PRD §5. |
| `src/app/api/v1/plants/[plantId]/photo-entries/route.ts` | route / file-I/O | `src/app/api/v1/photos/upload/route.ts:1-104` | (multipart POST pattern) | NEW. POST: multipart (photo + note). GET: list with pagination. Both: `requireVerifiedUser`. |
| `src/app/api/v1/photo-entries/[photoEntryId]/route.ts` | route / CRUD | `src/app/api/v1/iam/me/route.ts:28-39` | `export async function GET(request: Request) { const r = await requireApiUser(request); if (!r.ok) { return errorResponse(r.code, "Sessão inválida."); } ... return NextResponse.json({ user: enriched }); }` | NEW. DELETE only. `requireVerifiedUser`, call `delete-photo-entry` use-case, return 204 or errorResponse. |
| `src/app/api/v1/locations/route.ts` | route / request-response | `src/app/api/v1/iam/me/route.ts:28-39` | (GET-only handler pattern) | NEW. GET only. `requireVerifiedUser`. Call `list-locations` use-case. Returns merged user suggestions + i18n defaults. |
| `tests/e2e/fixtures/authed-user.ts` | test fixture / request-response | `tests/integration/fixtures/seed-user.ts:1-69` + `tests/e2e/global-setup.ts:91-110` | `export async function seedUser(opts: SeedUserOptions): Promise<{ id: string }> { const supabaseAdmin = createClient(...); const { data, error } = await supabaseAdmin.auth.admin.createUser({ email: opts.email, password: opts.password, email_confirm: true }); ... await sql\`INSERT INTO users (...) VALUES (...) ON CONFLICT ...\`; return { id: data.user.id }; }` | NEW (D-26). Playwright fixture wrapping `seedUser` shape + `supabase.auth.admin.createUser`. Also inserts ConsentLog × 2 + Subscription (trialing 14d). Injects session cookies via `context.addCookies(...)` instead of dumping a JWT file. Reference `global-setup.ts:91-110` for the postgres direct-insert seed pattern. |
| `tests/e2e/fixtures/read-only.ts` | test fixture / request-response | `tests/e2e/axe-placeholder-pages.spec.ts:1-46` (fixture usage pattern) | `test("...", async ({ page }) => { ... })` — extend base fixture | NEW (D-21). Playwright fixture that extends `authed-user` fixture and sets `useSubscription` stub to `readOnly: true`. No real analog — stubs the hook at the module level or via route-handler MSW intercept. RESEARCH.md is authoritative. |
| `tests/integration/setup.ts` | test infrastructure / CRUD (extend) | `tests/integration/photo-upload.integration.test.ts:73-147` | `function makeFakeAdapter(): FakeAdapter { const stored = new Set<string>(); const uploadObject = vi.fn(async ({ bucket, objectKey }) => { stored.add(\`${bucket}/${objectKey}\`); return { bucket, objectKey }; }); ... const asAdapter: StorageAdapterShape = { uploadObject: ..., createSignedUrl: ..., deletePrefix: ..., deleteObject: ..., listBuckets: ..., listObjectsUnderPrefix: ... }; return { ... asAdapter }; }` | Extend (D-27): call `__setStorageAdapterForTests(new InMemoryStorageAdapter())` in `beforeAll` and restore to `null` in `afterAll`. `InMemoryStorageAdapter` uses `Map<bucket, Map<key, Buffer>>` — same interface as `FakeAdapter` in photo-upload test but with real buffer storage. Phase 2 D-43 transaction-rollback handles DB cleanup. |
| `tests/unit/setup-idb.ts` | test infrastructure | `tests/unit/setup-env.ts` (nearest unit setup file) | (`import "..."` side-effect-only setup file) | NEW (D-25). Single line: `import "fake-indexeddb/auto"`. Per-test isolation via `beforeEach(() => { indexedDB.deleteDatabase("folhario-cache"); })` or unique DB name per test. |
| `messages/pt-BR.json` | config / CRUD (extend) | `messages/pt-BR.json` (existing file) | (extend `catalog.*` namespace) | Extend with: `catalog.locations.defaults` (string array), `catalog.empty.*`, `catalog.sort.*`, `catalog.deleteConfirm.*`, `catalog.photoJournal.*`, `catalog.validation.*`. No new top-level key — all under existing `catalog` namespace. |
| `drizzle/migrations/XXXX_add_pending_storage_deletions.sql` | migration / CRUD | `drizzle/migrations/0003_phase04_iam_extensions.sql:1-60` | `CREATE TABLE "auth_throttle" (...); --> statement-breakpoint ALTER TABLE "email_verification_tokens" ADD CONSTRAINT ... --> statement-breakpoint ALTER TABLE "auth_throttle" ENABLE ROW LEVEL SECURITY;--> statement-breakpoint CREATE POLICY "service_role_only" ON "auth_throttle" FOR ALL TO service_role USING (true) WITH CHECK (true);` | NEW. Follow `0003` format: Drizzle-generated CREATE TABLE + manual `ENABLE ROW LEVEL SECURITY` + idempotent `DO $$ BEGIN IF NOT EXISTS ... CREATE POLICY ... END IF; END $$` per `0001_phase_02_rls_policies.sql:128-157` owner-all pattern. `pending_deletion_status` pgEnum defined here. Add `(status, scheduled_at)` index. |
| `drizzle/migrations/XXXX_add_location_suggestions.sql` | migration / CRUD | `drizzle/migrations/0001_phase_02_rls_policies.sql:128-157` | `DO $$ BEGIN IF NOT EXISTS (SELECT 1 FROM pg_policies WHERE policyname = 'consent_logs_owner_all') THEN CREATE POLICY "consent_logs_owner_all" ON public.consent_logs FOR ALL TO authenticated USING ((select auth.uid()) is not null and (select auth.uid()) = user_id) WITH CHECK ((select auth.uid()) is not null and (select auth.uid()) = user_id); END IF; END $$;` | NEW. Same idempotent RLS block pattern. Composite PK `(user_id, label_normalized)` instead of UUID PK. Owner-all policy: `USING (auth.uid() = user_id)`. |

---

## Shared Cross-Cutting Patterns

| Pattern | Source File | Apply To |
|---------|-------------|----------|
| `requireVerifiedUser(request)` auth gate | `src/shared/api/auth.ts:55-66` | All Phase 5 route handlers (`/api/v1/plants/*`, `/api/v1/photo-entries/*`, `/api/v1/locations`) |
| `errorResponse(code, message)` closed registry | `src/shared/config/errors.ts:64-81` | All route handlers, all use-cases returning `{ ok: false }` |
| `withUnitOfWork(userId, async (tx) => ...)` | `src/shared/db/unit-of-work.ts:88-103` | All mutating use-cases: `create-plant`, `update-plant`, `delete-plant`, `create-photo-entry`, `delete-photo-entry` |
| Discriminated-union result `{ok: true, ...} \| {ok: false, code, reason}` | `src/contexts/catalog/application/upload-photo.ts:82-91` | All application use-cases |
| `DbClient \| TransactionalDb` union typing | `src/contexts/catalog/infrastructure/db/plants.ts:16` | All repository functions |
| Ownership filter `WHERE user_id = $1 AND id = $2` at SQL layer | `src/contexts/catalog/infrastructure/db/plants.ts:24-30` | All repository read/mutate functions (RLS is defense in depth only) |
| PostHog server-side capture after TX commit via `getPostHog()` | `src/shared/telemetry/posthog-server.ts:1-23` | `create-plant.ts`, `update-plant.ts`, `delete-plant.ts` route handlers |
| `__setStorageAdapterForTests(adapter \| null)` test seam | `src/contexts/catalog/infrastructure/photo-storage.ts:74-79` | All catalog integration tests (D-27) |
| `export const runtime = "nodejs"` | `src/app/api/v1/photos/upload/route.ts:29` | Any route handler importing `sharp` or Node-only modules |
| Idempotent `DO $$ BEGIN IF NOT EXISTS ... END IF; END $$` RLS blocks | `drizzle/migrations/0001_phase_02_rls_policies.sql:128-157` | Both new Phase 5 migrations |
| i18n via `getTranslations("catalog.*")` (server) | `src/app/(app)/catalog/page.tsx:3` | All new server components: catalog page, add page, plant profile, journal |
| snake_case JSON response keys per PRD §5 | `src/app/api/v1/photos/upload/route.ts:92-103` | All Phase 5 API route responses |

---

## No Analog Found

| File | Role | Data Flow | Reason |
|------|------|-----------|--------|
| `src/shared/ui/combobox.tsx` | component | request-response | No headless Combobox or `useId`/`aria-activedescendant` pattern exists. Nearest structural reference: `modal-sheet.tsx` for file conventions; RESEARCH.md WAI-ARIA APG 1.2 is authoritative. |
| `src/shared/ui/lightbox.tsx` | component | event-driven | No full-screen image overlay exists; closest is `modal-sheet.tsx` for Radix Dialog wrapper shape. |
| `src/shared/ui/inline-edit-field.tsx` | component | request-response | No tap-to-edit primitive exists. Compose from `text-input.tsx` + `inline-error.tsx`. |
| `src/shared/ui/query-provider.tsx` | provider | request-response | No TanStack Query provider exists in codebase. RESEARCH.md TanStack v5 + `@tanstack/query-async-storage-persister` docs are authoritative. |
| `src/contexts/catalog/queries/index.ts` | utility | request-response | No query factory pattern exists. RESEARCH.md Tkdodo pattern is authoritative. |
| `src/shared/ui/storage-budget-guard.ts` | utility | event-driven | No `navigator.storage.estimate()` wrapper exists. RESEARCH.md is authoritative. |
| `tests/e2e/fixtures/read-only.ts` | test fixture | request-response | No subscription-state fixture exists. Extend `authed-user.ts` fixture + intercept `use-subscription` stub. RESEARCH.md is authoritative. |
| `src/app/(app)/catalog/[plantId]/page.tsx` (plant profile) | component | request-response | No dynamic route page with inline-edit pattern exists yet. Blend: server component shell from `catalog/page.tsx`, form field conventions from auth form components. |
| `src/contexts/billing/application/use-subscription.ts` | hook (stub) | request-response | No billing hook exists. Single-return stub; nearest: `use-online-status.ts` for hook file shape. |

---

## Key Findings

1. **`requireVerifiedUser` has no route consumer yet.** `src/shared/api/auth.ts:55` defines it; only a re-export at `src/contexts/iam/application/require-verified.ts:7` exists. All Phase 5 route handlers are first consumers.

2. **`withIdempotency` has no route-level wire-up yet.** `src/shared/api/idempotency.ts:109` defines the helper but no `/api/v1/*` route calls it. Phase 5 POST `/plants` and POST `/plants/:id/photo-entries` are first consumers.

3. **No `delete-*` use case exists anywhere.** `delete-plant.ts` and `delete-photo-entry.ts` are the first delete use-cases. Model compensating-delete + UoW pattern from `upload-photo.ts:200-228`.

4. **Inngest cron real analog absent.** `billingTrialEndingNotifier` at `src/contexts/billing/inngest/functions.ts:11-14` is a stub cron. The `cleanupStorageReconciler` is the first real cron. Shape: copy `notificationsSendEmail` (`src/contexts/notifications/inngest/functions.ts:14-41`) for step/retry structure; replace `triggers` with `{ cron: "0 * * * *" }` matching the stub pattern.

5. **Cursor shape extension needed.** Phase 2's `cursor.ts` encodes `{id, createdAt}`. Phase 5 D-12 needs `{sort_id, last_value, last_id}`. New `encodeSortCursor`/`decodeSortCursor` helpers should live in `src/shared/api/cursor.ts` or a new `src/contexts/catalog/application/list-plants-cursor.ts` without breaking the existing `CursorPayload` schema.

---

## PATTERN MAPPING COMPLETE
