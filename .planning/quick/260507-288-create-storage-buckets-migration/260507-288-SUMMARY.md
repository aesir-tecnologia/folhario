---
quick_id: 260507-288
description: Create Drizzle migration for Supabase Storage buckets
status: complete
completed: 2026-05-07
---

# Quick Task 260507-288 — Summary

## Outcome

Added an idempotent Drizzle migration to create/update the required Supabase Storage buckets in hosted preview/production projects.

## Changed

- `drizzle/migrations/0008_create_storage_buckets.sql`
  - Creates/updates `plant-photos`, `plant-thumbnails`, and `data-exports`.
  - Sets `public=false`, matching `supabase/config.toml`.
  - Sets file size limits and MIME allowlists.
  - Uses a `to_regclass('storage.buckets')` guard so plain Postgres CI databases without Supabase Storage skip safely.
- `drizzle/migrations/meta/_journal.json`
  - Registered migration `0008_create_storage_buckets`.
- `drizzle/migrations/meta/0008_snapshot.json`
  - Copied previous public-schema snapshot because this custom migration only touches Supabase's `storage` schema.
- `DEPLOY_FROM_LOCAL.md`
  - Updated bucket setup docs to point at the migration as source of truth.

## Verification

- `pnpm exec drizzle-kit check --config=drizzle.config.ts` — passes.
