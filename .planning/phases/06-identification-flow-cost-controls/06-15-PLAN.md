---
phase: 06-identification-flow-cost-controls
plan: 15
type: execute
wave: 7
depends_on: ["06-02", "06-10", "06-13", "06-14"]
files_modified:
  - tests/e2e/identify-consent.spec.ts
  - tests/e2e/identify-multi-photo.spec.ts
  - tests/e2e/identify-paywall.spec.ts
  - tests/e2e/identify-history.spec.ts
  - tests/e2e/identify-states.spec.ts
  - tests/e2e/identify-offline.spec.ts
autonomous: false
requirements:
  - IDENT-02
  - IDENT-17
  - IDENT-20
  - IDENT-21
  - UI-06
  - UI-12
must_haves:
  truths:
    - "identify-consent.spec.ts: authedUser without identification_third_party consent → tap Camera → LGPD modal renders; tap 'Entendo e aceito' → POST /api/v1/iam/consents; modal closes; identify request fires"
    - "identify-multi-photo.spec.ts: consentedUser → pick 3 photos via fixture file → PhotoStrip shows 3/5 badge + 3 thumbnails + remove-X buttons → tap remove → 2/5 badge → tap Identificar → stub provider returns 3 results → IdentificationResultCard stack visible"
    - "identify-paywall.spec.ts: consentedUser with read-only subscription → tap Camera → Radix Dialog (NOT bottom sheet) overlays with 'Reative sua assinatura...' + 'Ver assinatura' CTA linking to /settings/subscription"
    - "identify-history.spec.ts: consentedUser with prior identifications → /identify/history → list of IdentificationHistoryItem cards rendered; tap one → detail modal; for unlinked success items → 'Associar à planta' CTA visible"
    - "identify-states.spec.ts: visual snapshot per state machine (13 states × 2 themes × 2 motion preferences = 52 snapshots) — Phase 5 catalog visual baseline pattern"
    - "identify-offline.spec.ts: extends Phase 5 placeholder spec — context.setOffline(true) → identify CTAs hidden + capture guide STILL VISIBLE above offline composition (UI-SPEC line 121)"
  artifacts:
    - path: "tests/e2e/identify-consent.spec.ts"
      provides: "IDENT-02 LGPD consent modal smoke"
      min_lines: 40
    - path: "tests/e2e/identify-multi-photo.spec.ts"
      provides: "IDENT-17 multi-photo + capture guide smoke"
      min_lines: 60
    - path: "tests/e2e/identify-paywall.spec.ts"
      provides: "IDENT-21 read-only paywall smoke"
      min_lines: 30
    - path: "tests/e2e/identify-history.spec.ts"
      provides: "UI-12 history pages + re-associate smoke"
      min_lines: 50
    - path: "tests/e2e/identify-states.spec.ts"
      provides: "UI-06 13-state visual baseline matrix"
      min_lines: 80
  key_links:
    - from: "tests/e2e/identify-consent.spec.ts"
      to: "tests/e2e/fixtures/authed-user.ts"
      via: "import { test } from '../fixtures/authed-user' (NOT consented-user — this spec exercises the no-prior-consent path)"
      pattern: "fixtures/authed-user"
    - from: "tests/e2e/identify-multi-photo.spec.ts"
      to: "tests/e2e/fixtures/consented-user.ts"
      via: "import { test } from '../fixtures/consented-user'"
      pattern: "fixtures/consented-user"
---

<objective>
Wire the Phase 6 Playwright E2E suite covering all five user-facing flows from CONTEXT + UI-SPEC. Stub-provider mode is enforced via `playwright.config.ts` (set in 06-02 Wave 0); these specs never hit real Plant.id or OpenAI APIs.

Per 06-VALIDATION.md, these six specs close the gap rows for IDENT-02, IDENT-17, IDENT-20 (extend), IDENT-21, UI-06, UI-12. Stub provider returns deterministic fixtures (3 results: Costela-de-adão 0.85, Jiboia 0.62, Espada-de-são-jorge 0.35) so visual baselines are stable.

Output: 6 Playwright spec files. The visual baseline matrix (52 snapshots × 4 combos = 208 snapshot images) is generated via the Docker baseline script established in Phase 3 Plan 03-05; this plan's Task 4 includes a checkpoint for human-supervised baseline generation.
</objective>

<execution_context>
@/Users/machado/Projects/folhario/.claude/get-shit-done/workflows/execute-plan.md
@/Users/machado/Projects/folhario/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-identification-flow-cost-controls/06-CONTEXT.md
@.planning/phases/06-identification-flow-cost-controls/06-UI-SPEC.md
@.planning/phases/06-identification-flow-cost-controls/06-VALIDATION.md
@tests/e2e/fixtures/authed-user.ts
@tests/e2e/fixtures/consented-user.ts
@tests/e2e/catalog-grid.spec.ts
@tests/e2e/identify-offline.spec.ts
@tests/e2e/_test-resources/test-photo.jpg
@playwright.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: identify-consent + identify-multi-photo specs</name>
  <files>tests/e2e/identify-consent.spec.ts, tests/e2e/identify-multi-photo.spec.ts</files>
  <read_first>
    - tests/e2e/fixtures/authed-user.ts (no identification_third_party consent — used by identify-consent)
    - tests/e2e/fixtures/consented-user.ts (built in 06-02 — has identification_third_party consent — used by identify-multi-photo)
    - tests/e2e/catalog-create-plant.spec.ts (analog for multi-step + file-upload flow)
    - .planning/phases/06-identification-flow-cost-controls/06-UI-SPEC.md (§ State Machine + § LGPD Consent Modal)
  </read_first>
  <action>
    1. **`identify-consent.spec.ts`** (~40 lines):
       - Imports `test, expect` from `tests/e2e/fixtures/authed-user`
       - `test("first identify → LGPD modal → grant → identify fires", async ({ authedUser, page }) => {...})`
       - Steps: navigate to `/identify` → click Câmera button (or the underlying file input) → wait for `dialog[role="alertdialog"]` to appear → assert headline "Para identificar, precisamos enviar suas fotos" → click "Entendo e aceito" → wait for POST `/api/v1/iam/consents` to fire (use `page.waitForResponse`) → assert dialog closes → assert state transitions to `picking` (capture guide still visible without consent overlay)
       - Second test: click "Cancelar" → modal closes → state remains `idle` → no consent log row inserted (verify via SQL query against test DB)
    2. **`identify-multi-photo.spec.ts`** (~60 lines):
       - Imports `test, expect` from `tests/e2e/fixtures/consented-user`
       - `test("multi-photo identify happy path", async ({ consentedUser, page }) => {...})`
       - Steps: navigate to `/identify` → upload 3 photos via `page.setInputFiles('input[type=file]', [...])` (use the existing `test-photo.jpg` fixture × 3) → assert PhotoStrip shows 3 thumbnails + count badge "3/5" → tap one remove-X → count badge "2/5" → tap "Adicionar mais fotos" + upload 1 more → "3/5" → tap "Identificar" → wait for POST `/api/v1/identifications` to complete → assert state transitions through `uploading` → `identifying` → `results` → assert 3 IdentificationResultCard elements with confidence ladders → tap a result → `<ModalSheet>` opens with "Adicionar Costela-de-adão?" → submit "Adicionar à minha estante" → POST `/api/v1/identifications/{id}/confirm` → assert page.url() includes `/catalog/`
       - Validate stub provider deterministic results: 3 results, top result = "Costela-de-adão" / "Monstera deliciosa" / 0.85

    Both specs use `playwright.config.ts`'s `IDENTIFICATION_PROVIDER_MODE=stub` (set in 06-02), so Plant.id + OpenAI are bypassed.
  </action>
  <verify>
    <automated>pnpm exec playwright test tests/e2e/identify-consent.spec.ts tests/e2e/identify-multi-photo.spec.ts 2>&1 | tee /tmp/06-15-task1.log | grep -E "[0-9]+ passed"</automated>
  </verify>
  <acceptance_criteria>
    - identify-consent.spec.ts has >= 2 tests; both pass
    - identify-multi-photo.spec.ts has >= 1 happy-path test; passes
    - File contains literal "Entendo e aceito" (the LGPD CTA copy)
    - Stub mode: no real fetch to api.plant.id (verified via `page.route` interception expecting only stub URLs)
  </acceptance_criteria>
  <done>IDENT-02 + IDENT-17 covered by working e2e specs.</done>
</task>

<task type="auto">
  <name>Task 2: identify-paywall + identify-history + identify-offline specs</name>
  <files>tests/e2e/identify-paywall.spec.ts, tests/e2e/identify-history.spec.ts, tests/e2e/identify-offline.spec.ts</files>
  <read_first>
    - tests/e2e/fixtures/consented-user.ts
    - tests/e2e/identify-offline.spec.ts (existing Phase 5 placeholder spec — Phase 6 EXTENDS, does NOT replace)
    - .planning/phases/06-identification-flow-cost-controls/06-UI-SPEC.md (§ State 13 read-only-paywall, § State 12 offline, § History pages)
  </read_first>
  <action>
    1. **`identify-paywall.spec.ts`** (~30 lines):
       - Need a `readOnlyConsentedUser` fixture variant. Either extend `consented-user.ts` to accept a `readOnly: true` flag, OR create a new fixture in `tests/e2e/fixtures/readonly-consented-user.ts`. Recommend extending `consented-user.ts` with a `consentedUserReadOnly` named export. The fixture seeds a user whose Subscription is `status='canceled'` (or whatever flips Phase 5's `useSubscription()` stub to readOnly:true — verify the stub's contract).
       - Test: navigate to `/identify` → tap Câmera → assert Radix Dialog appears (selector: `[role="dialog"][data-state="open"]`) → assert headline "Reative sua assinatura para identificar novas plantas." → tap "Ver assinatura" → assert URL change to `/settings/subscription`
       - Second test: ESC key dismisses dialog (per UI-SPEC State 13 dismissOnEsc=true)
    2. **`identify-history.spec.ts`** (~50 lines):
       - Pre-seed user with 5 prior Identification rows (3 success + 1 timeout + 1 cap_hit) via `tests/integration/setup-identification.ts` helpers OR a Playwright-side SQL helper.
       - Test: navigate to `/identify/history` → assert top bar "Histórico de identificações" → assert 5 IdentificationHistoryItem cards visible → tap one with `status='timeout'` → assert detail modal opens with full record dump → close
       - Second test: per-plant history at `/catalog/{plantId}/identifications` → assert 1 card → assert empty state for a different plant uses RAW composition (NOT `<EmptyState>` — assert NO Canopy CTA element)
       - Third test: re-associate flow → unlinked success card with selectedResult → tap "Associar à planta" → ModalSheet opens with pre-loaded form
    3. **`identify-offline.spec.ts`** (EXTEND existing):
       - Read the current Phase 5 placeholder spec body
       - Extend with: after going offline (`context.setOffline(true)`), navigate to `/identify` → assert Câmera + Galeria CTAs are HIDDEN → assert capture guide grid IS visible (per UI-SPEC line 127 — capture guide stays above the offline composition) → assert offline copy "Identificação requer conexão à internet."
       - Online → CTAs visible again
  </action>
  <verify>
    <automated>pnpm exec playwright test tests/e2e/identify-paywall.spec.ts tests/e2e/identify-history.spec.ts tests/e2e/identify-offline.spec.ts 2>&1 | tee /tmp/06-15-task2.log | grep -E "[0-9]+ passed"</automated>
  </verify>
  <acceptance_criteria>
    - All three specs pass
    - identify-paywall.spec.ts asserts Radix Dialog (NOT bottom sheet) — specifically uses a Dialog-specific selector
    - identify-history.spec.ts covers per-plant raw-empty-state divergence
    - identify-offline.spec.ts EXTENDS the Phase 5 placeholder (not replaces) — preserves any existing assertions
  </acceptance_criteria>
  <done>IDENT-20 (extended), IDENT-21, UI-12 covered.</done>
</task>

<task type="auto">
  <name>Task 3: identify-states.spec.ts (visual baselines + structural assertions)</name>
  <files>tests/e2e/identify-states.spec.ts</files>
  <read_first>
    - tests/e2e/visual-snapshots.spec.ts (or whatever the Phase 3 Plan 03-05 visual-baseline file is named — find via `ls tests/e2e/`)
    - .planning/phases/06-identification-flow-cost-controls/06-UI-SPEC.md (§ State Machine table — all 13 states + transitions)
  </read_first>
  <action>
    1. Implement a Playwright spec that walks ALL 13 states by mocking server responses where needed:
       - State 1 (idle): consentedUser navigates to `/identify`
       - State 2 (consent-modal): authedUser (no consent) → tap Câmera
       - State 3 (picking): same as idle (UX-equivalent)
       - State 4 (photos-selected): pick 2 photos via file input
       - State 5 (uploading): mock POST `/api/v1/identifications` with a 3-second delay (`page.route`)
       - State 6 (identifying): mock POST with a longer delay (after the upload step in the state machine)
       - State 7 (results): allow stub to complete normally
       - State 8 (zero-results): mock POST → return 201 with empty results array
       - State 9 (cap-hit): mock POST → return 429 with cap_hit error
       - State 10 (provider-unavailable): mock POST → return 503
       - State 11 (timeout): mock POST → return 504
       - State 12 (offline): context.setOffline(true)
       - State 13 (read-only-paywall): readonly fixture + tap Câmera
    2. For each state, capture a `page.screenshot()` keyed `identify-state-{N}-{theme}-{motion}.png`. Use Playwright's `toMatchSnapshot` matcher.
    3. Loop over `[light, dark]` × `[normal-motion, reduced-motion]`:
       - Theme: set the cookie before navigation (Phase 3 D-30 cookie-based theme)
       - Motion: emulate via `page.emulateMedia({reducedMotion: 'reduce'})` for the reduced variant
    4. Add structural assertions on top of pixel snapshots:
       - State 9: NO retry button (per AC-ID-009 ban — assert `await page.locator('button:has-text("Tentar")')` count === 0 within the cap-hit container)
       - State 13: assert dialog has `role="dialog"` (NOT `role="alertdialog"` — paywall is dismissible by ESC, only the LGPD consent is alertdialog)
       - State 7: assert at least one ConfidenceLadder element with SR label "Confiança alta" / "Confiança média" / "Confiança baixa"
  </action>
  <verify>
    <automated>pnpm exec playwright test tests/e2e/identify-states.spec.ts 2>&1 | tee /tmp/06-15-task4.log | grep -E "[0-9]+ passed"</automated>
  </verify>
  <acceptance_criteria>
    - Spec compiles + tests pass (visual snapshots locked after Task 3 checkpoint approval)
    - State 9 has the no-retry-button assertion
    - State 13 uses `role="dialog"` (paywall) NOT `role="alertdialog"` (LGPD consent)
    - All 52 baseline images committed to `tests/e2e/__snapshots__/identify-states/` (or wherever Phase 3 baselines live)
  </acceptance_criteria>
  <done>UI-06 fully covered; visual regression suite locks all 13 states.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Generate + visually approve 52 baselines</name>
  <files>tests/e2e/__snapshots__/identify-states/ (generated)</files>
  <action>
    1. Verify Phase 5's Docker visual-baseline harness (`pnpm visual:baseline:docker`) is functional. If absent, document as a gap and ship the spec as expectation-only (DOM-only assertions; pixel snapshots deferred).
    2. Run `pnpm visual:baseline:docker -- --update tests/e2e/identify-states.spec.ts` to generate 52 baseline PNGs (13 states × light/dark × normal/reduced-motion).
    3. Wait for human visual inspection (see <how-to-verify>).
    4. After approval, commit the baselines to `tests/e2e/__snapshots__/identify-states/`.
  </action>
  <what-built>
    All 52 baseline PNGs generated by Task 3's spec via the Phase 5 Docker harness. Baselines lock the visual contract for the 13-state machine across light + dark themes + normal + reduced motion.
  </what-built>
  <how-to-verify>
    1. Open the 52 generated PNGs (any image viewer; or a Playwright HTML report).
    2. Visually inspect each baseline:
       - All 13 states render correctly
       - No emoji anywhere (Phase 6 banned-pattern)
       - No circular spinner in `uploading` / `identifying` (skeleton only — Phase 3 D-27)
       - Confidence ladder bars match tier color (high=Canopy, medium=Honey Amber, low=Calm Slate)
       - LGPD modal AiProvenanceChip visible at top
       - Read-only paywall is a Radix Dialog (centered overlay), NOT a bottom sheet
       - Capture guide present in idle/picking/offline states; HIDDEN in uploading/identifying/results
    3. Type "approved" if all baselines look correct. Otherwise describe per-state issues.
  </how-to-verify>
  <verify>
    <automated>ls tests/e2e/__snapshots__/identify-states/ 2>/dev/null | wc -l | awk '$1 < 52 { exit 1 }'</automated>
  </verify>
  <acceptance_criteria>
    - 52 baseline PNGs exist under `tests/e2e/__snapshots__/identify-states/` (or matching Phase 3 baseline directory convention)
    - Human approval recorded
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe issues per state</resume-signal>
  <done>Visual regression suite locked; downstream Phase 6 verification (gsd-verify-work) compares against these.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Playwright fixture seed → DB | Service-role connection inside fixture; matches Phase 5 pattern |
| Stub provider mode → CI | `IDENTIFICATION_PROVIDER_MODE=stub` enforced in playwright.config.ts (06-02); no real provider keys needed in CI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-15-01 | T (Tampering) | Real-provider call from CI | mitigate | playwright.config.ts forces stub mode; spec assertions can verify by checking that no fetch to `api.plant.id` occurred (`page.route` capture) |
| T-06-15-02 | I (Information Disclosure) | Test fixture credentials in logs | mitigate | Per-test unique email Date.now()-suffixed; matches Phase 5 fixture pattern |
| T-06-15-03 | T | Visual baseline drift | mitigate | Docker baseline generation (Phase 3 Plan 03-05) ensures consistent rendering across machines; checkpoint:human-verify before lock-in |
| T-06-15-04 | E (Elevation) | Read-only fixture bypass | accept | The fixture seeds DB state directly (service-role); Phase 10 will replace the stub with real Stripe state — these tests will need to be updated then |

</threat_model>

<verification>
- All 5 e2e spec files pass (excluding identify-states.spec.ts which depends on Task 3 baseline approval)
- After Task 3 approval, identify-states.spec.ts passes with 52 visual snapshots committed
- Stub mode enforced (no real Plant.id / OpenAI fetches in CI logs)
- The Sentry PII scrubbing invariant is verified end-to-end: an integration test (or e2e) that POSTs to `/api/v1/identifications` captures a Sentry breadcrumb and asserts request.data is dropped (already in 06-10 acceptance criteria — this plan is a downstream consumer)
</verification>

<success_criteria>
- IDENT-02, IDENT-17, IDENT-20, IDENT-21, UI-06, UI-12 all covered by working e2e specs
- 52 visual baselines locked after human checkpoint
- All Phase 6 e2e specs run in CI under stub-mode (no real provider calls)
- Phase 5's identify-offline spec extended (not replaced)
</success_criteria>

<output>
After completion, create `.planning/phases/06-identification-flow-cost-controls/06-15-SUMMARY.md`. SUMMARY MUST note:
1. Six e2e spec files (5 + 1 extended)
2. Visual baseline count (52 = 13 states × 4 combos)
3. Stub-mode enforcement: no real provider calls verified
4. Reminder for Phase 7: care-guide augment trigger event `identification.succeeded` is emitted from 06-09; Phase 7's first task is to consume it
5. Phase-completion checklist: all 35 requirement IDs (IDENT-01..21, COST-01..10, LGPD-09, UI-06, UI-12, UI-15) covered across plans 06-01..06-15
</output>
