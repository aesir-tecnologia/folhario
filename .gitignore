.DS_Store

# deps
node_modules/

# next
.next/
out/
next-env.d.ts

# serwist-generated service worker (build output)
public/sw.js
public/sw.js.map
public/swe-worker-*.js
public/workbox-*.js

# ts incremental
tsconfig.tsbuildinfo

# test + coverage
coverage/
playwright-report/
test-results/
.vitest-cache/
tests/e2e/.tmp-*.json

# env (NEVER commit .env — real creds live there)
.env
.env.local
.env.*.local

# supabase CLI
supabase/.temp/
supabase/.branches/

# ephemeral GSD subagent worktrees
.claude/worktrees/

# pnpm store created by Docker bind-mount install
.pnpm-store/

# Playwright MCP debug artifacts
.playwright-mcp/

# playwright-cli skill — browser session state (snapshots, console logs)
.playwright-cli/

# Local-only Playwright snapshots (CI uses linux baselines from Docker)
tests/e2e/visual-snapshots.spec.ts-snapshots/*darwin*.png
.vercel

# graphify — commit the graph, skip local-only artifacts
graphify-out/.graphify_*.json
graphify-out/.graphify_python
graphify-out/manifest.json
graphify-out/cost.json
graphify-out/cache/
