---
phase: 01
plan: 02
type: tdd
wave: 2
depends_on: [01]
files_modified:
  - src/contexts/iam/domain/.gitkeep
  - src/contexts/iam/application/.gitkeep
  - src/contexts/iam/infrastructure/.gitkeep
  - src/contexts/iam/api/.gitkeep
  - src/contexts/iam/inngest/.gitkeep
  - src/contexts/catalog/domain/.gitkeep
  - src/contexts/catalog/application/.gitkeep
  - src/contexts/catalog/infrastructure/.gitkeep
  - src/contexts/catalog/api/.gitkeep
  - src/contexts/catalog/inngest/.gitkeep
  - src/contexts/species-care/domain/.gitkeep
  - src/contexts/species-care/application/.gitkeep
  - src/contexts/species-care/infrastructure/.gitkeep
  - src/contexts/species-care/api/.gitkeep
  - src/contexts/species-care/inngest/.gitkeep
  - src/contexts/identification/domain/.gitkeep
  - src/contexts/identification/application/.gitkeep
  - src/contexts/identification/infrastructure/.gitkeep
  - src/contexts/identification/api/.gitkeep
  - src/contexts/identification/inngest/.gitkeep
  - src/contexts/reminders/domain/.gitkeep
  - src/contexts/reminders/application/.gitkeep
  - src/contexts/reminders/infrastructure/.gitkeep
  - src/contexts/reminders/api/.gitkeep
  - src/contexts/reminders/inngest/.gitkeep
  - src/contexts/billing/domain/.gitkeep
  - src/contexts/billing/application/.gitkeep
  - src/contexts/billing/infrastructure/.gitkeep
  - src/contexts/billing/api/.gitkeep
  - src/contexts/billing/inngest/.gitkeep
  - src/contexts/notifications/domain/.gitkeep
  - src/contexts/notifications/application/.gitkeep
  - src/contexts/notifications/infrastructure/.gitkeep
  - src/contexts/notifications/api/.gitkeep
  - src/contexts/notifications/inngest/.gitkeep
  - src/shared/db/.gitkeep
  - src/shared/events/.gitkeep
  - src/shared/adapters/.gitkeep
  - src/shared/config/errors.ts
  - src/shared/config/env.ts
  - tests/unit/scaffold.test.ts
  - tests/unit/errors.test.ts
  - tests/unit/env.test.ts
  - tests/unit/env-example.test.ts
autonomous: true
requirements:
  - INFRA-02
  - INFRA-16
  - INFRA-17
  - INFRA-20
tags:
  - scaffold
  - errors
  - env-validation
  - tdd

must_haves:
  truths:
    - "35 `.gitkeep` files exist under src/contexts/<7 contexts>/<5 layers>/ (per PRD §2)"
    - "3 `.gitkeep` files exist under src/shared/{db,events,adapters}/"
    - "`ErrorCode` object exports all 21 codes from PRD §5"
    - "Calling `errorResponse(CostCeilingReached, ...)` returns body with code `provider_unavailable` and HTTP status 503"
    - "Calling `errorResponse(BreakerOpen, ...)` returns body with code `provider_unavailable` and HTTP status 503"
    - "Response shape is `{ error: { code, message, details? } }` (D-11 nested shape)"
    - "`serverEnv` and `clientEnv` fail-parse on module load if required vars missing"
    - "`IDENTIFICATION_PROVIDER_MODE` rejects values other than `stub` / `real`"
    - "Empty-string Sentry DSN is coerced to undefined (D-19 local posture)"
    - "`.env.example` contains every PRD §20 variable name and no secret-like values"
  artifacts:
    - path: "src/shared/config/errors.ts"
      provides: "PRD §5 closed error-code registry + HTTP mapper + errorResponse helper"
      exports: ["ErrorCode", "ErrorBody", "errorResponse"]
      min_lines: 60
    - path: "src/shared/config/env.ts"
      provides: "Zod-validated env for server + client, fails fast at module load (D-29)"
      exports: ["serverEnv", "clientEnv"]
      min_lines: 30
    - path: "tests/unit/errors.test.ts"
      provides: "Exhaustive assertions for error registry + HTTP mapping + internal→public redaction"
      min_lines: 40
    - path: "tests/unit/env.test.ts"
      provides: "Assertions for server/client schema rejection of invalid input"
      min_lines: 25
    - path: "tests/unit/env-example.test.ts"
      provides: "Asserts .env.example lists every PRD §20 variable and holds no secret-looking values"
      min_lines: 20
    - path: "tests/unit/scaffold.test.ts"
      provides: "Asserts all 38 .gitkeep paths exist (INFRA-02)"
      min_lines: 25
  key_links:
    - from: "src/shared/config/errors.ts"
      to: "tests/unit/errors.test.ts"
      via: "unit test imports ErrorCode + errorResponse and asserts invariants"
      pattern: "import.*from.*errors"
    - from: "src/shared/config/env.ts"
      to: "process.env (boot time)"
      via: "zod schema parses at module load, throws on missing/invalid vars"
      pattern: "\\.parse\\(process\\.env\\)"
    - from: "tests/unit/env-example.test.ts"
      to: ".env.example"
      via: "readFileSync + key-presence assertion against hardcoded PRD §20 list"
      pattern: "readFileSync.*\\.env\\.example"
---

<objective>
Scaffold the PRD §2 bounded-context folder tree (38 `.gitkeep` placeholders), ship the closed error-code registry at `src/shared/config/errors.ts` + typed env validation at `src/shared/config/env.ts`, and land their RED→GREEN TDD tests. Error registry and env validation are pure business logic with defined I/O → dedicated TDD plan.

Purpose: Every future route handler imports `errorResponse` + `ErrorCode`; every server/client module imports `serverEnv` or `clientEnv`. Getting these two modules wrong — or silently incomplete — cascades into every Phase 2+ feature returning ad-hoc error codes or leaking env variables. The TDD tests are the contract.

Output: Scaffold tree exists. Error registry is exhaustively tested. Env validation fails fast and tests prove it. `.env.example` lint test catches any future secret leak.
</objective>

<execution_context>
@/Users/machado/Projects/folhario/.claude/get-shit-done/workflows/execute-plan.md
@/Users/machado/Projects/folhario/.claude/get-shit-done/templates/summary.md
@/Users/machado/Projects/folhario/.claude/get-shit-done/references/tdd.md
</execution_context>

<context>
@/Users/machado/Projects/folhario/CLAUDE.md
@/Users/machado/Projects/folhario/.planning/phases/01-foundation/01-CONTEXT.md
@/Users/machado/Projects/folhario/.planning/phases/01-foundation/01-RESEARCH.md
@/Users/machado/Projects/folhario/.planning/phases/01-foundation/01-PATTERNS.md
@/Users/machado/Projects/folhario/.planning/phases/01-foundation/01-VALIDATION.md
@/Users/machado/Projects/folhario/docs/CAVE-PRD.md
@/Users/machado/Projects/folhario/.env.example

<interfaces>
<!-- Contracts produced by this plan (consumed by Plans 03, 05, 06, 07) -->

From src/shared/config/errors.ts:
```typescript
export const ErrorCode = {
  Unauthenticated: "unauthenticated",
  TokenExpired: "token_expired",
  InvalidCredentials: "invalid_credentials",
  Forbidden: "forbidden",
  EmailUnverified: "email_unverified",
  ValidationFailed: "validation_failed",
  InvalidPartnerCode: "invalid_partner_code",
  NotFound: "not_found",
  Conflict: "conflict",
  ConsentRequired: "consent_required",
  DeletionInProgress: "deletion_in_progress",
  SubscriptionRequired: "subscription_required",
  ReadOnlyMode: "read_only_mode",
  CapHit: "cap_hit",
  ProviderUnavailable: "provider_unavailable",
  CostCeilingReached: "cost_ceiling_reached", // INTERNAL
  BreakerOpen: "breaker_open",                // INTERNAL
  Timeout: "timeout",
  WebhookSignatureInvalid: "webhook_signature_invalid",
  RateLimited: "rate_limited",
  InternalError: "internal_error",
} as const;

export type ErrorCode = (typeof ErrorCode)[keyof typeof ErrorCode];
export type ErrorBody = { error: { code: ErrorCode; message: string; details?: Record<string, unknown> } };
export function errorResponse(code: ErrorCode, message: string, details?: Record<string, unknown>): Response;
```

From src/shared/config/env.ts:
```typescript
// serverEnv shape (all required except optional() wrapped ones)
serverEnv.DATABASE_URL: string  // required, url
serverEnv.DATABASE_POOL_URL: string  // required, url
serverEnv.SUPABASE_SERVICE_ROLE_KEY: string  // required, min(1)
serverEnv.SENTRY_AUTH_TOKEN: string | undefined  // optional (empty=>undefined)
serverEnv.SENTRY_ORG: string | undefined
serverEnv.SENTRY_PROJECT: string | undefined
serverEnv.IDENTIFICATION_PROVIDER_MODE: "stub" | "real"  // default "stub"

// clientEnv shape
clientEnv.NEXT_PUBLIC_SUPABASE_URL: string  // required, url
clientEnv.NEXT_PUBLIC_SUPABASE_ANON_KEY: string  // required, min(1)
clientEnv.NEXT_PUBLIC_SENTRY_DSN: string | undefined  // optional
clientEnv.NEXT_PUBLIC_POSTHOG_KEY: string | undefined
clientEnv.NEXT_PUBLIC_POSTHOG_HOST: string | undefined
```

PRD §5 HTTP status mapping (every ErrorCode → HTTP status):
- unauthenticated, token_expired, invalid_credentials, webhook_signature_invalid → 401
- forbidden, email_unverified, consent_required, deletion_in_progress → 403
- validation_failed, invalid_partner_code → 400
- not_found → 404
- conflict → 409
- subscription_required, read_only_mode → 402
- cap_hit, rate_limited → 429
- provider_unavailable, cost_ceiling_reached, breaker_open → 503
- timeout → 504
- internal_error → 500

INTERNAL_ONLY codes (MUST surface as `provider_unavailable` in response body per AC-COST-007):
- cost_ceiling_reached
- breaker_open
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold PRD §2 bounded-context folder tree (38 .gitkeep files) + write scaffold.test.ts</name>
  <files>src/contexts/*/*/.gitkeep (35 files), src/shared/{db,events,adapters}/.gitkeep (3 files), tests/unit/scaffold.test.ts</files>
  <read_first>
    - /Users/machado/Projects/folhario/.planning/phases/01-foundation/01-RESEARCH.md §Recommended Project Structure (lines 338-414), §Folder count + scaffold bash (lines 410-427)
    - /Users/machado/Projects/folhario/.planning/phases/01-foundation/01-PATTERNS.md §.gitkeep scaffold files (lines 971-993)
    - /Users/machado/Projects/folhario/.planning/phases/01-foundation/01-CONTEXT.md D-09 (empty dirs with .gitkeep, no README stubs, no layer markers)
    - /Users/machado/Projects/folhario/docs/CAVE-PRD.md §2 (bounded context tree authoritative source)
  </read_first>
  <action>
Step 1 — Create the bounded-context folder tree. Run exactly this block:

```bash
mkdir -p src/shared/{db,events,adapters,config,telemetry}

for ctx in iam catalog species-care identification reminders billing notifications; do
  for layer in domain application infrastructure api inngest; do
    mkdir -p "src/contexts/$ctx/$layer"
    touch "src/contexts/$ctx/$layer/.gitkeep"
  done
done

for dir in db events adapters; do
  touch "src/shared/$dir/.gitkeep"
done
```

Result: 35 `.gitkeep` files in contexts (7 × 5), 3 `.gitkeep` files in shared. Total = 38. DO NOT create `.gitkeep` in `src/shared/config/` or `src/shared/telemetry/` — those dirs will immediately hold real files landed by Tasks 2-3 (env.ts, errors.ts) and Plans 05-06.

Step 2 — Write `tests/unit/scaffold.test.ts`:

```typescript
import { describe, it, expect } from "vitest";
import { existsSync } from "node:fs";
import { resolve } from "node:path";

const CONTEXTS = ["iam", "catalog", "species-care", "identification", "reminders", "billing", "notifications"] as const;
const LAYERS = ["domain", "application", "infrastructure", "api", "inngest"] as const;
const SHARED_DIRS = ["db", "events", "adapters"] as const;

describe("INFRA-02 bounded-context scaffold (PRD §2)", () => {
  it.each(CONTEXTS.flatMap((ctx) => LAYERS.map((layer) => [ctx, layer] as const)))(
    "src/contexts/%s/%s/.gitkeep exists",
    (ctx, layer) => {
      const p = resolve(process.cwd(), `src/contexts/${ctx}/${layer}/.gitkeep`);
      expect(existsSync(p), `expected ${p}`).toBe(true);
    },
  );

  it.each(SHARED_DIRS)("src/shared/%s/.gitkeep exists", (dir) => {
    const p = resolve(process.cwd(), `src/shared/${dir}/.gitkeep`);
    expect(existsSync(p), `expected ${p}`).toBe(true);
  });

  it("total .gitkeep count is 38 (35 contexts + 3 shared)", () => {
    const ctxCount = CONTEXTS.length * LAYERS.length;
    const sharedCount = SHARED_DIRS.length;
    expect(ctxCount + sharedCount).toBe(38);
  });
});
```

Step 3 — Run `pnpm test:unit` — the scaffold test must PASS (all 38 paths exist).
  </action>
  <verify>
    <automated>ls -1 src/contexts/iam/domain/.gitkeep src/contexts/notifications/inngest/.gitkeep src/shared/db/.gitkeep src/shared/events/.gitkeep src/shared/adapters/.gitkeep && test $(find src/contexts -name .gitkeep | wc -l | tr -d ' ') -eq 35 && test $(find src/shared -maxdepth 2 -name .gitkeep | wc -l | tr -d ' ') -eq 3 && pnpm test:unit -- tests/unit/scaffold.test.ts</automated>
  </verify>
  <done>
    - 35 `.gitkeep` files under `src/contexts/<7 contexts>/<5 layers>/`
    - 3 `.gitkeep` files under `src/shared/{db,events,adapters}/`
    - NO `.gitkeep` under `src/shared/config/` or `src/shared/telemetry/` (real files arrive in later tasks)
    - `tests/unit/scaffold.test.ts` passes with ≥39 individual assertions
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED→GREEN for error registry (`src/shared/config/errors.ts`) — exhaustive test of all 21 codes + HTTP mapper + internal→public redaction</name>
  <files>tests/unit/errors.test.ts, src/shared/config/errors.ts</files>
  <read_first>
    - /Users/machado/Projects/folhario/.planning/phases/01-foundation/01-PATTERNS.md §SP-1 Error registry (lines 95-166)
    - /Users/machado/Projects/folhario/.planning/phases/01-foundation/01-RESEARCH.md §Pattern 4 Error-code registry (lines 539-629)
    - /Users/machado/Projects/folhario/.planning/phases/01-foundation/01-CONTEXT.md D-10, D-11, D-12
    - /Users/machado/Projects/folhario/docs/CAVE-PRD.md §5 (closed registry authoritative)
  </read_first>
  <behavior>
    - Test 1: `ErrorCode` object literal contains all 21 codes with the exact string values listed in the `<interfaces>` block
    - Test 2: `ErrorCode` union type (via typeof) accepts only those 21 literals
    - Test 3: Every `ErrorCode` value maps to exactly one HTTP status per PRD §5 table (21 explicit assertions; no default)
    - Test 4: `errorResponse(ErrorCode.Unauthenticated, "msg")` returns Response with status 401 and body `{ error: { code: "unauthenticated", message: "msg" } }`
    - Test 5: `errorResponse(ErrorCode.CostCeilingReached, "msg")` returns Response with status 503 AND body `{ error: { code: "provider_unavailable", ... } }` (internal redaction — AC-COST-007)
    - Test 6: `errorResponse(ErrorCode.BreakerOpen, "msg")` same redaction as Test 5
    - Test 7: `errorResponse(ErrorCode.ValidationFailed, "msg", { field: "email" })` body includes `error.details: { field: "email" }`
    - Test 8: `errorResponse` omits `error.details` key entirely when details argument is undefined (no `details: undefined` noise)
    - Test 9: Response content-type is `application/json`
  </behavior>
  <action>
RED phase:

Step 1 — Write `tests/unit/errors.test.ts` covering all 9 behaviors above. Do NOT create `errors.ts` yet. Run `pnpm test:unit -- tests/unit/errors.test.ts` — MUST fail because the module does not exist.

```typescript
import { describe, it, expect } from "vitest";
import { ErrorCode, errorResponse, type ErrorBody } from "@shared/config/errors";

const EXPECTED_CODES = {
  Unauthenticated: "unauthenticated",
  TokenExpired: "token_expired",
  InvalidCredentials: "invalid_credentials",
  Forbidden: "forbidden",
  EmailUnverified: "email_unverified",
  ValidationFailed: "validation_failed",
  InvalidPartnerCode: "invalid_partner_code",
  NotFound: "not_found",
  Conflict: "conflict",
  ConsentRequired: "consent_required",
  DeletionInProgress: "deletion_in_progress",
  SubscriptionRequired: "subscription_required",
  ReadOnlyMode: "read_only_mode",
  CapHit: "cap_hit",
  ProviderUnavailable: "provider_unavailable",
  CostCeilingReached: "cost_ceiling_reached",
  BreakerOpen: "breaker_open",
  Timeout: "timeout",
  WebhookSignatureInvalid: "webhook_signature_invalid",
  RateLimited: "rate_limited",
  InternalError: "internal_error",
} as const;

const EXPECTED_HTTP: Record<string, number> = {
  unauthenticated: 401, token_expired: 401, invalid_credentials: 401, webhook_signature_invalid: 401,
  forbidden: 403, email_unverified: 403, consent_required: 403, deletion_in_progress: 403,
  validation_failed: 400, invalid_partner_code: 400,
  not_found: 404, conflict: 409,
  subscription_required: 402, read_only_mode: 402,
  cap_hit: 429, rate_limited: 429,
  provider_unavailable: 503, cost_ceiling_reached: 503, breaker_open: 503,
  timeout: 504, internal_error: 500,
};

describe("INFRA-20 error registry (PRD §5)", () => {
  it("ErrorCode exports all 21 expected codes", () => {
    expect(Object.entries(ErrorCode)).toHaveLength(21);
    for (const [k, v] of Object.entries(EXPECTED_CODES)) {
      expect((ErrorCode as Record<string, string>)[k]).toBe(v);
    }
  });

  it.each(Object.entries(EXPECTED_HTTP))(
    "errorResponse(%s) returns HTTP %i (or redacted 503 for internal)",
    async (code, expectedStatus) => {
      const res = errorResponse(code as never, "test msg");
      // internal-only codes surface as provider_unavailable=503, but HTTP_STATUS[provider_unavailable] is also 503
      expect(res.status).toBe(expectedStatus === 503 ? 503 : expectedStatus);
    },
  );

  it("cost_ceiling_reached redacts to provider_unavailable in the response body", async () => {
    const res = errorResponse(ErrorCode.CostCeilingReached, "internal");
    const body: ErrorBody = await res.json();
    expect(body.error.code).toBe("provider_unavailable");
    expect(res.status).toBe(503);
  });

  it("breaker_open redacts to provider_unavailable in the response body", async () => {
    const res = errorResponse(ErrorCode.BreakerOpen, "internal");
    const body: ErrorBody = await res.json();
    expect(body.error.code).toBe("provider_unavailable");
    expect(res.status).toBe(503);
  });

  it("non-internal codes keep their literal code in the body", async () => {
    const res = errorResponse(ErrorCode.Unauthenticated, "nope");
    const body: ErrorBody = await res.json();
    expect(body.error.code).toBe("unauthenticated");
    expect(body.error.message).toBe("nope");
  });

  it("body shape is { error: { code, message, details? } } per D-11", async () => {
    const res = errorResponse(ErrorCode.ValidationFailed, "bad", { field: "email" });
    const body = await res.json();
    expect(Object.keys(body)).toEqual(["error"]);
    expect(body.error).toMatchObject({ code: "validation_failed", message: "bad", details: { field: "email" } });
  });

  it("omits details key entirely when argument is undefined", async () => {
    const res = errorResponse(ErrorCode.NotFound, "gone");
    const body = await res.json();
    expect(body.error).not.toHaveProperty("details");
  });

  it("content-type is application/json", () => {
    const res = errorResponse(ErrorCode.InternalError, "x");
    expect(res.headers.get("content-type")).toBe("application/json");
  });
});
```

Step 2 — Confirm RED: `pnpm test:unit -- tests/unit/errors.test.ts` FAILS (module not found).

Commit the failing test:
```bash
git add tests/unit/errors.test.ts
git commit -m "test(01-02): add failing tests for error registry (PRD §5)"
```

GREEN phase:

Step 3 — Create `src/shared/config/errors.ts` implementing the contract. Use the verbatim implementation from PATTERNS.md SP-1 (lines 104-157). Rewrite here for clarity — ensure the EXACT code:

```typescript
// src/shared/config/errors.ts
// Closed error-code registry (PRD §5). Internal-only codes surface as `provider_unavailable` per AC-COST-007.

export const ErrorCode = {
  Unauthenticated: "unauthenticated",
  TokenExpired: "token_expired",
  InvalidCredentials: "invalid_credentials",
  Forbidden: "forbidden",
  EmailUnverified: "email_unverified",
  ValidationFailed: "validation_failed",
  InvalidPartnerCode: "invalid_partner_code",
  NotFound: "not_found",
  Conflict: "conflict",
  ConsentRequired: "consent_required",
  DeletionInProgress: "deletion_in_progress",
  SubscriptionRequired: "subscription_required",
  ReadOnlyMode: "read_only_mode",
  CapHit: "cap_hit",
  ProviderUnavailable: "provider_unavailable",
  CostCeilingReached: "cost_ceiling_reached",
  BreakerOpen: "breaker_open",
  Timeout: "timeout",
  WebhookSignatureInvalid: "webhook_signature_invalid",
  RateLimited: "rate_limited",
  InternalError: "internal_error",
} as const;

export type ErrorCode = (typeof ErrorCode)[keyof typeof ErrorCode];

const INTERNAL_ONLY: ReadonlySet<ErrorCode> = new Set([
  ErrorCode.CostCeilingReached,
  ErrorCode.BreakerOpen,
]);

const HTTP_STATUS: Record<ErrorCode, number> = {
  unauthenticated: 401,
  token_expired: 401,
  invalid_credentials: 401,
  forbidden: 403,
  email_unverified: 403,
  validation_failed: 400,
  invalid_partner_code: 400,
  not_found: 404,
  conflict: 409,
  consent_required: 403,
  deletion_in_progress: 403,
  subscription_required: 402,
  read_only_mode: 402,
  cap_hit: 429,
  provider_unavailable: 503,
  cost_ceiling_reached: 503,
  breaker_open: 503,
  timeout: 504,
  webhook_signature_invalid: 401,
  rate_limited: 429,
  internal_error: 500,
};

export type ErrorBody = {
  error: {
    code: ErrorCode;
    message: string;
    details?: Record<string, unknown>;
  };
};

export function errorResponse(
  code: ErrorCode,
  message: string,
  details?: Record<string, unknown>,
): Response {
  const publicCode: ErrorCode = INTERNAL_ONLY.has(code) ? ErrorCode.ProviderUnavailable : code;
  const body: ErrorBody = {
    error: {
      code: publicCode,
      message,
      ...(details ? { details } : {}),
    },
  };
  return new Response(JSON.stringify(body), {
    status: HTTP_STATUS[publicCode],
    headers: { "content-type": "application/json" },
  });
}
```

Step 4 — Run `pnpm test:unit -- tests/unit/errors.test.ts` — MUST pass.

Step 5 — Run `pnpm typecheck` — MUST pass (exhaustive `Record<ErrorCode, number>` check proves no missing/extra codes at compile time).

Commit:
```bash
git add src/shared/config/errors.ts
git commit -m "feat(01-02): implement PRD §5 error registry + HTTP mapper"
```
  </action>
  <verify>
    <automated>pnpm test:unit -- tests/unit/errors.test.ts && pnpm typecheck && grep -c "'[a-z_]*': [0-9]" src/shared/config/errors.ts | grep -q "^21$" || grep -cE "^\s+[a-z_]+:\s+[0-9]+" src/shared/config/errors.ts | awk '$1 >= 21' | grep -q .</automated>
  </verify>
  <done>
    - `src/shared/config/errors.ts` exports `ErrorCode` (21 codes), `ErrorBody` type, `errorResponse` function
    - `tests/unit/errors.test.ts` passes with ≥28 assertions (21 HTTP mapping + 7 behavior tests)
    - `errorResponse(ErrorCode.CostCeilingReached, ...)` returns status 503, body `error.code === "provider_unavailable"`
    - Two atomic commits: failing test → passing implementation
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: RED→GREEN for env validation (`src/shared/config/env.ts`) + `.env.example` lint test</name>
  <files>tests/unit/env.test.ts, tests/unit/env-example.test.ts, src/shared/config/env.ts</files>
  <read_first>
    - /Users/machado/Projects/folhario/.planning/phases/01-foundation/01-PATTERNS.md §SP-2 Typed env validation (lines 168-213), §.env.example (lines 427-462)
    - /Users/machado/Projects/folhario/.planning/phases/01-foundation/01-RESEARCH.md §Pattern 5 Typed env validation with Zod (lines 631-673)
    - /Users/machado/Projects/folhario/.planning/phases/01-foundation/01-CONTEXT.md D-29, D-30
    - /Users/machado/Projects/folhario/.env.example (from Plan 01 Task 3)
  </read_first>
  <behavior>
    - Test env-1: A schema-shape test (NOT on process.env): passing a valid object through the schema returns expected shapes
    - Test env-2: Passing `IDENTIFICATION_PROVIDER_MODE=bogus` through the server schema rejects
    - Test env-3: Omitting `IDENTIFICATION_PROVIDER_MODE` → default = "stub"
    - Test env-4: Missing `DATABASE_URL` through the server schema rejects with readable error
    - Test env-5: Empty-string `NEXT_PUBLIC_SENTRY_DSN` parses to `undefined` (D-19 local posture)
    - Test env-6: Invalid URL in `NEXT_PUBLIC_SUPABASE_URL` rejects
    - Test example-1: `.env.example` file exists and is readable
    - Test example-2: Every required PRD §20 variable name appears as a key in `.env.example` (hardcoded list — do NOT diff against live `.env`)
    - Test example-3: No value in `.env.example` matches secret-like patterns (`^sk_live_`, JWT shape `eyJ[A-Za-z0-9_-]{20,}`, base64 >40 chars)
  </behavior>
  <action>
RED phase — env schemas:

Step 1 — Because `src/shared/config/env.ts` parses `process.env` at module load, the test file must NOT import it directly (that would parse the actual environment, which in test contexts may not have the required vars). Instead, export the **zod schemas** separately from the parsed results, and test the schemas with synthetic input.

Refactor strategy (write test against this contract):
- `env.ts` exports `serverSchema`, `clientSchema` (the Zod objects) AND `serverEnv`, `clientEnv` (the parsed results)
- Tests import and exercise the schemas; boot-time parsing is tested implicitly by the build step and Plan 08 CI run.

Step 2 — Write `tests/unit/env.test.ts`:

```typescript
import { describe, it, expect } from "vitest";
import { serverSchema, clientSchema } from "@shared/config/env";

describe("INFRA-16 + INFRA-17 env validation (D-29)", () => {
  const validServer = {
    DATABASE_URL: "postgres://u:p@localhost:5432/db",
    DATABASE_POOL_URL: "postgres://u:p@localhost:5432/db",
    SUPABASE_SERVICE_ROLE_KEY: "k",
    IDENTIFICATION_PROVIDER_MODE: "stub",
  };
  const validClient = {
    NEXT_PUBLIC_SUPABASE_URL: "http://localhost:54321",
    NEXT_PUBLIC_SUPABASE_ANON_KEY: "anon",
  };

  it("accepts a minimally valid server env", () => {
    expect(() => serverSchema.parse(validServer)).not.toThrow();
  });

  it("rejects IDENTIFICATION_PROVIDER_MODE other than stub/real", () => {
    expect(() => serverSchema.parse({ ...validServer, IDENTIFICATION_PROVIDER_MODE: "bogus" })).toThrow();
  });

  it("defaults IDENTIFICATION_PROVIDER_MODE to 'stub' when absent", () => {
    const { IDENTIFICATION_PROVIDER_MODE, ...rest } = validServer;
    const parsed = serverSchema.parse(rest);
    expect(parsed.IDENTIFICATION_PROVIDER_MODE).toBe("stub");
  });

  it("rejects missing DATABASE_URL", () => {
    const { DATABASE_URL, ...rest } = validServer;
    expect(() => serverSchema.parse(rest)).toThrow();
  });

  it("coerces empty-string NEXT_PUBLIC_SENTRY_DSN to undefined (D-19)", () => {
    const parsed = clientSchema.parse({ ...validClient, NEXT_PUBLIC_SENTRY_DSN: "" });
    expect(parsed.NEXT_PUBLIC_SENTRY_DSN).toBeUndefined();
  });

  it("coerces empty-string NEXT_PUBLIC_POSTHOG_KEY to undefined (D-20)", () => {
    const parsed = clientSchema.parse({ ...validClient, NEXT_PUBLIC_POSTHOG_KEY: "" });
    expect(parsed.NEXT_PUBLIC_POSTHOG_KEY).toBeUndefined();
  });

  it("rejects invalid NEXT_PUBLIC_SUPABASE_URL", () => {
    expect(() => clientSchema.parse({ ...validClient, NEXT_PUBLIC_SUPABASE_URL: "not-a-url" })).toThrow();
  });
});
```

Step 3 — Write `tests/unit/env-example.test.ts` — hardcoded PRD §20 list (do NOT diff against live `.env` per advisor note):

```typescript
import { describe, it, expect } from "vitest";
import { readFileSync, existsSync } from "node:fs";
import { resolve } from "node:path";

// PRD §20 variables — hardcoded to survive laptop-specific operator-extras in live .env
const REQUIRED_KEYS = [
  "DATABASE_URL",
  "DATABASE_POOL_URL",
  "NEXT_PUBLIC_SUPABASE_URL",
  "NEXT_PUBLIC_SUPABASE_ANON_KEY",
  "SUPABASE_SERVICE_ROLE_KEY",
  "SUPABASE_PROJECT_REF",
  "SUPABASE_ACCESS_TOKEN",
  "IDENTIFICATION_PROVIDER_MODE",
  "NEXT_PUBLIC_SENTRY_DSN",
  "SENTRY_AUTH_TOKEN",
  "SENTRY_ORG",
  "SENTRY_PROJECT",
  "NEXT_PUBLIC_POSTHOG_KEY",
  "NEXT_PUBLIC_POSTHOG_HOST",
  "INNGEST_EVENT_KEY",
  "INNGEST_SIGNING_KEY",
  "VERCEL_ORG_ID",
  "VERCEL_PROJECT_ID",
  "VERCEL_TOKEN",
];

const SECRET_PATTERNS = [
  /sk_live_[A-Za-z0-9]{20,}/,
  /eyJ[A-Za-z0-9_-]{20,}\.[A-Za-z0-9_-]{20,}/, // JWT shape
  /=([A-Za-z0-9+/]{40,}=*)$/m,                 // raw base64 ≥40 chars in a value
];

describe("INFRA-17 .env.example mirrors PRD §20 without leaking secrets", () => {
  const path = resolve(process.cwd(), ".env.example");

  it(".env.example exists", () => {
    expect(existsSync(path)).toBe(true);
  });

  const content = existsSync(path) ? readFileSync(path, "utf-8") : "";

  it.each(REQUIRED_KEYS)("contains key %s", (key) => {
    const re = new RegExp(`^${key}=`, "m");
    expect(content).toMatch(re);
  });

  it.each(SECRET_PATTERNS)("does not leak secrets matching %s", (pattern) => {
    expect(content).not.toMatch(pattern);
  });
});
```

Step 4 — RED: `pnpm test:unit -- tests/unit/env.test.ts tests/unit/env-example.test.ts` MUST fail because `src/shared/config/env.ts` does not export `serverSchema` / `clientSchema`.

Commit:
```bash
git add tests/unit/env.test.ts tests/unit/env-example.test.ts
git commit -m "test(01-02): add failing tests for env validation + .env.example lint"
```

GREEN phase:

Step 5 — Create `src/shared/config/env.ts`. Export both the schemas (for testability) AND the parsed results (for runtime):

```typescript
// src/shared/config/env.ts
// D-29: Zod-validated env split server vs client by NEXT_PUBLIC_ prefix. Fails fast at module load.
// Schemas are exported so tests can exercise them with synthetic input.

import { z } from "zod";

const optional = (schema: z.ZodString) =>
  z.preprocess((v) => (v === "" ? undefined : v), schema.optional());

export const serverSchema = z.object({
  DATABASE_URL: z.string().url(),
  DATABASE_POOL_URL: z.string().url(),
  SUPABASE_SERVICE_ROLE_KEY: z.string().min(1),
  SENTRY_AUTH_TOKEN: optional(z.string().min(1)),
  SENTRY_ORG: optional(z.string().min(1)),
  SENTRY_PROJECT: optional(z.string().min(1)),
  IDENTIFICATION_PROVIDER_MODE: z.enum(["stub", "real"]).default("stub"),
});

export const clientSchema = z.object({
  NEXT_PUBLIC_SUPABASE_URL: z.string().url(),
  NEXT_PUBLIC_SUPABASE_ANON_KEY: z.string().min(1),
  NEXT_PUBLIC_SENTRY_DSN: optional(z.string().url()),
  NEXT_PUBLIC_POSTHOG_KEY: optional(z.string().min(1)),
  NEXT_PUBLIC_POSTHOG_HOST: optional(z.string().url()),
});

export const serverEnv = serverSchema.parse(process.env);

export const clientEnv = clientSchema.parse({
  NEXT_PUBLIC_SUPABASE_URL: process.env.NEXT_PUBLIC_SUPABASE_URL,
  NEXT_PUBLIC_SUPABASE_ANON_KEY: process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY,
  NEXT_PUBLIC_SENTRY_DSN: process.env.NEXT_PUBLIC_SENTRY_DSN,
  NEXT_PUBLIC_POSTHOG_KEY: process.env.NEXT_PUBLIC_POSTHOG_KEY,
  NEXT_PUBLIC_POSTHOG_HOST: process.env.NEXT_PUBLIC_POSTHOG_HOST,
});
```

Step 6 — Test execution: Vitest unit tests import `serverSchema` / `clientSchema` (the schema objects, not the parsed results), so the module will try to `.parse(process.env)` at load time. If `DATABASE_URL` etc. are not in the test env, loading will throw before the tests can run.

**Resolution: use `vitest` setup that primes `process.env` before the env module is imported.** Add this to the TOP of each env test file (before any other import):

```typescript
// MUST run BEFORE `import { serverSchema } from "@shared/config/env"`
process.env.DATABASE_URL ??= "postgres://u:p@localhost:5432/db";
process.env.DATABASE_POOL_URL ??= "postgres://u:p@localhost:5432/db";
process.env.SUPABASE_SERVICE_ROLE_KEY ??= "test-key";
process.env.NEXT_PUBLIC_SUPABASE_URL ??= "http://localhost:54321";
process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY ??= "test-anon";
```

Wait — the above will still run AFTER static imports in a typical ESM file. The clean solution is a Vitest globalSetup or a per-file setup file.

Create `tests/unit/setup-env.ts`:

```typescript
// Primes process.env BEFORE env.ts parses at module load. Referenced via vitest.config.ts setupFiles.
process.env.DATABASE_URL ??= "postgres://u:p@localhost:5432/db";
process.env.DATABASE_POOL_URL ??= "postgres://u:p@localhost:5432/db";
process.env.SUPABASE_SERVICE_ROLE_KEY ??= "test-key";
process.env.NEXT_PUBLIC_SUPABASE_URL ??= "http://localhost:54321";
process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY ??= "test-anon";
process.env.IDENTIFICATION_PROVIDER_MODE ??= "stub";
```

Update `vitest.config.ts` (from Plan 01 Task 3) by adding `setupFiles` to BOTH projects:

```typescript
// vitest.config.ts — add setupFiles: ["tests/unit/setup-env.ts"] to each project's test object
```

Note: This is a modification to a file created by Plan 01. Since Plan 01 already shipped at Wave 1, Plan 02 at Wave 2 can modify it. DO NOT forget to re-run `pnpm test:unit` after editing.

Step 7 — Run `pnpm test:unit` — all env/env-example/errors/scaffold tests PASS.

Commit:
```bash
git add src/shared/config/env.ts tests/unit/setup-env.ts vitest.config.ts
git commit -m "feat(01-02): implement Zod env validation with schema+parsed exports"
```
  </action>
  <verify>
    <automated>pnpm test:unit -- tests/unit/env.test.ts tests/unit/env-example.test.ts && grep -q "serverSchema" src/shared/config/env.ts && grep -q "clientSchema" src/shared/config/env.ts && grep -q "IDENTIFICATION_PROVIDER_MODE.*enum" src/shared/config/env.ts && grep -q "setupFiles" vitest.config.ts && pnpm typecheck</automated>
  </verify>
  <done>
    - `src/shared/config/env.ts` exports `serverSchema`, `clientSchema`, `serverEnv`, `clientEnv`
    - `tests/unit/env.test.ts` covers all 7 env behaviors (shape, mode enum, default, missing var, empty-string coercion, invalid url)
    - `tests/unit/env-example.test.ts` asserts 19 required keys + 3 secret-pattern guards
    - `tests/unit/setup-env.ts` primes process.env and is registered via `vitest.config.ts` `setupFiles`
    - Two atomic commits: failing tests → passing implementation
    - `pnpm test:unit` passes all 4 test files (scaffold, errors, env, env-example)
    - `pnpm typecheck` exits 0
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Route handlers → clients | Internal error codes must redact to `provider_unavailable` |
| Boot process → runtime | Env vars must pass schema before any downstream code runs |
| `.env.example` → git history | File cannot leak real secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | Internal error code leak (`cost_ceiling_reached`, `breaker_open`) | mitigate | `errorResponse` redacts both codes to `provider_unavailable` before building body (AC-COST-007). Tests assert literal redacted string in body across both codes. |
| T-02-02 | Tampering | Env var injection at runtime | mitigate | Zod `.parse()` on `process.env` at module load; invalid values throw before any downstream module initializes. `IDENTIFICATION_PROVIDER_MODE` enum enforced. |
| T-02-03 | Information Disclosure | `.env.example` leaking real secrets | mitigate | Hand-authored from PRD §20 table (Plan 01 Task 3); Plan 02 test asserts no `sk_live_`, JWT shape, or long base64 strings. |
| T-02-04 | Information Disclosure | `error.details` leaking stack traces / PII from caller | accept-delegated | Route handlers (Phase 2+) MUST NOT pass raw error objects into `details`; responsibility is at the callsite. Scrub rule lives in Plan 05 Sentry scrubber as defense in depth. |

Threats deferred: route-handler JWT verification (Phase 4), input validation for route bodies (Phase 2), RLS (Phase 2).
</threat_model>

<verification>
1. `find src/contexts -name .gitkeep | wc -l` returns 35.
2. `find src/shared -maxdepth 2 -name .gitkeep | wc -l` returns 3.
3. `pnpm test:unit` passes all four unit test files (scaffold, errors, env, env-example).
4. `pnpm typecheck` exits 0 — exhaustive `Record<ErrorCode, number>` proves no missing/extra codes.
5. Grep confirms `errorResponse` has the exact redaction branch: `grep -q "INTERNAL_ONLY.has(code)" src/shared/config/errors.ts`.
6. Grep confirms Zod schemas are exported: `grep -q "export const serverSchema" src/shared/config/env.ts` and `grep -q "export const clientSchema" src/shared/config/env.ts`.
7. At least 4 atomic commits: `test(01-02): add failing tests for error registry`, `feat(01-02): implement PRD §5 error registry`, `test(01-02): add failing tests for env validation`, `feat(01-02): implement Zod env validation`.
</verification>

<success_criteria>
- [ ] 35 `.gitkeep` files under `src/contexts/` (7 × 5 = 35)
- [ ] 3 `.gitkeep` files under `src/shared/{db,events,adapters}/`
- [ ] `src/shared/config/errors.ts` exports `ErrorCode` (21 codes), `ErrorBody` type, `errorResponse` function
- [ ] Internal codes (`cost_ceiling_reached`, `breaker_open`) surface as `provider_unavailable` in response body
- [ ] `src/shared/config/env.ts` exports `serverSchema`, `clientSchema`, `serverEnv`, `clientEnv`
- [ ] `IDENTIFICATION_PROVIDER_MODE` rejects invalid values + defaults to `stub`
- [ ] Empty-string `NEXT_PUBLIC_SENTRY_DSN` / `NEXT_PUBLIC_POSTHOG_KEY` parse to `undefined`
- [ ] `.env.example` contains all 19 required PRD §20 variables
- [ ] `.env.example` contains NO secret-like values
- [ ] `vitest.config.ts` updated with `setupFiles: ["tests/unit/setup-env.ts"]`
- [ ] `pnpm test:unit` passes all 4 unit test files
- [ ] `pnpm typecheck` exits 0
- [ ] TDD cadence: ≥4 atomic commits (RED + GREEN for errors, RED + GREEN for env)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md` documenting:
- Count of scaffold files produced (exact: 35 + 3 = 38)
- All 21 error codes + their HTTP statuses
- Env schemas shape (server 7 keys, client 5 keys)
- Confirmation that `vitest.config.ts` was edited to add `setupFiles`
- Reference to next plans (01-03, 01-04 in Wave 2)
</output>
