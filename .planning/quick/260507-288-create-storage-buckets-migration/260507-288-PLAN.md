---
quick_id: 260507-288
description: Create Drizzle migration for Supabase Storage buckets
status: ready
created: 2026-05-07
---

# Quick Task 260507-288 — Create Storage buckets migration

## Goal

Add an idempotent Drizzle SQL migration that creates/updates required Supabase Storage buckets for hosted preview and production projects, matching `supabase/config.toml`.

## Scope

- Add next migration under `drizzle/migrations/`.
- Use `insert ... on conflict (id) do update` against `storage.buckets`.
- Buckets: `plant-photos`, `plant-thumbnails`, `data-exports`.
- Preserve private buckets (`public=false`), file size limits, and MIME allowlists.

## Verify

- Inspect generated SQL.
- Optionally run TypeScript checks if scripts are touched (not expected).
