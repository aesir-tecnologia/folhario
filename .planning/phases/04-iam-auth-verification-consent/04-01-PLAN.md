---
phase: 04-iam-auth-verification-consent
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/04-iam-auth-verification-consent/04-PREREQ-AUDIT.md
autonomous: false
requirements: []
must_haves:
  truths:
    - "Phase 2 deliverables required by Phase 4 are verified present"
    - "Phase 3 design tokens + UI primitives are verified present per D-33 (Phase 3 absence is a HARD BLOCKER at execute-time unless --allow-missing-phase-3 override is supplied)"
    - "Plan 02+ executor sees a written audit document describing exactly which Phase 2/3 artifacts exist"
    - "If anything is missing, execution blocks until the user runs the missing phase first"
    - "D-33: at plan-phase time the audit is non-fatal (warn only); at execute-phase time the audit FAILS without --allow-missing-phase-3 override"
  artifacts:
    - path: ".planning/phases/04-iam-auth-verification-consent/04-PREREQ-AUDIT.md"
      provides: "Written audit of Phase 2 + Phase 3 prerequisite state"
      contains: "Status: GO or BLOCKED"
  key_links:
    - from: ".planning/STATE.md"
      to: "Phase 2/3 completion status"
      via: "STATE.md `progress.completed_phases` + per-phase plan completion table"
      pattern: "Phase 2.*Plans Complete"
---

<objective>
Verify that the Phase 2 (Data Layer & Bounded Contexts) and Phase 3 (Design System & App Shell) deliverables Phase 4 inherits are actually present in the worktree before any Phase 4 implementation begins.

Purpose: Phase 4 RESEARCH.md "Phase 2 Inheritance Audit" lists ~13 Phase 2 deliverables Phase 4 depends on. STATE.md reports Phase 2 = 0/10 plans complete and Phase 3 has not been planned. UI-SPEC.md:38 explicitly flags Phase 3 as a hard prerequisite. **Per resolved D-33** this audit is now a hard execute-time block: at plan-phase time the audit issues a warning only (so planning can proceed); at execute-phase time the audit FAILS the run unless `--allow-missing-phase-3` is supplied to the audit task. This plan is a one-task gate that audits the worktree, writes a PREREQ-AUDIT.md document with a binary GO/BLOCKED verdict, and — if BLOCKED — exits non-zero so the executor halts before any Phase 4-specific work runs.

Output: `.planning/phases/04-iam-auth-verification-consent/04-PREREQ-AUDIT.md` containing concrete file/table existence checks and a final `Status: GO` or `Status: BLOCKED — run /gsd-execute-phase 2 (and 3) first` line.
</objective>

<execution_context>
@/Users/machado/Projects/folhario/.claude/get-shit-done/workflows/execute-plan.md
@/Users/machado/Projects/folhario/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-iam-auth-verification-consent/04-CONTEXT.md
@.planning/phases/04-iam-auth-verification-consent/04-RESEARCH.md
@.planning/phases/04-iam-auth-verification-consent/04-PATTERNS.md
@.planning/phases/04-iam-auth-verification-consent/04-UI-SPEC.md

<interfaces>
<!-- The audit checks for the EXISTENCE of these files/tables/exports. Each check below is the contract Plan 02+ assumes is true. -->

Phase 2 expected deliverables (from RESEARCH.md "Phase 2 Inheritance Audit" lines 1086-1107):

1. `src/contexts/iam/infrastructure/db/schema.ts` exists and exports `users` table (Drizzle pgTable) with at least: id, email, age_confirmed_at, timezone, partner_code, deletion_requested_at, created_at columns (Phase 4 ALTER will add `email_verified_at`).
2. `src/contexts/iam/infrastructure/db/schema.ts` (or peer file in iam) exports `consentLogs` (or equivalent) with `purpose` literal union including BOTH "terms_of_service" AND "privacy_policy", and `source` literal union including "signup" AND "settings".
3. `policy_versions` table exists with `is_current` boolean column.
4. `subscriptions` table exists with `status` enum including "trialing".
5. `idempotency_keys` table exists.
6. `partner_store` table exists (D-32 — signup partner_code lookup target).
7. Database `auth.users → public.users` insert trigger exists (D-35) — visible via `SELECT tgname FROM pg_trigger`.
8. Runtime DB client `src/contexts/iam/infrastructure/db/client.ts` (or `src/shared/db/client.ts`) exists, uses `postgres-js` with `prepare: false`.
9. `src/contexts/iam/infrastructure/auth-adapter.ts` (or equivalent) exists and exports JWT verification functions (`verifyJWT`, `getUserById`).
10. `src/proxy.ts` is API-aware (does NOT skip `/api/*`) — D-34 prerequisite for Phase 4 D-21.
11. RLS enabled on `users`, `consent_logs`, `subscriptions`, `idempotency_keys` tables.

Phase 3 expected deliverables (UI-SPEC.md:38 + D-33):

1. Design tokens (Paper Cream + Night Cream palettes) defined in `src/shared/ui/tokens/*` (or Tailwind config) — D-33 references this exact path.
2. Source Serif 4 + Plus Jakarta Sans fonts loaded.
3. Focus ring (3px Canopy @ 40% opacity, 2px offset, 8px radius) applied globally — Plan 10 MUST NOT hand-roll this per D-33.
4. Button, Input, Card primitives exist per Phase 3 plan — Plan 10 depends on these per D-33.
5. Bottom-nav primitive exists.
</interfaces>
</context>

<threat_model>

## Trust Boundaries

| Boundary                      | Description                                                              |
| ----------------------------- | ------------------------------------------------------------------------ |
| Plan 01 audit script ↔ Phase 4 | Audit must NOT silently pass when prereqs are missing — that would surface a phantom-pass to downstream plans. |

## STRIDE Threat Register

| Threat ID | Category   | Component                          | Disposition | Mitigation Plan                                                                                                                                                                                                                                                                                       |
| --------- | ---------- | ---------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-04-01-01 | Tampering  | PREREQ-AUDIT.md verdict line       | mitigate    | The audit task uses `grep` / `psql` / `ls` checks; the final Status line is computed from those checks, not human-judged. Acceptance criteria require the audit file to contain BOTH "Status: GO" or "Status: BLOCKED" verbatim AND a per-check checklist with PASS/FAIL marks. No subjective approval allowed. |
| T-04-01-02 | Repudiation | Audit decision lacks evidence      | mitigate    | Every check in the audit document references the file path or SQL it ran. Reviewers can re-execute the same commands and verify the verdict.                                                                                                                                                          |
| T-04-01-03 | Information Disclosure | Audit script logs DB credentials   | accept      | The audit uses `DATABASE_POOL_URL` from `.env.local` for local checks only; no credentials are written to the audit document. Sentry scrub already protects logs (Phase 1 D-22 / LGPD-13).                                                                                                              |
| T-04-01-04 | Tampering  | D-33 override misuse — operator passes `--allow-missing-phase-3` to bypass real Phase 3 absence | mitigate | Override flag is ALWAYS recorded in the verdict line as `Status: GO (override --allow-missing-phase-3 — Phase 3 will hand-roll in Plan 10 with debt entry)`. Audit document includes a "Debt entry" section listing every Phase 3 check that failed under override. Plan 10's SUMMARY must reference these for visibility. |

</threat_model>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Audit Phase 2 + Phase 3 prerequisites and write PREREQ-AUDIT.md</name>
  <files>.planning/phases/04-iam-auth-verification-consent/04-PREREQ-AUDIT.md</files>
  <read_first>
    - `.planning/STATE.md` — current phase progress (Phase 2 = 0/10 expected per orchestrator handoff)
    - `.planning/phases/04-iam-auth-verification-consent/04-RESEARCH.md` lines 1086-1107 — full Phase 2 Inheritance Audit table
    - `.planning/phases/04-iam-auth-verification-consent/04-UI-SPEC.md` line 38 — Phase 3 prerequisite flag
    - `.planning/phases/04-iam-auth-verification-consent/04-PATTERNS.md` lines 933-965 — Hard Prerequisites the Planner Surfaces
    - `src/contexts/iam/infrastructure/` directory listing — current empty/.gitkeep state confirms Phase 2 hasn't shipped IAM infra
    - `src/proxy.ts` lines 1-15 — confirm whether Phase 2 D-34 (API-aware proxy) shipped
    - Cite decision IDs: D-19 (AuthAdapter scope), D-21 (gate enforcement), D-22 (email_verified_at source of truth), D-25 (signup atomicity), **D-33 (Phase 3 hard execute-time block with override)**
  </read_first>
  <action>
    Run a series of concrete existence checks and write each result into `.planning/phases/04-iam-auth-verification-consent/04-PREREQ-AUDIT.md` as a markdown checklist. Then compute a verdict on the last line.

    **Override detection (read FIRST before computing verdict):**

    Look for the env var `GSD_ALLOW_MISSING_PHASE_3` (or a CLI argument `--allow-missing-phase-3` if the orchestrator passed one through). If set to `"true"`, set local variable `phase3Override = true`. Otherwise `false`.

    **Phase 2 file/code checks (each must produce PASS or FAIL with the command output):**

    1. `test -f src/contexts/iam/infrastructure/db/schema.ts && grep -E 'export const users|users.*pgTable' src/contexts/iam/infrastructure/db/schema.ts` — PASS if file exists AND exports `users` table.
    2. `grep -E 'consentLogs|consent_logs.*pgTable' src/contexts/iam/infrastructure/db/schema.ts` — PASS if `consentLogs` table is exported.
    3. `grep -E "purpose.*'terms_of_service'.*'privacy_policy'|'privacy_policy'.*'terms_of_service'" src/contexts/iam/infrastructure/db/schema.ts` — PASS if purpose union includes both literals.
    4. `grep -E "source.*'signup'.*'settings'|'settings'.*'signup'" src/contexts/iam/infrastructure/db/schema.ts` — PASS if source union includes both.
    5. `grep -E 'policyVersions|policy_versions.*pgTable' src/contexts/iam/infrastructure/db/schema.ts && grep -E 'is_current|isCurrent' src/contexts/iam/infrastructure/db/schema.ts` — PASS if `policy_versions` table with `is_current` column.
    6. `grep -E 'subscriptions.*pgTable' src/contexts/iam/infrastructure/db/schema.ts && grep -E "'trialing'" src/contexts/iam/infrastructure/db/schema.ts` — PASS if `subscriptions` table with status `trialing`.
    7. `grep -rE 'idempotencyKeys|idempotency_keys.*pgTable' src/contexts/ src/shared/db/` — PASS if `idempotency_keys` table exists somewhere.
    8. `grep -rE 'partnerStore|partner_store.*pgTable' src/contexts/` — PASS if `partner_store` table exists (D-32 — signup partner_code lookup target). Phase 2 D-13 seeds it.
    9. `test -f src/contexts/iam/infrastructure/db/client.ts || test -f src/shared/db/client.ts` AND `grep -E "prepare:\s*false" {found-file}` — PASS if runtime DB client exists with `{ prepare: false }`.
    10. `test -f src/contexts/iam/infrastructure/auth-adapter.ts` (or `src/contexts/iam/infrastructure/supabase-auth-adapter.ts`) AND `grep -E 'verifyJWT|getUserById|getCurrentUser' {found-file}` — PASS if AuthAdapter exists with required exports.
    11. `grep -E 'matcher|/api/v1' src/proxy.ts` — PASS if proxy is API-aware (covers `/api/v1/*`); FAIL if proxy still excludes `/api/*` per Phase 1 D-24 baseline.
    12. If `pnpm db:start` is running locally, query `psql` for: `SELECT relname, relrowsecurity FROM pg_class WHERE relname IN ('users','consent_logs','subscriptions','idempotency_keys') AND relnamespace = 'public'::regnamespace;` — PASS if RLS is `t` (true) on all 4 tables. If `pnpm db:start` is NOT running, mark this check as NOT-VERIFIED with note "run `pnpm db:start` and re-run audit".
    13. `psql -c "SELECT tgname FROM pg_trigger WHERE tgname LIKE '%user%';" | grep -E 'on_auth_user_created|public_users_insert'` — PASS if Phase 2 D-35 trigger exists.

    **Phase 3 UI primitive checks (D-33: HARD BLOCKERS at execute-time unless override):**

    14. `find src/shared/ui/tokens/ -type f \( -name '*.css' -o -name '*.ts' -o -name 'tokens.*' \) 2>/dev/null | xargs grep -l 'Paper Cream\|#FBF7EF\|paper-cream' 2>/dev/null` — PASS if Paper Cream token defined in the D-33-mandated token directory.
    15. `grep -rE 'Source Serif|Plus Jakarta' src/app/ src/contexts/ src/shared/` — PASS if fonts referenced.
    16. `test -f src/shared/ui/Button.tsx || test -f src/shared/ui/components/Button.tsx` AND `test -f src/shared/ui/Input.tsx || test -f src/shared/ui/components/Input.tsx` AND `test -f src/shared/ui/Card.tsx || test -f src/shared/ui/components/Card.tsx` — PASS if Phase 3 UI primitives (Button, Input, Card) exist per D-33.
    17. Global focus ring (3px Canopy @ 40% opacity, 8px radius) defined in CSS: `grep -rE '(:focus-visible|focus-visible).*outline.*3px' src/app/globals.css src/styles/ 2>/dev/null` — PASS if global focus ring is defined.
    18. `find src/app -name 'layout.tsx' -exec grep -l 'BottomNav\|bottom-nav' {} \;` — PASS if bottom-nav component is wired in a layout.

    **Audit document structure** (create the file with this exact skeleton):

    ```markdown
    # Phase 4 — Prerequisite Audit

    **Audited:** {ISO-8601 UTC timestamp with Z}
    **Auditor:** Claude (plan 04-01 task 1)
    **Override flag:** {phase3Override ? "GSD_ALLOW_MISSING_PHASE_3=true" : "(none)"}

    ## Phase 2 Deliverables (HARD blockers — no override available)

    | # | Check | Status | Evidence |
    |---|-------|--------|----------|
    | 1 | iam/infrastructure/db/schema.ts exports `users` | PASS / FAIL | {grep output excerpt} |
    | 2 | schema.ts exports `consentLogs` | PASS / FAIL | {output} |
    | ... | (one row per check 1-13 above) | | |

    ## Phase 3 Deliverables (HARD blockers per D-33 unless override)

    | # | Check | Status | Evidence |
    |---|-------|--------|----------|
    | 14 | Paper Cream token in src/shared/ui/tokens/ | PASS / FAIL | {output} |
    | 15 | Source Serif + Plus Jakarta fonts loaded | PASS / FAIL | {output} |
    | 16 | Button + Input + Card primitives exist | PASS / FAIL | {output} |
    | 17 | Global focus ring defined | PASS / FAIL | {output} |
    | 18 | Bottom-nav primitive wired | PASS / FAIL | {output} |

    ## Verdict

    Status: GO

    or

    Status: GO (override --allow-missing-phase-3 — Phase 3 will hand-roll in Plan 10 with debt entry)

    or

    Status: BLOCKED — run /gsd-execute-phase 2 first
    Missing: <comma-separated check numbers>

    or

    Status: BLOCKED — run /gsd-execute-phase 3 first (or pass --allow-missing-phase-3 to acknowledge hand-rolling in Plan 10)
    Missing: <comma-separated check numbers from Phase 3 table only>

    ## Debt entry (only present if Status: GO with --allow-missing-phase-3 override)

    Phase 3 deliverables NOT shipped (will be hand-rolled in Plan 10 within UI-SPEC §17 brand tokens):
    - <numbered list of FAIL checks 14-18>

    ## Resolution path (when BLOCKED)

    - **Phase 2 missing:** Stop Phase 4 execution → run /gsd-execute-phase 2 → re-run audit → resume Phase 4.
    - **Phase 3 missing AND no override:** Stop Phase 4 execution → either (a) run /gsd-execute-phase 3 first, OR (b) re-run with `GSD_ALLOW_MISSING_PHASE_3=true` (or `--allow-missing-phase-3`) to acknowledge hand-rolled Plan 10 primitives.
    ```

    **Verdict computation rule (NON-NEGOTIABLE — D-33 hard block at execute-time):**

    - **Phase 2 checks (1-13) are ALWAYS HARD BLOCKERS.** If ANY of checks 1-13 is FAIL, write `Status: BLOCKED — run /gsd-execute-phase 2 first` AND list the failing check numbers in the `Missing:` line.
    - **Phase 3 checks (14-18) are HARD BLOCKERS at execute-phase time per D-33.** If ANY of checks 14-18 is FAIL:
      - If `phase3Override === true` (env `GSD_ALLOW_MISSING_PHASE_3=true` or CLI flag) → write `Status: GO (override --allow-missing-phase-3 — Phase 3 will hand-roll in Plan 10 with debt entry)` AND populate the "Debt entry" section.
      - If `phase3Override === false` → write `Status: BLOCKED — run /gsd-execute-phase 3 first (or pass --allow-missing-phase-3 to acknowledge hand-rolling in Plan 10)` AND list the failing check numbers.
    - The NOT-VERIFIED status on check #12 (RLS, requires `pnpm db:start`) is treated as FAIL for blocking purposes — re-run audit after starting the local Postgres stack.
    - **Verdict examples:**
      - All 18 checks PASS → `Status: GO`.
      - All Phase 2 PASS, ALL Phase 3 FAIL, override = true → `Status: GO (override --allow-missing-phase-3 — Phase 3 will hand-roll in Plan 10 with debt entry)`.
      - Any Phase 2 FAIL → `Status: BLOCKED — run /gsd-execute-phase 2 first` + Missing list.
      - All Phase 2 PASS, any Phase 3 FAIL, override = false → `Status: BLOCKED — run /gsd-execute-phase 3 first (or pass --allow-missing-phase-3 ...)` + Missing list.

    **At plan-phase time (this audit being run by the planner):** the audit STILL writes the file, but if BLOCKED, the planner logs a warning and continues planning. Only execute-phase respects the BLOCKED verdict. (The planner-time behavior is enforced by the orchestrator, not this task.)

    **At execute-phase time:** the executor reads the verdict line from PREREQ-AUDIT.md. If `Status: BLOCKED`, the executor halts. If `Status: GO` (with or without override), the executor proceeds.

    **If BLOCKED:** the task ALSO emits `process.exitCode = 1` (or equivalent) so the executor halts. The orchestrator surfaces the audit document path to the user. DO NOT attempt to "patch in" missing Phase 2 deliverables — Phase 4 plans 02-11 assume Phase 2 already shipped (per planner_authority_limits in the orchestrator instructions).

    Implementation choices:
    - Use shell commands directly via `Bash` tool (this is an audit, not production code).
    - Capture stdout into the audit document; truncate noisy output to the first 3 lines per check.
    - For psql checks, use `DATABASE_POOL_URL` from `.env.local` (already populated by Phase 1 plan 01-04). If the env var is empty, mark psql-dependent checks as NOT-VERIFIED.

    Per D-19 (AuthAdapter scope), D-21 (gate enforcement), D-22 (`email_verified_at` source of truth), D-25 (signup atomicity), D-32 (PartnerStore lookup at signup), D-33 (Phase 3 hard execute-time block with override), and D-33 from Phase 2 (JWKS verification): all of these decisions ASSUME Phase 2 shipped. The audit makes that assumption falsifiable.
  </action>
  <what-built>
    A markdown audit document at `.planning/phases/04-iam-auth-verification-consent/04-PREREQ-AUDIT.md` with 18 concrete file/code/database checks and a final binary verdict line. Phase 3 absence is now a hard execute-time block per D-33, with `--allow-missing-phase-3` (env `GSD_ALLOW_MISSING_PHASE_3=true`) as the documented override.
  </what-built>
  <how-to-verify>
    1. Open `.planning/phases/04-iam-auth-verification-consent/04-PREREQ-AUDIT.md`.
    2. Confirm the document has all 18 checks listed in the two tables.
    3. Confirm the final verdict line is one of the four documented forms (`Status: GO`, `Status: GO (override ...)`, `Status: BLOCKED — Phase 2 ...`, `Status: BLOCKED — Phase 3 ...`).
    4. If `Status: GO`: phase 4 may proceed to plan 04-02. Phase 3 was confirmed shipped.
    5. If `Status: GO (override --allow-missing-phase-3 ...)`: phase 4 proceeds, Plan 10 hand-rolls primitives, debt entry tracked.
    6. If `Status: BLOCKED — Phase 2 ...`: stop. Run `/gsd-execute-phase 2` first.
    7. If `Status: BLOCKED — Phase 3 ...`: stop. Either run `/gsd-execute-phase 3` OR re-run with `GSD_ALLOW_MISSING_PHASE_3=true` to acknowledge.
  </how-to-verify>
  <acceptance_criteria>
    - File exists: `test -f .planning/phases/04-iam-auth-verification-consent/04-PREREQ-AUDIT.md`
    - File contains the literal heading: `grep -c '^# Phase 4 — Prerequisite Audit' .planning/phases/04-iam-auth-verification-consent/04-PREREQ-AUDIT.md` returns 1
    - File contains 18 numbered check rows: `grep -cE '^\| [0-9]+ \|' .planning/phases/04-iam-auth-verification-consent/04-PREREQ-AUDIT.md` returns 18
    - File contains exactly one verdict line: `grep -cE '^Status: (GO|BLOCKED)' .planning/phases/04-iam-auth-verification-consent/04-PREREQ-AUDIT.md` returns 1
    - File records the override flag state: `grep -cE '^\*\*Override flag:\*\*' .planning/phases/04-iam-auth-verification-consent/04-PREREQ-AUDIT.md` returns 1
    - If verdict is `Status: GO` (without override): every check row has PASS in the Status column — `grep -cE '\| FAIL \|' .planning/phases/04-iam-auth-verification-consent/04-PREREQ-AUDIT.md` returns 0.
    - If verdict is `Status: GO (override --allow-missing-phase-3 ...)`: file contains a "Debt entry" section listing every Phase 3 FAIL check — `grep -cE '^## Debt entry' .planning/phases/04-iam-auth-verification-consent/04-PREREQ-AUDIT.md` returns 1.
    - If verdict is `Status: BLOCKED`: file contains a `Missing:` line listing at least one failing check number — `grep -E '^Missing: ' .planning/phases/04-iam-auth-verification-consent/04-PREREQ-AUDIT.md` returns at least one match.
    - User has explicitly typed "approved" OR "BLOCKED-acknowledged-stopping" in the resume signal.
  </acceptance_criteria>
  <resume-signal>
Type "approved" if the audit verdict is `Status: GO` or `Status: GO (override --allow-missing-phase-3 ...)`, OR type "BLOCKED-acknowledged-stopping" if the verdict is `Status: BLOCKED` (Phase 2 or Phase 3 is missing — run the missing /gsd-execute-phase first OR re-run with `GSD_ALLOW_MISSING_PHASE_3=true`).
  </resume-signal>
</task>

</tasks>

<verification>
- `04-PREREQ-AUDIT.md` exists in the phase directory.
- The audit contains all 18 checks with concrete evidence.
- The final verdict is binary (GO with optional override / BLOCKED) and matches the per-check states.
- If verdict is BLOCKED, no other Phase 4 plan executes until Phase 2/3 ship OR override is supplied.
- The override flag state is recorded in the audit document.
</verification>

<success_criteria>
- A reviewer reading `04-PREREQ-AUDIT.md` can re-run every command in the Evidence column and reproduce the verdict.
- The verdict is "GO" only when all Phase 2 + Phase 3 deliverables Phase 4 inherits are demonstrably present (or Phase 3 absence is acknowledged via override).
- A "BLOCKED" verdict halts Phase 4 execution before any irreversible work happens.
- D-33 hard block at execute-time is enforced; planner-time warning is non-fatal.
</success_criteria>

<output>
After completion, create `.planning/phases/04-iam-auth-verification-consent/04-01-SUMMARY.md` capturing:
- The verdict (GO / GO with override / BLOCKED).
- A list of which Phase 2 / Phase 3 deliverables were missing (if any).
- If override used: the debt entry items so Plan 10's executor knows what to hand-roll.
- Any non-trivial discoveries about the prerequisites (e.g. "Phase 2 schema added `email_verified_at` itself — Phase 4 plan 02 ALTER becomes a no-op").
</output>
</content>
