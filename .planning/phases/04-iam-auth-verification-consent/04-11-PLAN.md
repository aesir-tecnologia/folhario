---
phase: 04-iam-auth-verification-consent
plan: 11
type: execute
wave: 9
depends_on: ["04-10"]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - docs/CAVE-PRD.md
autonomous: false
requirements: []
must_haves:
  truths:
    - "REQUIREMENTS.md AUTH-14 wording matches resolved Q-AUTH-14 (cookie + refresh-token, NOT JWT denylist)"
    - "ROADMAP.md Phase 4 success criterion 3 matches the resolved AUTH-14 wording"
    - "PRD §4 User schema includes email_verified_at TIMESTAMPTZ NULL (resolved Open Question 3)"
    - "AUTH-v2-02 (logout-all-devices) explicitly cross-referenced from AUTH-14 as the post-MVP follow-up"
    - "REQUIREMENTS.md traceability table marks AUTH-01..15 + INFRA-10 + NOTIF-01..02 + UI-13 with their Phase 4 plan completions"
    - "All Phase 4 requirements are marked Complete with the matching plan numbers"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated AUTH-14 wording + AUTH-v2-02 cross-reference + Phase 4 status updates"
    - path: ".planning/ROADMAP.md"
      provides: "Updated Phase 4 SC-3 wording + plan list"
    - path: "docs/CAVE-PRD.md"
      provides: "Updated User schema (§4) with email_verified_at column"
  key_links:
    - from: ".planning/REQUIREMENTS.md AUTH-14"
      to: "AUTH-v2-02"
      via: "cross-reference: 'Logout-all-devices is post-MVP per AUTH-v2-02'"
      pattern: "AUTH-v2-02"
---

<objective>
Reconcile the planning + product documentation with the resolved Phase 4 decisions: (1) amend AUTH-14 wording in REQUIREMENTS.md and the matching ROADMAP.md Phase 4 SC-3 to reflect resolved Q-AUTH-14 (cookie + refresh-token revoke, NOT JWT invalidation); (2) add `email_verified_at TIMESTAMPTZ NULL` to PRD §4 User schema (resolved Open Question 3 — Plan 02 added it via ALTER TABLE; this plan amends the data model doc to match); (3) cross-reference AUTH-v2-02 from AUTH-14 as the post-MVP follow-up; (4) mark Phase 4 requirement IDs Complete in REQUIREMENTS.md traceability table; (5) update ROADMAP.md Phase 4 plan list to reflect the 11 plans actually created.

Purpose: Documentation drift is a known anti-pattern (per Plan 01-08's prior reconciliation work). Phase 4 makes intentional documentation amendments — the system of record (CONTEXT.md, RESEARCH.md, PLAN files) reflects the truth; this plan synchronizes REQUIREMENTS.md, ROADMAP.md, and the PRD to match.

Output: 3 modified planning/PRD files; 0 new code.
</objective>

<execution_context>
@/Users/machado/Projects/folhario/.claude/get-shit-done/workflows/execute-plan.md
@/Users/machado/Projects/folhario/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/04-iam-auth-verification-consent/04-RESEARCH.md
@.planning/phases/04-iam-auth-verification-consent/04-07-SUMMARY.md

<interfaces>
<!-- Files this plan modifies (with the lines that need editing) -->

`.planning/REQUIREMENTS.md`:
- Line 26 (AUTH-14): replace "revokes that JWT and its push subscription only" with "clears the device's cookie and revokes its refresh token; the existing JWT continues to be valid until its `exp` (≤1h)"
- AUTH-14 entry: append cross-reference: "**See AUTH-v2-02** for post-MVP logout-all-devices."
- Traceability table: mark AUTH-01..15 + INFRA-10 + NOTIF-01..02 + UI-13 as Complete with their plan numbers.

`.planning/ROADMAP.md`:
- Lines 119-121 (Phase 4 success criterion 3): replace "log out of the current device (revoking that JWT and its push subscription only)" with "log out of the current device (clears the device cookie and revokes its refresh token; the existing JWT remains valid until its `exp` ≤1h — see AUTH-v2-02 for post-MVP logout-all-devices)"
- Phase 4 plan list: replace `**Plans**: TBD` with the 11 plans actually shipped.
- Progress table: update Phase 4 row to `11/11 — Complete`.

`docs/CAVE-PRD.md`:
- §4 User schema: add `email_verified_at TIMESTAMPTZ NULL` column to the User table description.
</interfaces>

</context>

<threat_model>

| Threat ID  | Category    | Component                              | Disposition | Mitigation Plan                                                                                           |
| ---------- | ----------- | -------------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------- |
| T-04-11-01 | Repudiation | Doc-drift creates ambiguous semantics  | mitigate    | This plan IS the doc-fix. Acceptance criteria require grep-verifying the exact updated wording in 3 files. |

</threat_model>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Update REQUIREMENTS.md AUTH-14 wording + traceability + cross-reference; update ROADMAP.md Phase 4 SC-3 + plan list; update PRD §4 User schema</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, docs/CAVE-PRD.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` lines 26 (current AUTH-14) and lines 252-253 (AUTH-v2-02)
    - `.planning/REQUIREMENTS.md` lines 325-525 (traceability table — Phase 4 entries)
    - `.planning/ROADMAP.md` lines 113-124 (Phase 4 entry + SC-3)
    - `.planning/phases/04-iam-auth-verification-consent/04-RESEARCH.md` Pitfall 12 lines 908-914 (full rationale for AUTH-14 wording change)
    - `.planning/phases/04-iam-auth-verification-consent/04-07-SUMMARY.md` (Plan 07 confirmed integration test asserts cookie + refresh fail, NOT JWT rejection)
    - `docs/CAVE-PRD.md` §4 (User table schema — currently missing email_verified_at)
  </read_first>
  <action>
    **1. REQUIREMENTS.md changes:**

    a. Find AUTH-14 entry (current line 26):
    ```
    - [ ] **AUTH-14**: User can log out of the current device (revokes that JWT and its push subscription only) (§14)
    ```
    Replace with:
    ```
    - [x] **AUTH-14**: User can log out of the current device — clears the device's cookie and revokes its refresh token; the existing JWT continues to be valid until its `exp` (≤1h on Supabase default). See AUTH-v2-02 for post-MVP logout-all-devices (global JWT revocation). (§14, resolved Phase 4 Q-AUTH-14 on 2026-04-26)
    ```

    b. Find AUTH-v2-02 entry (current line 253):
    ```
    - **AUTH-v2-02**: Logout-all-devices (global JWT revocation)
    ```
    Replace with:
    ```
    - **AUTH-v2-02**: Logout-all-devices (global JWT revocation). Cross-referenced from AUTH-14 — Phase 4 ships single-device logout (cookie + refresh-token); MVP accepts the JWT exp window as the effective per-device logout latency. Post-MVP requires either a JWT denylist table or Supabase Auth Hooks integration.
    ```

    c. Update traceability table (lines 325-525) — mark Phase 4 requirement IDs as `Complete (Plan X)`:
    - AUTH-01 → `Complete (Plan 06)`
    - AUTH-02 → `Complete (Plan 06)` (verification gate is implemented in shared/api/auth.ts and Plan 06 wires the gate at /me + /resend-verification + /verify route handlers)
    - AUTH-03 → `Complete (Plan 09)`
    - AUTH-04 → `Complete (Plan 06)`
    - AUTH-05 → `Complete (Plan 07)`
    - AUTH-06 → `Complete (Plan 03 schema + Plan 06 enforcement)`
    - AUTH-07 → `Complete (Plan 06)`
    - AUTH-08 → `Complete (Plan 03 schema + Plan 06 enforcement)`
    - AUTH-09 → `Complete (Plan 06)`
    - AUTH-10 → `Complete (Plan 03)` (per-IP throttle middleware ships Plan 03; route handlers Plans 06-09 wrap)
    - AUTH-11 → `Complete (Plan 08)`
    - AUTH-12 → `Complete (Plan 08)`
    - AUTH-13 → `Complete (Plan 09)`
    - AUTH-14 → `Complete (Plan 07)`
    - AUTH-15 → `Complete (Plan 10)`
    - INFRA-10 → `Complete (Plan 04)`
    - NOTIF-01 → `Complete (Plan 05)`
    - NOTIF-02 → `Complete (Plan 05)`
    - UI-13 → `Complete (Plan 10)`

    d. Update Phase 4 per-phase total at the bottom of REQUIREMENTS.md (line 533):
    ```
    Phase 4 (IAM): 19 — AUTH-01..15 + INFRA-10 + NOTIF-01,02 + UI-13
    ```
    No change needed in this line; the per-phase total is correct.

    **2. ROADMAP.md changes:**

    a. Find Phase 4 success criterion 3 (line 120):
    ```
      3. Returning users log in with email+password via per-device JWT (no server sessions), log out of the current device (revoking that JWT and its push subscription only), request a password reset from a public endpoint that always returns 200 (no enumeration) and — when the email exists — receive a Resend email with a single-use hashed token expiring in 1h that lets them set a new password while existing JWTs remain valid.
    ```
    Replace with:
    ```
      3. Returning users log in with email+password via per-device JWT (no server sessions), log out of the current device (clears the device cookie and revokes its refresh token; the existing JWT remains valid until its `exp` ≤1h — see AUTH-v2-02 for post-MVP logout-all-devices), request a password reset from a public endpoint that always returns 200 (no enumeration) and — when the email exists — receive a Resend email with a single-use hashed token expiring in 1h that lets them set a new password while existing JWTs remain valid.
    ```

    b. Find Phase 4 plan list (currently `**Plans**: TBD` at line 122):
    Replace with:
    ```
    **Plans**: 11 plans

    Plans:
    - [ ] 04-01-PLAN.md -- Phase 2/3 prerequisite gate
    - [ ] 04-02-PLAN.md -- Schema additions (email_verified_at + 3 tables) + drizzle-kit push
    - [ ] 04-03-PLAN.md -- Shared foundation (env vars, helpers, Zod, fixtures, locale)
    - [ ] 04-04-PLAN.md -- Inngest serve handler + 8 functions registered (1 real cron + 7 stubs)
    - [ ] 04-05-PLAN.md -- Resend onboarding + 3 React Email templates + send-email function
    - [ ] 04-06-PLAN.md -- Signup orchestration + verify route + resend-verification + welcome-back
    - [ ] 04-07-PLAN.md -- Login + logout (single-device per resolved Q-AUTH-14)
    - [ ] 04-08-PLAN.md -- Password reset (always-200 + Inngest async lookup + consume)
    - [ ] 04-09-PLAN.md -- Change password + me endpoints + OAuth callback + oauth-complete
    - [ ] 04-10-PLAN.md -- UI surfaces (auth pages + Settings shell + UnverifiedBlocker + root layout gate)
    - [ ] 04-11-PLAN.md -- Doc-fixes (AUTH-14 wording, PRD §4, AUTH-v2-02 cross-reference)
    ```

    c. Update progress table (around line 250):
    Find `| 4. IAM — Auth, Verification, Consent | 0/TBD | Not started | - |` and prepare to update its plan count to `0/11` (the table will reflect actual completion as plans ship; this plan only sets the denominator from `TBD` to `11`).

    Replace with: `| 4. IAM — Auth, Verification, Consent | 0/11 | Ready to execute | - |`

    **3. docs/CAVE-PRD.md changes:**

    a. Find §4 User table schema (find "User" header in §4). Add `email_verified_at TIMESTAMPTZ NULL` column to the schema description, with an inline annotation:
    ```
    email_verified_at TIMESTAMPTZ NULL  -- Set on /auth/verify (email+password) or /auth/oauth-complete (Google OAuth pre-verified per AUTH-03). Never use auth.users.email_confirmed_at for product gating per Phase 4 D-22.
    ```

    Position it logically after the existing `age_confirmed_at` column for parity (both are nullable signup-completion timestamps).

    Per resolved Open Question 3: Phase 4 added the column via ALTER TABLE in Plan 02; this PRD doc-fix amends the data model doc so it matches the live database.
  </action>
  <what-built>
    Three documentation files updated with the resolved Phase 4 decisions:
    - REQUIREMENTS.md: AUTH-14 wording, AUTH-v2-02 cross-reference, traceability table.
    - ROADMAP.md: Phase 4 SC-3 wording, plan list, progress table denominator.
    - PRD §4: email_verified_at column added to User schema.
  </what-built>
  <how-to-verify>
    1. Run the grep checks in Acceptance Criteria.
    2. Read AUTH-14 in REQUIREMENTS.md — confirm wording matches resolved Q-AUTH-14.
    3. Read Phase 4 SC-3 in ROADMAP.md — confirm wording matches AUTH-14 (consistency).
    4. Read PRD §4 User table — confirm email_verified_at is documented.
    5. Confirm ROADMAP.md plan list lists all 11 Phase 4 plans by filename.
    6. Confirm AUTH-v2-02 in REQUIREMENTS.md references AUTH-14 explicitly.
  </how-to-verify>
  <acceptance_criteria>
    - REQUIREMENTS.md AUTH-14 wording: `grep -E "clears the device's cookie and revokes its refresh token" .planning/REQUIREMENTS.md` returns one match.
    - REQUIREMENTS.md AUTH-14 NO LONGER says "revokes that JWT": `grep -E "AUTH-14.*revokes that JWT" .planning/REQUIREMENTS.md` returns 0 matches.
    - REQUIREMENTS.md AUTH-v2-02 references AUTH-14: `grep -E "AUTH-v2-02.*Cross-referenced from AUTH-14|AUTH-14.*See AUTH-v2-02" .planning/REQUIREMENTS.md` returns at least one match.
    - REQUIREMENTS.md traceability marks AUTH-01..15 as Complete: `grep -cE "\| AUTH-(0[1-9]|1[0-5]) \| Phase 4 \| Complete" .planning/REQUIREMENTS.md` returns `15`.
    - REQUIREMENTS.md traceability marks INFRA-10, NOTIF-01, NOTIF-02, UI-13 as Complete: `grep -cE "\| (INFRA-10|NOTIF-0[12]|UI-13) \| Phase 4 \| Complete" .planning/REQUIREMENTS.md` returns `4`.
    - ROADMAP.md Phase 4 SC-3 has updated wording: `grep -E "clears the device cookie and revokes its refresh token" .planning/ROADMAP.md` returns one match.
    - ROADMAP.md Phase 4 plan list has all 11 plans: `grep -cE "04-[0-9]{2}-PLAN\\.md" .planning/ROADMAP.md` returns at least `11`.
    - ROADMAP.md progress table updated: `grep -E "4\\. IAM.*0/11" .planning/ROADMAP.md` returns one match.
    - PRD §4 includes email_verified_at: `grep -E "email_verified_at TIMESTAMPTZ" docs/CAVE-PRD.md` returns at least one match.
    - User has approved the doc-fix changes (read-through verification).
  </acceptance_criteria>
  <resume-signal>Type "approved" if all 3 file updates are accurate, or describe specific issues for revision.</resume-signal>
</task>

</tasks>

<verification>
- AUTH-14 wording reflects resolved Q-AUTH-14 in both REQUIREMENTS.md and ROADMAP.md SC-3.
- AUTH-v2-02 cross-references AUTH-14 explicitly.
- PRD §4 documents email_verified_at column.
- All 19 Phase 4 requirement IDs marked Complete in the traceability table.
- ROADMAP.md plan list has 11 plans.
</verification>

<success_criteria>
- A reviewer reading REQUIREMENTS.md AUTH-14 sees the same wording as the integration test asserts (cookie + refresh, not JWT rejection).
- A reviewer reading ROADMAP.md Phase 4 SC-3 sees the same wording as AUTH-14.
- The PRD User schema reflects what the live database has (email_verified_at).
</success_criteria>

<output>
After completion, create `.planning/phases/04-iam-auth-verification-consent/04-11-SUMMARY.md` capturing:
- The exact wording changes applied to REQUIREMENTS.md AUTH-14 + ROADMAP.md SC-3.
- Confirmation that all 19 Phase 4 requirement IDs are marked Complete.
- Phase 4 is officially complete; STATE.md will reflect 11/11 once execute-phase 4 completes.
</output>
</content>
</invoke>